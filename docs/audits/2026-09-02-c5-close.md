# C5 批次收口（2026-09-02）

> 类型：纠偏 C5 结束对照（[纠偏方案 §3 C5 验收](../plans/course-correction-2026-08.md) + §6 E 项）
> 基准：`origin/main` @ `c476fc2`（[#220](https://github.com/czmomocha/craftarena/pull/220) 静态盒阔相合入后）
> 本文件不是解冻令。冻结仍须人类对 E1–E14 全绿后明确解除。

## 1. C5 原验收

| 条 | 状态 |
|---|---|
| 最大生产文件 < 400 行 | **成立**。`game/src/**/*.gd` 与 `backend` 非测试 `*.ts` 由 `tools/dev-launcher/tests/e9_production_line_cap.test.ts` 锁死。测试 / addons / GUT 不在本口径。2026-08-26 最大文件是 `match_lobby_shell.gd` 1,140 行 |
| 所有者文档「当前生效值」30 秒可读 | **成立**（C5 第 7 章；`doc_governance.test.ts` 限 80 行） |
| CD-62 无与事实不符的状态 | **本刀对齐**：E3 已锁、E6 已签；CD-62 / CD-53 文首不再写「锁定仍待人类拍板」「可玩性签署未发生」 |
| CD-61 每条 CD-11 §4 必做项有里程碑所有者 | **成立**（C5 第 8 章） |

C5 原 6 项产出（大厅 L4 拆分、文档去追加、CD-62 修正、CD-61 重排、Web 口径、runbook 更名）以及后续 E9 / E1 / 扫掠预算 / 静态盒阔相均已合入。

## 2. §6 E 项（纠偏完成 ≠ C5 结束）

| # | 本刀口径 |
|---|---|
| E1–E7、E9、E11–E14 | 已满足。证据在纠偏方案 §6 各行 |
| E8 | 前半基本满足。残余：相机距离 / FOV（D4 未问）、UI 安全区（D4 未答）、BASTION 色板（M6/M7 冻结）、**字体已拍板未入包**（许可证 + 子集范围属人类门禁） |
| E10 | **仍待人类**：周合入是否在 D9 纪律内、深审是否有逐行记录。AI 不得自填 |

## 3. 刻意不做（不是 C5 退出项）

- 解除冻结令；开工 M4a / M4b / M6 / M7 / 表现增强
- 字体入包、触控 UI、按 `asset_id` 解析视觉、传送门模型
- 胶囊—胶囊空间划分（席位 ≤ 8，线性可接受；移动体维护格网另开一刀）
- 改协议 / Schema / 官方课 JSON / 冻结常量数字

## 4. 下一动

C5 批次结束。下一动是人类：看 E10、决定是否入字体、以及是否解除 §1 冻结令。解冻前 Agent 不得开工 M 线新功能。

# Godot 4.1+
#
# 依据 Godot 官方 Version Control Systems 文档：.godot/ 是可再生的项目缓存，
# *.translation 是由 CSV 生成的二进制翻译产物，两者都不入库。
# 这覆盖了 CD-51 §2 要求的 .godot/imported 排除。
.godot/
*.translation

# 早期概念图
#
# 44 MB 的 AI 生成大厅概念图。AGENTS.md 已明确 early-concepts/ 是历史归档、
# 不构成任何决策依据，没有必要为它常年占用仓库和 LFS 配额。文件只留在本地。
/early-concepts/

# 导出与构建产物
/build/
/export/
*.pck
game/android/

# Blender 自动备份
*.blend1
*.blend2

# Node.js
node_modules/
dist/
coverage/
*.tsbuildinfo
.npm/
.pnpm-store/
.yarn/

# 密钥与本地配置
#
# CD-51 §3：仓库只允许提交本地或 PR 沙盒可随时销毁的假凭据，
# 长期测试环境的真实密钥必须留在 GitHub Secrets 或服务器外部配置。
.env
.env.*
!.env.example
*.pem
!backend/realtime-gateway/tests/fixtures/*.pem
*.key
*.p12
*.keystore
secrets.json

# Godot AI MCP（ADR-0003 / CD-51 §7）
#
# 只服务本机打开的编辑器，CI 与 Headless 不依赖它。启用插件会在本机
# 生成该目录；禁止入库。已提交的 project.godot 也不得写入该插件或
# _mcp_game_helper autoload。
game/addons/godot_ai/

# Cursor 若把 Godot AI attach 写进项目级 MCP 配置，其中含本机绝对路径，不得入库。
.cursor/mcp.json

# Cursor Run Modes 的自然语言权限指令。官方明确 Auto-review 不是安全边界，
# 入库会给人「配置即门禁」的错觉；本机路径也不该进仓库。
.cursor/permissions.json

# SQLite 本地数据库（CD-51 §1 测试期数据库，由控制面在本地生成）
*.sqlite
*.sqlite3
*.db
# WAL 模式会额外产生 -wal / -shm 边车文件，漏掉它们会在每次跑服务后污染 git status
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm
*.db-journal
*.db-wal
*.db-shm

# 日志与临时文件
*.log
logs/
tmp/
.cache/

# GUT 的 JUnit 结果，CI 用它打印各脚本耗时；本地跑同样会生成，不入库。
game/.gut-results.xml

# C1 §8 ping 原始输出含对端地址，不得入库（手册指定的本地文件名，含 24h 另存）
/rtt-raw.txt
/rtt-raw*.txt

# 操作系统与编辑器
.DS_Store
._*
.Spotlight-V100
.Trashes
Thumbs.db
desktop.ini
.idea/
*.swp
*~

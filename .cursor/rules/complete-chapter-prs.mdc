---
description: Submit complete chapters for human review, not half-finished PRs
alwaysApply: true
---

# 完整章节再提交

所有者：[CD-52 §3.1](Confirmed-docs/50-engineering/52-ai-workflow.md)。决策：`pr_scope = complete_chapter`。

能做成完整一章，就按完整一章开 PR，再请人类审查。短 PR 仍然要短，但必须能独立验收。

一章 = 同一条链路的一次闭合：要锁的契约 + 实现 + 正反例测试 + 所有者文档落点。

- 做：M3 第二十章「对局命令门禁与双人 Headless 冲线」——MatchRealtime 每占用槽位每个 commit_tick 至多入队一条命令（先到先得），后到的同槽命令拒绝；断开丢弃该槽排队，避免旧意图落到新连接；快照帧不能当命令。官方 course_01 两槽经同一回路各 5 步冲线，直播名次从快照派生，同磁带同快照/哈希。不改协议字段，不写结算。作为一章。
- 不做：账号绑定、重连补票、快照插值/预测校正、课程选择 API、离线模式、可达性叠加 gizmos、走路可达/合法路径距离、检查点到达时间、结算写库、本机最佳轨迹幽灵、已缓存 UGC/草稿试玩、个人试玩记录落盘、墙钟发送速率、MMR、Shove/Interact 线上 id、Tick/快照频率锁定、2 秒云端上传、5 分钟定时、BASTION 面板、批量生成、性能分析、时延自动门禁、重力/下落、二进制签名、产品速度、复活硬直；不开 M4 / M5 新章节；不为了两域并行而各交半章。
- 仍不做：把预算数字、走路可达、多人试玩房、Rule VM 真应用和整个 M3 塞进一个 PR。

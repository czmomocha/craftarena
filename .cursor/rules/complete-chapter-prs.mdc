---
description: Submit complete chapters for human review, not half-finished PRs
alwaysApply: true
---

# 完整章节再提交

所有者：[CD-52 §3.1](Confirmed-docs/50-engineering/52-ai-workflow.md)。决策：`pr_scope = complete_chapter`。

能做成完整一章，就按完整一章开 PR，再请人类审查。短 PR 仍然要短，但必须能独立验收。

一章 = 同一条链路的一次闭合：要锁的契约 + 实现 + 正反例测试 + 所有者文档落点。

冻结期内**章粒度下限与审查分级另有约束**，见 [章粒度下限与审查分级](.cursor/rules/chapter-granularity-and-review.mdc)；可开工范围见 [纠偏冻结令](.cursor/rules/course-correction-freeze.mdc)。

真机：PR Test plan 与 [章节真机清单](docs/runbooks/chapter-device-check.md) 本刀必须是编号步骤（怎么开、点什么、期望 / 失败）；禁止只写「真机再看一眼」。无 GUI 的章写「无」加原因。人工检查，不是 CI 门禁。口径见 [CD-52 §3.2](Confirmed-docs/50-engineering/52-ai-workflow.md)。

- 做（本刀 = 纠偏 C4：D7 输入抽象）：大厅与 Preview 的玩法采样改为方向向量 + 上升沿动作事件。键盘是适配器；`shove` / `sprint` / `reset_checkpoint` 进 Input Map（F / Shift / R）。量化成 8 向 `dx/dz` 仍走 `MatchMoveFacing`，模拟量按符号变成整步，不把未锁定的模拟速度写进 MoveIntent。**数值一个不改**：GUT 全绿 + `--bot-run` 三张课步数与动作序列逐项不变（5 / 5 / 12）。
- 不做：触控 UI / 虚拟摇杆；按键重映射界面；手柄承诺；改协议 / Schema / 官方课 JSON；改 `play_move_step`；Preview Move 加 `yaw_bam`；动画状态契约；字体与本地化键；C5 拆壳。
- 仍欠着（不因本章合入而消失）：触控 UI；动画状态契约；按 `asset_id` 解析视觉；传送门模型；字体与本地化键；远端协议层 RTT 回填；C3 网络参数锁定；E6 有美术之后的可玩性签署；扫掠取样代价无上限；`match_lobby_shell.gd` 超 E9 行数上限。
- 仍不做：把模拟摇杆的幅度当成产品速度；把 Input Map 死区写成产品常量。

纠偏归属：C4
是否新增冻结常量引用：否
审查级别：常审（输入层接线；权威命令仍是同一套 8 向整步）
隔离方式：`feat/c4-play-input`

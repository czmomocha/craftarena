---
description: Submit complete chapters for human review, not half-finished PRs
alwaysApply: true
---

# 完整章节再提交

所有者：[CD-52 §3.1](Confirmed-docs/50-engineering/52-ai-workflow.md)。决策：`pr_scope = complete_chapter`。

能做成完整一章，就按完整一章开 PR，再请人类审查。短 PR 仍然要短，但必须能独立验收。

一章 = 同一条链路的一次闭合：要锁的契约 + 实现 + 正反例测试 + 所有者文档落点。

冻结期内**章粒度下限与审查分级另有约束**，见 [章粒度下限与审查分级](.cursor/rules/chapter-granularity-and-review.mdc)；可开工范围见 [纠偏冻结令](.cursor/rules/course-correction-freeze.mdc)。

真机：PR Test plan 与 [章节真机清单](docs/runbooks/chapter-device-check.md) 本刀必须是编号步骤（怎么开、点什么、期望 / 失败）；禁止只写「真机再看一眼」。无 GUI 的章写「无」加原因。人工检查，不是 CI 门禁。口径见 [CD-52 §3.2](Confirmed-docs/50-engineering/52-ai-workflow.md)。

- 做（本刀 = `[freeze-exception]`：恢复 CI 的 Godot 门禁）：五处测试把胶囊尺寸从 Q48.16 字面量 `1` 改回 `TraprushPlayStubs.CAPSULE_RADIUS / CAPSULE_HEIGHT`；CI 的 GUT step 自己掐 10 分钟使超时变红而非灰色 `cancelled`；新增最慢测试脚本耗时打印；在 `main` 上补跑一次完整 GUT；落 [门禁失守记录](docs/audits/2026-08-28-ci-gate-timeout.md)、CD-91 `ci_green_claim`、CD-52 §3.3。
- 不做：给 `SimulationWorld._sweep_step_count` 加取样上限（动权威碰撞落点与状态哈希，另立深审章）；调高 job 的 `timeout-minutes`；改任何玩法、表现或数值；替人类改 `main` 的分支保护设置。
- 仍欠着（不因本章合入而消失）：24 小时 ICMP 回填；远端协议层 RTT 回填；C3 网络参数锁定；E6 有美术之后的可玩性签署；扫掠取样代价无上限（宪法第十七条缺口）；`main` 分支保护为何放行了那五次合并。
- 仍不做：把可玩性自动判定与网络参数锁定塞进同一个 PR。

纠偏归属：freeze-exception（CI 的 Godot 门禁自 PR #176 起连续被 15 分钟超时终止，五刀带着未通过的门禁进了 `main`；这是宪法第十条与第二十四条的阻断性缺陷，按最小变更修）
是否新增冻结常量引用：否（净减少：五处硬编码尺寸改为读已有的 `CAPSULE_RADIUS` / `CAPSULE_HEIGHT`）
审查级别：常审（改的是测试传参与 CI 工作流，产品代码零改动；但门禁本身的处置结论要人类看）
隔离方式：`fix/ci-gut-timeout`

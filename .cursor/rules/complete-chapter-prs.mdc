---
description: Submit complete chapters for human review, not half-finished PRs
alwaysApply: true
---

# 完整章节再提交

所有者：[CD-52 §3.1](Confirmed-docs/50-engineering/52-ai-workflow.md)。决策：`pr_scope = complete_chapter`。

能做成完整一章，就按完整一章开 PR，再请人类审查。短 PR 仍然要短，但必须能独立验收。

一章 = 同一条链路的一次闭合：要锁的契约 + 实现 + 正反例测试 + 所有者文档落点。

冻结期内**章粒度下限与审查分级另有约束**，见 [章粒度下限与审查分级](.cursor/rules/chapter-granularity-and-review.mdc)；可开工范围见 [纠偏冻结令](.cursor/rules/course-correction-freeze.mdc)。

真机：PR Test plan 与 [章节真机清单](docs/runbooks/chapter-device-check.md) 本刀必须是编号步骤（怎么开、点什么、期望 / 失败）；禁止只写「真机再看一眼」。无 GUI 的章写「无」加原因。人工检查，不是 CI 门禁。口径见 [CD-52 §3.2](Confirmed-docs/50-engineering/52-ai-workflow.md)。

- 做（本刀 = 纠偏 C3 第 4 章：机关伤害 / 击退最小实现）：固体半周期占用重叠由服务端扫描，不读客户端命中断言，不读 Authoring `hazard.damage` / `knockback`（官方课仍是 0）。先按盒心→胶囊做 8 向水平击退（中心重叠时向 +Z），步长从 `TraprushPlayStubs` 注入且不得超过 `Fixed.SCALE`；仍重叠则环境失败复位到最近检查点。出界复位走同一条硬直写入。硬直是注入的 1 tick 占位，拒绝 Move/Jump/UseItem/Sprint/Shove，允许 ResetToCheckpoint。`hash_state` 含 `stun_remaining`。`course_01` 出生点 −Z 加两格立足面（entity 87/88）以便走到洋红盒。灰盒磁带路径仍不接伤害。BotRunner `_state_key` 含硬直。不改 `Confirmed-docs/`（CD-21 由 C5 覆盖写）。
- 不做：把硬直写成 D5 的 0.5 / 1.0 / 1.5 产品秒数；锁定 Tick / 快照 / 插值（不得用 C1 近端 ICMP）；第 4 张官方赛道；90 秒语义关；预警动画；机关 HUD / 新颜色；编辑器 Place hazard；无控保护；产品击退距离；新增冻结常量引用点。
- 仍欠着（不因本章合入而消失）：D5 复活硬直三候选待人类选（本章硬直仍是 1 tick 桩）；24 小时 ICMP；协议层 RTT；C3 网络参数锁定；符合 CD-61 §4.1 语义的赛道。
- 仍不做：把 C3 机关伤害与网络参数锁定塞进同一个 PR。

纠偏归属：C3
是否新增冻结常量引用：否
审查级别：深审

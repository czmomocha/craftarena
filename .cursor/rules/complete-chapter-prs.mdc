---
description: Submit complete chapters for human review, not half-finished PRs
alwaysApply: true
---

# 完整章节再提交

所有者：[CD-52 §3.1](Confirmed-docs/50-engineering/52-ai-workflow.md)。决策：`pr_scope = complete_chapter`。

能做成完整一章，就按完整一章开 PR，再请人类审查。短 PR 仍然要短，但必须能独立验收。

一章 = 同一条链路的一次闭合：要锁的契约 + 实现 + 正反例测试 + 所有者文档落点。

冻结期内**章粒度下限与审查分级另有约束**，见 [章粒度下限与审查分级](.cursor/rules/chapter-granularity-and-review.mdc)；可开工范围见 [纠偏冻结令](.cursor/rules/course-correction-freeze.mdc)。

真机：PR Test plan 与 [章节真机清单](docs/runbooks/chapter-device-check.md) 本刀必须是编号步骤（怎么开、点什么、期望 / 失败）；禁止只写「真机再看一眼」。无 GUI 的章写「无」加原因。人工检查，不是 CI 门禁。口径见 [CD-52 §3.2](Confirmed-docs/50-engineering/52-ai-workflow.md)。

- 做（本刀 = 纠偏 C3 第 3 章：爆破球 + 冲刺最小集）：服务端裁决拾取、使用、冷却、命中。Authoring 已有 `inventory.item_state`（`bomb` / `dash`）编进 v1 可空 `pickups` 袋；拾取盒非固体；每种最多持有 1 个；每个 pickup 实体每玩家只拾一次；已持有该 kind 时不标记 taken。UseItem 无弹拒绝，未命中不扣弹，不读客户端命中断言；SprintIntent（`intent_id=6`）无冲刺拒绝，有则沿胶囊 yaw 做 8 向水平位移且不穿固体。冷却与步长从 `TraprushPlayStubs` 注入。官方三张课出生点叠放 entity 100/101。大厅 Left Shift + Sprint 按钮；不要把 Interact/E 接成冲刺。灰盒 `try_use_item` 保持无背包。BotRunner `_state_key` 含 bomb/dash 持有量，不加 sprint 动作。不改 `Confirmed-docs/`（CD-21 意图清单仍由 C5 覆盖写）。
- 不做：机关伤害与击退（阻塞于 D5 复活硬直秒数）；锁定 Tick / 快照 / 插值（不得用 C1 近端 ICMP）；复活硬直产品秒数；第 4 张官方赛道；90 秒语义关；编辑器 Place pickup；道具 HUD / 新颜色；道具栏 UI；刷新重生；产品伤害 / 冲刺距离；新增冻结常量引用点。
- 仍欠着（不因本章合入而消失）：机关伤害/击退；D5 复活硬直三候选待人类选；24 小时 ICMP；协议层 RTT；C3 网络参数锁定；符合 CD-61 §4.1 语义的赛道。
- 仍不做：把 C3 机关伤害与网络参数锁定塞进同一个 PR。

纠偏归属：C3
是否新增冻结常量引用：否
审查级别：深审

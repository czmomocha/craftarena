---
description: Submit complete chapters for human review, not half-finished PRs
alwaysApply: true
---

# 完整章节再提交

所有者：[CD-52 §3.1](Confirmed-docs/50-engineering/52-ai-workflow.md)。决策：`pr_scope = complete_chapter`。

能做成完整一章，就按完整一章开 PR，再请人类审查。短 PR 仍然要短，但必须能独立验收。

一章 = 同一条链路的一次闭合：要锁的契约 + 实现 + 正反例测试 + 所有者文档落点。

冻结期内**章粒度下限与审查分级另有约束**，见 [章粒度下限与审查分级](.cursor/rules/chapter-granularity-and-review.mdc)；可开工范围见 [纠偏冻结令](.cursor/rules/course-correction-freeze.mdc)。

真机：PR Test plan 与 [开发机窗口验收](docs/runbooks/dev-window-check.md) 本刀必须是编号步骤（怎么开、点什么、期望 / 失败）；禁止只写「真机再看一眼」。无 GUI 的章写「无」加原因。人工检查，不是 CI 门禁。口径见 [CD-52 §3.2](Confirmed-docs/50-engineering/52-ai-workflow.md)。

- 做（本刀 = C5 第 7 章：文档治理）：每份所有者文档文首「当前生效值」；CD-41 §5 巨段收敛；CD-21 一期最小道具回写；CD-62 状态纠偏 + 两条新增；runbook 更名为 [开发机窗口验收](docs/runbooks/dev-window-check.md)；CD-61 重排草案（不改现行里程碑正文）；D2/D11 Web 与域名口径同步 CD-11 / CD-51。`npm test` 钉死上述字符串。
- 不做：把 CD-61 草案写成已拍板；改协议 / Schema / 官方课 JSON；拆剩余超 400 行文件；字体入包；触控 UI；用 ICMP 锁定 Tick / 快照 / 插值。
- 仍欠着（不因本章合入而消失）：E9 全仓（控制面 / `course_completion_probe.gd` / `graybox_course.gd` / `simulation_world.gd` / `simulation_bundle.gd` / `authoring_editor_shell.gd` 等仍超 400）；触控 UI；按 `asset_id` 解析视觉；传送门模型；字体入包；远端协议层 RTT 回填；C3 网络参数锁定；E6 有美术之后的可玩性签署；扫掠取样代价无上限；CD-61 重排须人类逐条批准后才回写。
- 仍不做：给嵌入子窗口设 `content_scale_*`；把 locale 热切换做成产品设置页。

纠偏归属：C5
是否新增冻结常量引用：否
审查级别：轻审（所有者文档与 runbook；无玩法 / 协议变更；CD-53 §1.1 不新增强断言）
隔离方式：`feat/c5-doc-governance`

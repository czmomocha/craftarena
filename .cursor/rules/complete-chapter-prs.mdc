---
description: Submit complete chapters for human review; default to main, PR only when asked
alwaysApply: true
---

# 完整章节再提交

所有者：[CD-52 §3.1](Confirmed-docs/50-engineering/52-ai-workflow.md)。决策：`pr_scope = complete_chapter`；落地：`git_workflow = trunk_direct_after_auth`。

能做成完整一章，就按完整一章交给人类审查。短提交仍然要短，但必须能独立验收。

一章 = 同一条链路的一次闭合：要锁的契约 + 实现 + 正反例测试 + 所有者文档落点。

**默认提交 `main`。** 人类验证并说「提交」之后 commit **并** push。只有明确说「不要 push」才不推。只有人类主动说「开 PR」才拉分支开 PR。大改动先做完、让人类验证、得到授权才提交。

**章粒度 5× 与审查分级仍有效**，见 [章粒度下限与审查分级](.cursor/rules/chapter-granularity-and-review.mdc)。纠偏冻结令已解除；开工顺序见 [纠偏冻结令（已解除）](.cursor/rules/course-correction-freeze.mdc) 与 [CD-61](Confirmed-docs/60-plan/61-milestones.md)。

真机：[开发机窗口验收](docs/runbooks/dev-window-check.md) 本刀必须是编号步骤（怎么开、点什么、期望 / 失败）；禁止只写「真机再看一眼」。无 GUI 的章写「无」加原因。人工检查，不是 CI 门禁。口径见 [CD-52 §3.2](Confirmed-docs/50-engineering/52-ai-workflow.md)。

- 做（**下一刀 = 天空盒 A3「多层官方课 `course_06`」**。A1 / A2 已交；**M6 F2 仍未开工**，排在 A3 之后）：人类 2026-09-17 把「天空盒与多层官方课」三章插在 **M6 F1 之后、F2 之前**，计划见 [天空盒与多层官方课](docs/plans/sky-and-multilayer-course-2026-09.md)（A1 → A2 → A3）。**A1 已交**（2026-09-17）：`environment` 组件（Component Schema v1 第 20 个，恰好 `sky_id`）、SimulationBundle 第 23 个可选表现袋（**空时省略**，两条金标一个字未改仍绿）、`sky_catalog.gd`、两张 1024×512 全景贴图入库、CD-11 §8.1 新增「独立运行时贴图」档。**A1 没有画面，第一次看见天空是 A2。** **A2 已交**（2026-09-18）：对局壳 / Preview / Editor 挂 `Camera3D.environment`（**不是** `WorldEnvironment`），IBL 显式关闭，创作者下拉走 `place` / `set_component`。A3 的多层课层距必须 2 格（1 格时上层地板会堵住下层站立位）、底层满铺、顶层两层不放弹射垫（峰值约 4 格会冲出 ±8 出界盒）、竖直转移优先弹射垫 / 电梯而非传送门（探针的传送启发式只允许两次中转，且**跳跃爬不上一整格**）。M6 章节拆分见 [M6 章节计划](docs/plans/m6-bastion-1v1.md)（11 章，D1 → F2）。**D1–D5 交的是全离线玩法底座**；**E1 交了 type 5/6 实时帧、intent id 7–12、布障服务端裁剪**；**E2 交了第一张官方蓝图 `blueprint_01`（对称双线）**；**E3 交了对局进程分派、匹配 HTTP 玩法位与结算队伍结果**；**E4 交了客户端对局壳**（快照跟从 / 建造交互无预测 / HUD tokens / 布障只画本方 / 音频 router 复用已登记 cue）。**F1 交了 S1 主大厅壳**（落地 `s1_lobby.tscn`、双玩法入口、BASTION 卡开放；频道仍是自绘 Window；全局 theme 仍不挂）。**第一次有画面是 E4。** **八项已拍板或按诚实边界落地**（2026-09-15 / 09-16）：蓝图编译产物是**独立新类型**；**M6 最小集 = 九个原型**只锁「用哪九个」；v1 加新帧类型不升大版本；隐藏布障走服务端裁剪；官方蓝图 01 = 对称双线；匹配 HTTP 可选 `gameplay` + 独立 `blueprint`；结算 HTTP 可选 `teams[]` 复用 `mvpSlot`；E4 音效复用已登记 cue、无新 OGG。结论见 [CD-91 D.4](Confirmed-docs/90-reference/91-decision-log.md)。**F1 已让 M5 那份 TRAPRUSH 可玩性签署作废，须在 F2 重签。** **测试期 VPS Web 分发已交**（2026-09-14：`npm run export:web` / `npm run deploy:web`，Nginx 明文 `:80`，Web 默认连本 VPS 8080/8090；人类 2026-09-15 确认部署与测试已自行走过）。**M5 已退出**（2026-09-13 人类签署，结论「基本通过」）。**字体入包已交**。**UI 接线第一批已交**（2026-09-13：四项前置 + S3 接入运行时；2026-09-14：广场列表改走 `HTTPRequest`，开窗 / 切 tab 不堵主线程）。对照 [CD-61](Confirmed-docs/60-plan/61-milestones.md)。S3 三项设计缺口仍待产品输入，不得自行发明：Solo / 建房在设计里无位置（临时动作行）、排序 / 标签 / 搜索无后端（已禁用）、卡片作者与缩略图契约里不存在（已隐藏）。见 [ui-wiring.md §2.2](docs/runbooks/ui-wiring.md)。交章时整节替换 [开发机窗口验收](docs/runbooks/dev-window-check.md) 本刀。
- 不做：从 CD-22 §4.2 / §5.1 / §5.2 三张表示例自行补全（原文就写着「不是锁定清单」）；把 E4 的复用 cue 说成新 BASTION 音效素材已入库（新 OGG 仍须宪法第十八条）；把 M6 最小集之外的塔 / 兵 / 障碍登记进白名单（狙击 / 电弧 / 增幅塔、护盾 / 支援 / 首领兵、护盾柱 / 干扰塔座 / 视野雾区都**不在 M6**）；改 Component Schema v1 或 Bundle v2 来装 BASTION；把 F1 的 S1 接线说成不影响 M5 那份可玩性签署（**换 UI 后须在 F2 重签**）；把 M5 的两处遗留说成已解决——**乱序 / 重复包未严格注入**（待 Linux `tc netem`）、**可玩性签署只对当前自绘大厅 + 占位美术 + 占位数值那一版**；代签或代填任何人类清单；把 S1 从 M6 章拆出去单独开工；开工 2v2 / 蓝图 Edit / 自动对打 / 单人对 AI（**M7**）；开工 Android / iOS 烟测；公开 TLS / PR Web 沙盒（**M7 之后**）；改爆破半径、推击力度、道具重生（D-F4 / D-F5 / D-F6）；改默认相机距离/FOV 产品值；自由旋转镜头；改 `SimulationCore` 定点数合同；动 `SimulationBundle` 原有那 22 个袋的字段**或它们「总是 emit」的行为**（A1 新增的第 23 个 `environment` 袋是唯一「空时省略」的袋，那 22 个的空数组早已进了已发布内容的哈希，改它们就是改 ContentHash）或 TRAPRUSH 帧布局；把独立贴图说成已被 `npm run asset-budget` 覆盖（它只扫 `.glb`）；改 `AuthoringDocument` 的四键或新增第四个 EditCommand `op`；改 `STUB_HALF` 出界盒尺寸；给 BASTION 加天空；开 Web 预设的 VRAM 压缩；在 A3 之前开工 M6 F2；Kenney 24 皮入库（D-F9 未拍）；发明新 Hz；触控 UI；引入 WebRTC；**把外部网络故障注入工具写进仓库或 CI**。
- 仍欠着（不因 D1–F1 / 字体入包 / S3 而消失）：网络故障九项里的乱序 / 重复包；发布候选清单（CD-53 §4.4）从未签；外部真人测试从未做；Android / iOS 烟测（**一期收尾**）；公开 TLS / PR Web 沙盒（**M7 之后**）；M-Art 剩余（UI 接线第三批、皮肤池等 D-F9）；**字体只有 Regular 一个字重**（加字重约 +800 KB，需人类拍板）；**emoji 不在子集内**（F1 已带走 S1 那一处 🔒，别处再用 emoji 在 Web 上仍是豆腐块）；胶囊对胶囊仍线性；**BASTION 的 D 段离线底座、E1 协议帧、E2 官方蓝图、E3 对局进程 / 匹配 / 结算、E4 客户端壳与 F1 S1 壳已交**——`game/src/games/bastion/` 与 `game/src/ugc/bastion_*` 与 `bastion_frame_*.gd` 与 `game/content/official/bastion/blueprint_01.json` 与 `game/src/client/bastion_*.gd` 与 `s1_lobby.gd` / `match_lobby_home.gd` 已落地，互设障碍在会话层、协议裁剪与画面上都成立，匹配 HTTP 已认 `blueprint_01`，结算可写 `teams[]`，S1 可进两个频道，**但 F2 退出验收未做**，**九个原型的数值仍是 `play_stubs.gd` 的占位桩，不是产品表**。音效是复用 TRAPRUSH 已登记 cue，不是 BASTION 专用音色。频道仍是自绘 Window，不是 S2。**天空盒 A1 / A2 已交**：`environment` 组件、第 23 个袋、两张贴图、对局 / Preview / Editor 渲染都在；**左右接缝靠窗口看**（`PanoramaSkyMaterial` 横向环绕，源图不一定是严格等距圆柱投影），不是自动门禁；独立贴图**没有 CI 预算门禁**（`asset-budget` 只扫 `.glb`）；多层官方课 `course_06` 尚不存在，匹配白名单仍只认 01–05。
- 仍不做：把 locale 热切换做成产品设置页。
- 一处口径待人类澄清（不是 AI 能择一的）：CD-61 §1 把「角色选择界面」排在 UI 接线第三批之后，同一文件两处括注写「紧跟主大厅壳」。见 [M6 章节计划 §5.3](docs/plans/m6-bastion-1v1.md)。

里程碑归属：M-Art 插入段（天空盒与多层官方课，排在 M6 F1 之后、F2 之前；M6 本号未退出）
是否绕过 placeholder_spec 散落几何/色板常量：否。天空 id 与贴图路径在 `game/src/shared/sky_catalog.gd` 一处；BASTION 玩法占位数值仍在 `game/src/games/bastion/play_stubs.gd` 一处，色板仍归 `placeholder_spec.gd`
审查级别：A3 常审（多层课 + 匹配白名单那一处按深审）；A1 深审 / A2 常审已交
隔离方式：worktree（A1 / A2 在 `F:/study/craftarena-wt-sky-a1`，分支 `feat/sky-a1`）

---
description: Submit complete chapters for human review, not half-finished PRs
alwaysApply: true
---

# 完整章节再提交

所有者：[CD-52 §3.1](Confirmed-docs/50-engineering/52-ai-workflow.md)。决策：`pr_scope = complete_chapter`。

能做成完整一章，就按完整一章开 PR，再请人类审查。短 PR 仍然要短，但必须能独立验收。

一章 = 同一条链路的一次闭合：要锁的契约 + 实现 + 正反例测试 + 所有者文档落点。

冻结期内**章粒度下限与审查分级另有约束**，见 [章粒度下限与审查分级](.cursor/rules/chapter-granularity-and-review.mdc)；可开工范围见 [纠偏冻结令](.cursor/rules/course-correction-freeze.mdc)。

真机：PR Test plan 与 [章节真机清单](docs/runbooks/chapter-device-check.md) 本刀必须是编号步骤（怎么开、点什么、期望 / 失败）；禁止只写「真机再看一眼」。无 GUI 的章写「无」加原因。人工检查，不是 CI 门禁。口径见 [CD-52 §3.2](Confirmed-docs/50-engineering/52-ai-workflow.md)。

- 做（本刀 = 纠偏 C4 产出 6：本地化键）：锁 `craft_arena.ui.*` 键表（`en` + `zh_CN`）。`UiCopy` 自解析 `content/locale/craft_arena.csv`（不走 Godot CSV 翻译导入）。大厅 / Editor / Preview 的窗题、按钮、占位符与 CD-13 离线横幅走键。缺键返回键名。`zh*` 归一到 `zh_CN`，其余落到 `en`。**不入字体**。**数值一个不改**：GUT 全绿 + `--bot-run` 三张课步数与动作序列逐项不变（5 / 5 / 12）。
- 不做：字体入包 / 子集化；Godot Translation CSV 导入；改协议 / Schema / 官方课 JSON；播 clip；触控 UI；按 `asset_id` 解析视觉；C5 拆壳。
- 仍欠着（不因本章合入而消失）：触控 UI；按 `asset_id` 解析视觉；传送门模型；字体入包；远端协议层 RTT 回填；C3 网络参数锁定；E6 有美术之后的可玩性签署；扫掠取样代价无上限；`match_lobby_shell.gd` 超 E9 行数上限。
- 仍不做：把 `join=` / `pads=` / `FPS` / 动画状态名当可翻译句；把 locale 热切换做成产品设置页。

纠偏归属：C4
是否新增冻结常量引用：否
审查级别：常审偏轻审（表现/UI 文案；权威命令与快照字节不变）
隔离方式：`feat/c4-localization-keys`

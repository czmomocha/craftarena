---
description: Submit complete chapters for human review; default to main, PR only when asked
alwaysApply: true
---

# 完整章节再提交

所有者：[CD-52 §3.1](Confirmed-docs/50-engineering/52-ai-workflow.md)。决策：`pr_scope = complete_chapter`；落地：`git_workflow = trunk_direct_after_auth`。

能做成完整一章，就按完整一章交给人类审查。短提交仍然要短，但必须能独立验收。

一章 = 同一条链路的一次闭合：要锁的契约 + 实现 + 正反例测试 + 所有者文档落点。

**默认提交 `main`。** 人类验证并说「提交」之后 commit **并** push。只有明确说「不要 push」才不推。只有人类主动说「开 PR」才拉分支开 PR。大改动先做完、让人类验证、得到授权才提交。

**章粒度 5× 与审查分级仍有效**，见 [章粒度下限与审查分级](.cursor/rules/chapter-granularity-and-review.mdc)。纠偏冻结令已解除；开工顺序见 [纠偏冻结令（已解除）](.cursor/rules/course-correction-freeze.mdc) 与 [CD-61](Confirmed-docs/60-plan/61-milestones.md)。

真机：[开发机窗口验收](docs/runbooks/dev-window-check.md) 本刀必须是编号步骤（怎么开、点什么、期望 / 失败）；禁止只写「真机再看一眼」。无 GUI 的章写「无」加原因。人工检查，不是 CI 门禁。口径见 [CD-52 §3.2](Confirmed-docs/50-engineering/52-ai-workflow.md)。

- 做（**下一刀 = M6 E3「对局进程 + 匹配 + 结算」**。D1–E2 已交；E3 被章节计划 §5.2 第 4、5 项挡住，AI 不得自选、不得开工）：章节拆分见 [M6 章节计划](docs/plans/m6-bastion-1v1.md)（11 章，D1 → F2）。**D1–D5 交的是全离线玩法底座**；**E1 交了 type 5/6 实时帧、intent id 7–12、布障服务端裁剪**；**E2 交了第一张官方蓝图 `blueprint_01`（对称双线）**。**对局进程 / 客户端壳仍没有，现在开不出一局给人看，第一次有画面仍是 E4**。**五项已拍板**（2026-09-15）：蓝图编译产物是**独立新类型**；**M6 最小集 = 九个原型**只锁「用哪九个」；v1 加新帧类型不升大版本；隐藏布障走服务端裁剪；官方蓝图 01 = 对称双线。结论见 [CD-91 D.4](Confirmed-docs/90-reference/91-decision-log.md)。**其余三项仍待拍板**（匹配 HTTP 玩法判别位、结算 HTTP 队伍结果、BASTION 音效素材），卡在各自那一章之前。UI 接线第二批 S1 在 **F1**，仍在 M6 章内、不提前、不单独开工。**测试期 VPS Web 分发已交**（2026-09-14：`npm run export:web` / `npm run deploy:web`，Nginx 明文 `:80`，Web 默认连本 VPS 8080/8090；人类 2026-09-15 确认部署与测试已自行走过）。**M5 已退出**（2026-09-13 人类签署，结论「基本通过」）。**字体入包已交**。**UI 接线第一批已交**（2026-09-13：四项前置 + S3 接入运行时；2026-09-14：广场列表改走 `HTTPRequest`，开窗 / 切 tab 不堵主线程）。对照 [CD-61](Confirmed-docs/60-plan/61-milestones.md)。S3 三项设计缺口仍待产品输入，不得自行发明：Solo / 建房在设计里无位置（临时动作行）、排序 / 标签 / 搜索无后端（已禁用）、卡片作者与缩略图契约里不存在（已隐藏）。见 [ui-wiring.md §2.2](docs/runbooks/ui-wiring.md)。交章时整节替换 [开发机窗口验收](docs/runbooks/dev-window-check.md) 本刀；M6 的 D 段与 E1 / E2 没有可见窗口行为，写「无」加原因，**第一次有画面是 E4**。
- 不做：在 M6 章节计划 §5.2 剩下**三项**待拍板项上**自选结论**（CD-22 §4.2 / §5.1 / §5.2 三张表原文就写着「不是锁定清单」）；把 M6 最小集之外的塔 / 兵 / 障碍登记进白名单（狙击 / 电弧 / 增幅塔、护盾 / 支援 / 首领兵、护盾柱 / 干扰塔座 / 视野雾区都**不在 M6**）；改 Component Schema v1 或 Bundle v2 来装 BASTION；把 F1 的 S1 接线说成不影响 M5 那份可玩性签署（**换 UI 后须在 F2 重签**）；把 M5 的两处遗留说成已解决——**乱序 / 重复包未严格注入**（待 Linux `tc netem`）、**可玩性签署只对当前自绘大厅 + 占位美术 + 占位数值那一版**；代签或代填任何人类清单；把 S1 从 M6 章拆出去单独开工；开工 2v2 / 蓝图 Edit / 自动对打 / 单人对 AI（**M7**）；开工 Android / iOS 烟测；公开 TLS / PR Web 沙盒（**M7 之后**）；改爆破半径、推击力度、道具重生（D-F4 / D-F5 / D-F6）；改默认相机距离/FOV 产品值；自由旋转镜头；改 `SimulationCore` 定点数合同；动 `SimulationBundle` 的 22 个袋或 TRAPRUSH 帧布局；Kenney 24 皮入库（D-F9 未拍）；发明新 Hz；触控 UI；引入 WebRTC；**把外部网络故障注入工具写进仓库或 CI**。
- 仍欠着（不因 D1–E2 / 字体入包 / S3 而消失）：网络故障九项里的乱序 / 重复包；发布候选清单（CD-53 §4.4）从未签；外部真人测试从未做；Android / iOS 烟测（**一期收尾**）；公开 TLS / PR Web 沙盒（**M7 之后**）；M-Art 剩余（UI 接线第二、三批、皮肤池等 D-F9）；**字体只有 Regular 一个字重**（加字重约 +800 KB，需人类拍板）；**emoji 不在子集内**（S1 那个 🔒 在 Web 上是豆腐块，F1 改开放态只带走这一处）；胶囊对胶囊仍线性；**BASTION 的 D 段离线底座、E1 协议帧与 E2 官方蓝图已交**——`game/src/games/bastion/` 与 `game/src/ugc/bastion_*` 与 `bastion_frame_*.gd` 与 `game/content/official/bastion/blueprint_01.json` 已落地，互设障碍在会话层与协议裁剪上都成立，但匹配玩法位、结算队伍结果、客户端壳仍为零，**九个原型的数值仍是 `play_stubs.gd` 的占位桩，不是产品表**。E1 的「盲」不得说成已经有一场线上对局在藏对方布局（对局进程是 E3）。匹配 HTTP 仍不认 `blueprint_01`。
- 仍不做：把 locale 热切换做成产品设置页。
- 一处口径待人类澄清（不是 AI 能择一的）：CD-61 §1 把「角色选择界面」排在 UI 接线第三批之后，同一文件两处括注写「紧跟主大厅壳」。见 [M6 章节计划 §5.3](docs/plans/m6-bastion-1v1.md)。

里程碑归属：M6（BASTION；S1 在 M6 F1 章内）
是否绕过 placeholder_spec 散落几何/色板常量：否。BASTION 玩法占位数值在 `game/src/games/bastion/play_stubs.gd` 一处，色板仍归 `placeholder_spec.gd`
审查级别：深审（E3 匹配 HTTP + 结算 HTTP + 数据迁移；E2 常审已交）
隔离方式：当前 checkout

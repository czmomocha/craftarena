---
description: Submit complete chapters for human review; default to main, PR only when asked
alwaysApply: true
---

# 完整章节再提交

所有者：[CD-52 §3.1](Confirmed-docs/50-engineering/52-ai-workflow.md)。决策：`pr_scope = complete_chapter`；落地：`git_workflow = trunk_direct_after_auth`。

能做成完整一章，就按完整一章交给人类审查。短提交仍然要短，但必须能独立验收。

一章 = 同一条链路的一次闭合：要锁的契约 + 实现 + 正反例测试 + 所有者文档落点。

**默认提交 `main`。** 人类验证并说「提交」之后 commit **并** push。只有明确说「不要 push」才不推。只有人类主动说「开 PR」才拉分支开 PR。大改动先做完、让人类验证、得到授权才提交。

**章粒度 5× 与审查分级仍有效**，见 [章粒度下限与审查分级](.cursor/rules/chapter-granularity-and-review.mdc)。纠偏冻结令已解除；开工顺序见 [纠偏冻结令（已解除）](.cursor/rules/course-correction-freeze.mdc) 与 [CD-61](Confirmed-docs/60-plan/61-milestones.md)。

真机：[开发机窗口验收](docs/runbooks/dev-window-check.md) 本刀必须是编号步骤（怎么开、点什么、期望 / 失败）；禁止只写「真机再看一眼」。无 GUI 的章写「无」加原因。人工检查，不是 CI 门禁。口径见 [CD-52 §3.2](Confirmed-docs/50-engineering/52-ai-workflow.md)。

- 做（**下一刀 = UI 接线第一批：S3 公共内容广场**）：**M5 已退出**（2026-09-13 人类签署，结论「基本通过」）。**字体入包已交**（2026-09-13：Noto Sans SC 常用 3500 字子集，改名 `CraftArena Sans SC`，许可证与子集范围均由人类拍板）。对照 [CD-61](Confirmed-docs/60-plan/61-milestones.md)。**两个校验脚本已进 CI**（2026-09-13，接入当天修掉两处假绿，见 [ui-wiring.md §0](docs/runbooks/ui-wiring.md)）。UI 接线第一批的前置**已清完可开工的部分**：S3 + 卡片文案已迁 `UiCopy`（按屏前缀 `craft_arena.s3.*`；占位假数据不进表，见 [ui-wiring.md §0.3](docs/runbooks/ui-wiring.md)）。剩 `ui-wiring.md` 重写；S1 / S2 文案随第二 / 三批。交章时整节替换 [开发机窗口验收](docs/runbooks/dev-window-check.md) 本刀。
- 不做：把 M5 的两处遗留说成已解决——**乱序 / 重复包未严格注入**（待 Linux `tc netem`）、**可玩性签署只对当前自绘大厅 + 占位美术 + 占位数值那一版**；代签或代填任何人类清单；跳过字体入包直接接 UI；开工 Android / iOS 烟测；公开 TLS / PR Web 沙盒；抢先 M6 / M7；改爆破半径、推击力度、道具重生（D-F4 / D-F5 / D-F6）；改默认相机距离/FOV 产品值；自由旋转镜头；改 `SimulationCore` 定点数合同；Kenney 24 皮入库（D-F9 未拍）；发明新 Hz；触控 UI；**把外部网络故障注入工具写进仓库或 CI**。
- 仍欠着（不因字体入包而消失）：网络故障九项里的乱序 / 重复包；发布候选清单（CD-53 §4.4）从未签；外部真人测试从未做；Android / iOS 烟测（**一期收尾**）；公开 TLS / PR Web 沙盒；M-Art 剩余（UI 接线三批、皮肤池等 D-F9）；**字体只有 Regular 一个字重**（加字重约 +800 KB，需人类拍板）；**emoji 不在子集内**；胶囊对胶囊仍线性。
- 仍不做：把 locale 热切换做成产品设置页。

里程碑归属：M-Art（字体入包）
是否绕过 placeholder_spec 散落几何/色板常量：否
审查级别：轻审
隔离方式：当前 checkout

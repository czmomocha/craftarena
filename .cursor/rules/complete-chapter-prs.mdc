---
description: Submit complete chapters for human review, not half-finished PRs
alwaysApply: true
---

# 完整章节再提交

所有者：[CD-52 §3.1](Confirmed-docs/50-engineering/52-ai-workflow.md)。决策：`pr_scope = complete_chapter`。

能做成完整一章，就按完整一章开 PR，再请人类审查。短 PR 仍然要短，但必须能独立验收。

一章 = 同一条链路的一次闭合：要锁的契约 + 实现 + 正反例测试 + 所有者文档落点。

冻结期内**章粒度下限与审查分级另有约束**，见 [章粒度下限与审查分级](.cursor/rules/chapter-granularity-and-review.mdc)；可开工范围见 [纠偏冻结令](.cursor/rules/course-correction-freeze.mdc)。

真机：PR Test plan 与 [章节真机清单](docs/runbooks/chapter-device-check.md) 本刀必须是编号步骤（怎么开、点什么、期望 / 失败）；禁止只写「真机再看一眼」。无 GUI 的章写「无」加原因。人工检查，不是 CI 门禁。口径见 [CD-52 §3.2](Confirmed-docs/50-engineering/52-ai-workflow.md)。

- 做（本刀 = 纠偏 C3 第 1 章：权威重力积分、跳跃弧线与落地）：`SimulationWorld` 胶囊 `vy` 进入 `hash_state`；`TraprushGravity.integrate` / `apply_jump`；对局 / Solo / Preview / 灰盒 `commit_tick` 走积分而不是恒定位移；Jump 写冲量；落地/顶棚/`set_vy(0)`；灰盒磁带回放同一条弧哈希一致。`fall_dy` 语义改为每 tick 加速度，数值仍从 `TraprushPlayStubs` 注入。不改 `Confirmed-docs/`（CD-21 §3.3 仍由 C5 覆盖写）。
- 不做：铺官方沿路地板；把 ±8 出界桩从兜底改成真正边界；道具拾取/爆破球/冲刺；机关伤害与击退；锁定 Tick / 快照 / 插值（不得用 C1 近端 ICMP）；复活硬直产品秒数；新增冻结常量引用点。
- 仍欠着（不因本章合入而消失）：官方赛道沿路立足面；`course_03` 在重力下 BotRunner `budget_exhausted`（`course_01` / `02` 仍 `completable`）；24 小时 ICMP；协议层 RTT；C3 网络参数锁定；D5 复活硬直三候选待人类选（不阻塞本章）。
- 仍不做：把 C3 重力与道具/新赛道塞进同一个 PR。

纠偏归属：C3
是否新增冻结常量引用：否
审查级别：常审

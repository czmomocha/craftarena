---
description: Submit complete chapters for human review, not half-finished PRs
alwaysApply: true
---

# 完整章节再提交

所有者：[CD-52 §3.1](Confirmed-docs/50-engineering/52-ai-workflow.md)。决策：`pr_scope = complete_chapter`。

能做成完整一章，就按完整一章开 PR，再请人类审查。短 PR 仍然要短，但必须能独立验收。

一章 = 同一条链路的一次闭合：要锁的契约 + 实现 + 正反例测试 + 所有者文档落点。

冻结期内**章粒度下限与审查分级另有约束**，见 [章粒度下限与审查分级](.cursor/rules/chapter-granularity-and-review.mdc)；可开工范围见 [纠偏冻结令](.cursor/rules/course-correction-freeze.mdc)。

真机：PR Test plan 与 [章节真机清单](docs/runbooks/chapter-device-check.md) 本刀必须是编号步骤（怎么开、点什么、期望 / 失败）；禁止只写「真机再看一眼」。无 GUI 的章写「无」加原因。人工检查，不是 CI 门禁。口径见 [CD-52 §3.2](Confirmed-docs/50-engineering/52-ai-workflow.md)。

- 做（本刀 = C5 第 5 章：拆 Preview 映射）：把 `AuthoringPreviewMap` 拆成 `authoring_preview_map_convert` / `authoring_preview_map_occupancy` / `authoring_preview_map_gizmos` / `authoring_preview_map_overlay` / `authoring_preview_map_player` / 门面。六文件均 < 400 行。公开 API 仍在门面上。GUT 钉拆分性质与 E9 行数；现有 `test_authoring_preview*.gd` 仍覆盖公开 API。`--bot-run` 三张课步数与动作序列逐项不变（5 / 5 / 12）。
- 不做：改裁决顺序或哈希合同；改协议 / Schema / 官方课 JSON；拆 Preview 壳、控制面、探针、灰盒、`simulation_world.gd`；C5 其余产出；字体入包；触控 UI。
- 仍欠着（不因本章合入而消失）：E9 全仓（`authoring_preview_shell.gd` / 控制面 / `course_completion_probe.gd` / `graybox_course.gd` / `simulation_world.gd` / `simulation_bundle.gd` / `authoring_editor_shell.gd` 等仍超 400）；触控 UI；按 `asset_id` 解析视觉；传送门模型；字体入包；远端协议层 RTT 回填；C3 网络参数锁定；E6 有美术之后的可玩性签署；扫掠取样代价无上限。
- 仍不做：给嵌入子窗口设 `content_scale_*`；把 locale 热切换做成产品设置页。

纠偏归属：C5
是否新增冻结常量引用：否
审查级别：轻审（`creator/` Preview 表现映射拆分；占位盒 / gizmos / 脏检查合同不变，由既有 Preview GUT 钉死；CD-53 §1.1 不新增强断言）
隔离方式：`feat/c5-split-authoring-preview-map`

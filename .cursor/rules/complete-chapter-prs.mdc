---
description: Submit complete chapters for human review, not half-finished PRs
alwaysApply: true
---

# 完整章节再提交

所有者：[CD-52 §3.1](Confirmed-docs/50-engineering/52-ai-workflow.md)。决策：`pr_scope = complete_chapter`。

能做成完整一章，就按完整一章开 PR，再请人类审查。短 PR 仍然要短，但必须能独立验收。

一章 = 同一条链路的一次闭合：要锁的契约 + 实现 + 正反例测试 + 所有者文档落点。

**章粒度 5× 与审查分级仍有效**，见 [章粒度下限与审查分级](.cursor/rules/chapter-granularity-and-review.mdc)。纠偏冻结令已解除；开工顺序见 [纠偏冻结令（已解除）](.cursor/rules/course-correction-freeze.mdc) 与 [CD-61](Confirmed-docs/60-plan/61-milestones.md)。

真机：PR Test plan 与 [开发机窗口验收](docs/runbooks/dev-window-check.md) 本刀必须是编号步骤（怎么开、点什么、期望 / 失败）；禁止只写「真机再看一眼」。无 GUI 的章写「无」加原因。人工检查，不是 CI 门禁。口径见 [CD-52 §3.2](Confirmed-docs/50-engineering/52-ai-workflow.md)。

- 做（本刀 = 解除纠偏冻结令）：把 E10 选项 A、字体入包推迟到一期收尾、§1 解除写入所有者文档与 `.cursor/rules/`。E1–E14 全绿。对照 [freeze-lift audit](docs/audits/2026-09-03-freeze-lift.md)。
- 不做：开工 M4a / M4b / M6 / M7；改协议 / Schema / 官方课 JSON；发明新 Hz；字体入包；触控 UI；胶囊—胶囊空间划分。
- 仍欠着（不因本章合入而消失）：M-Export 收尾（TLS / PR Web 沙盒 / 移动端烟测）；M-Art 剩余（按 `asset_id` 解析、传送门模型、clip；字体入包在一期收尾）；胶囊对胶囊仍线性。
- 仍不做：给嵌入子窗口设 `content_scale_*`；把 locale 热切换做成产品设置页。

里程碑归属：本刀是纠偏闸门解除，不是 M 线新功能
是否绕过 placeholder_spec 散落几何/色板常量：否
审查级别：常审（治理文档；不改权威占用 / 协议）
隔离方式：`docs/unfreeze-e10` based on `origin/main`

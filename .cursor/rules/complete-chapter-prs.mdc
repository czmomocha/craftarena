---
description: Submit complete chapters for human review; default to main, PR only when asked
alwaysApply: true
---

# 完整章节再提交

所有者：[CD-52 §3.1](Confirmed-docs/50-engineering/52-ai-workflow.md)。决策：`pr_scope = complete_chapter`；落地：`git_workflow = trunk_direct_after_auth`。

能做成完整一章，就按完整一章交给人类审查。短提交仍然要短，但必须能独立验收。

一章 = 同一条链路的一次闭合：要锁的契约 + 实现 + 正反例测试 + 所有者文档落点。

**默认提交 `main`。** 人类验证并说「提交」之后 commit **并** push。只有明确说「不要 push」才不推。只有人类主动说「开 PR」才拉分支开 PR。大改动先做完、让人类验证、得到授权才提交。

**章粒度 5× 与审查分级仍有效**，见 [章粒度下限与审查分级](.cursor/rules/chapter-granularity-and-review.mdc)。纠偏冻结令已解除；开工顺序见 [纠偏冻结令（已解除）](.cursor/rules/course-correction-freeze.mdc) 与 [CD-61](Confirmed-docs/60-plan/61-milestones.md)。

真机：[开发机窗口验收](docs/runbooks/dev-window-check.md) 本刀必须是编号步骤（怎么开、点什么、期望 / 失败）；禁止只写「真机再看一眼」。无 GUI 的章写「无」加原因。人工检查，不是 CI 门禁。口径见 [CD-52 §3.2](Confirmed-docs/50-engineering/52-ai-workflow.md)。

- 做（**下一刀 = M6 D5「互设障碍阶段（盲设 → 锁定 → 揭示 → 重验 → 退点）」。D1–D4 已交**）：章节拆分见 [M6 章节计划](docs/plans/m6-bastion-1v1.md)（11 章，D1 → F2）。**D1–D4 交的是全离线玩法底座**（蓝图 bundle v1 与编译器、九个原型白名单、`play_stubs`、与玩法无关的确定性边图搜索、封路守卫、五阶段会话与镜像波次与胜负、炮塔与经济）；**协议帧 / 官方蓝图 / 对局进程 / 客户端壳一个都没有，现在开不出一局给人看，第一次有画面仍是 E4**。**两项硬阻断 2026-09-15 已拍板**：蓝图编译产物是**独立新类型**（`SimulationBundle` 与 Component Schema v1 一个字节不动）；**M6 最小集 = 箭塔 / 火炮塔 / 冰霜塔 + 快速兵 / 重装兵 / 集群兵 + 路障 / 减速地块 / 分流门**，只锁「M6 用哪九个」，数值仍是落 `game/src/games/bastion/play_stubs.gd` 一处的占位桩。结论见 [CD-91 D.4](Confirmed-docs/90-reference/91-decision-log.md)。**其余六项仍待拍板**（实时帧扩展方式、匹配 HTTP 玩法判别位、结算 HTTP 队伍结果、隐藏布障协议、官方蓝图主题、BASTION 音效素材），卡在各自那一章之前。UI 接线第二批 S1 在 **F1**，仍在 M6 章内、不提前、不单独开工。**测试期 VPS Web 分发已交**（2026-09-14：`npm run export:web` / `npm run deploy:web`，Nginx 明文 `:80`，Web 默认连本 VPS 8080/8090；人类 2026-09-15 确认部署与测试已自行走过）。**M5 已退出**（2026-09-13 人类签署，结论「基本通过」）。**字体入包已交**。**UI 接线第一批已交**（2026-09-13：四项前置 + S3 接入运行时；2026-09-14：广场列表改走 `HTTPRequest`，开窗 / 切 tab 不堵主线程）。对照 [CD-61](Confirmed-docs/60-plan/61-milestones.md)。S3 三项设计缺口仍待产品输入，不得自行发明：Solo / 建房在设计里无位置（临时动作行）、排序 / 标签 / 搜索无后端（已禁用）、卡片作者与缩略图契约里不存在（已隐藏）。见 [ui-wiring.md §2.2](docs/runbooks/ui-wiring.md)。交章时整节替换 [开发机窗口验收](docs/runbooks/dev-window-check.md) 本刀；M6 的 D 段四章没有可见窗口行为，写「无」加原因，**第一次有画面是 E4**。
- 不做：在 M6 章节计划 §5.2 剩下**六项**待拍板项上**自选结论**（CD-22 §4.2 / §5.1 / §5.2 三张表原文就写着「不是锁定清单」）；把 M6 最小集之外的塔 / 兵 / 障碍登记进白名单（狙击 / 电弧 / 增幅塔、护盾 / 支援 / 首领兵、护盾柱 / 干扰塔座 / 视野雾区都**不在 M6**）；改 Component Schema v1 或 Bundle v2 来装 BASTION；把 F1 的 S1 接线说成不影响 M5 那份可玩性签署（**换 UI 后须在 F2 重签**）；把 M5 的两处遗留说成已解决——**乱序 / 重复包未严格注入**（待 Linux `tc netem`）、**可玩性签署只对当前自绘大厅 + 占位美术 + 占位数值那一版**；代签或代填任何人类清单；把 S1 从 M6 章拆出去单独开工；开工 2v2 / 蓝图 Edit / 自动对打 / 单人对 AI（**M7**）；开工 Android / iOS 烟测；公开 TLS / PR Web 沙盒（**M7 之后**）；改爆破半径、推击力度、道具重生（D-F4 / D-F5 / D-F6）；改默认相机距离/FOV 产品值；自由旋转镜头；改 `SimulationCore` 定点数合同；动 `SimulationBundle` 的 22 个袋或 TRAPRUSH 帧布局；Kenney 24 皮入库（D-F9 未拍）；发明新 Hz；触控 UI；引入 WebRTC；**把外部网络故障注入工具写进仓库或 CI**。
- 仍欠着（不因 D1–D4 / 字体入包 / S3 而消失）：网络故障九项里的乱序 / 重复包；发布候选清单（CD-53 §4.4）从未签；外部真人测试从未做；Android / iOS 烟测（**一期收尾**）；公开 TLS / PR Web 沙盒（**M7 之后**）；M-Art 剩余（UI 接线第二、三批、皮肤池等 D-F9）；**字体只有 Regular 一个字重**（加字重约 +800 KB，需人类拍板）；**emoji 不在子集内**（S1 那个 🔒 在 Web 上是豆腐块，F1 改开放态只带走这一处）；胶囊对胶囊仍线性；**BASTION 只有 D 段的离线底座**——`game/src/games/bastion/` 与 `game/src/ugc/bastion_*` 已落地，但线上 intent id、快照帧、匹配玩法位、结算队伍结果、官方蓝图、客户端壳全部为零，**九个原型的数值仍是 `play_stubs.gd` 的占位桩，不是产品表**。
- 仍不做：把 locale 热切换做成产品设置页。
- 一处口径待人类澄清（不是 AI 能择一的）：CD-61 §1 把「角色选择界面」排在 UI 接线第三批之后，同一文件两处括注写「紧跟主大厅壳」。见 [M6 章节计划 §5.3](docs/plans/m6-bastion-1v1.md)。

里程碑归属：M6（BASTION；S1 在 M6 F1 章内）
是否绕过 placeholder_spec 散落几何/色板常量：否。BASTION 玩法占位数值在 `game/src/games/bastion/play_stubs.gd` 一处，色板仍归 `placeholder_spec.gd`
审查级别：常审
隔离方式：当前 checkout

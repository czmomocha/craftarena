---
description: Submit complete chapters for human review, not half-finished PRs
alwaysApply: true
---

# 完整章节再提交

所有者：[CD-52 §3.1](Confirmed-docs/50-engineering/52-ai-workflow.md)。决策：`pr_scope = complete_chapter`。

能做成完整一章，就按完整一章开 PR，再请人类审查。短 PR 仍然要短，但必须能独立验收。

一章 = 同一条链路的一次闭合：要锁的契约 + 实现 + 正反例测试 + 所有者文档落点。

冻结期内**章粒度下限与审查分级另有约束**，见 [章粒度下限与审查分级](.cursor/rules/chapter-granularity-and-review.mdc)；可开工范围见 [纠偏冻结令](.cursor/rules/course-correction-freeze.mdc)。

真机：PR Test plan 与 [章节真机清单](docs/runbooks/chapter-device-check.md) 本刀必须是编号步骤（怎么开、点什么、期望 / 失败）；禁止只写「真机再看一眼」。无 GUI 的章写「无」加原因。人工检查，不是 CI 门禁。口径见 [CD-52 §3.2](Confirmed-docs/50-engineering/52-ai-workflow.md)。

- 做（本刀 = 纠偏 C1 第二章：远端部署产物与客户端指向远端）：`game/src/client/server_endpoint.gd` 从命令行 / 环境变量解析并校验控制面与网关基址，大厅多一行 `Server host` + **Apply server**（运行中改，对局中拒绝，被拒地址只出 `server_error=` 不改生效值）；`infra/compose/` 落 `Dockerfile` + `docker-compose.yml` + `.env.example`（Godot 引擎构建时下载并强制校验 SHA512，MatchHost 不发布端口），`infra/tencent-cloud/README.md` 只写云上差异；`docs/runbooks/server-deploy.md` 是给人执行的部署与基线采集手册，**§12 那张「哪些零延迟参数不成立」的表才是本章主产出**；GUT 覆盖端点解析与大厅正反例，npm 契约测试守住「不发布 MatchHost 端口、不写死真实 IP」。本章不改 `Confirmed-docs/`（留到 C5 一次性同步），不自己部署、不自己联机——那两步是人类门禁。
- 不做：一切被 [纠偏冻结令](.cursor/rules/course-correction-freeze.mdc) §1 列为「不得开工」的内容——M3 剩余表现层增强、M4、M6 / M7、第 4 张及以后官方赛道、新增冻结常量引用点；本章另外不做域名、Let's Encrypt、`wss://` 与反向代理（D11 已把它们移出 C1），不做 Web 联机，不按实测改 Tick / 快照 / 插值参数（那是 C3，本章只**采集**并列出哪些不成立）；以及原先各章已列的不做项（账号绑定、远端外推碰撞、平滑对账、走路可达、MMR、Rule VM、BASTION、生产 CA 信任链、把 `godot_ai` 插件目录或 `_mcp_game_helper` 入库等）。
- 仍不做：把 C0 与 C1 / C2 塞进同一个 PR；不为了两域并行而各交半章。

---
description: Submit complete chapters for human review, not half-finished PRs
alwaysApply: true
---

# 完整章节再提交

所有者：[CD-52 §3.1](Confirmed-docs/50-engineering/52-ai-workflow.md)。决策：`pr_scope = complete_chapter`。

能做成完整一章，就按完整一章开 PR，再请人类审查。短 PR 仍然要短，但必须能独立验收。

一章 = 同一条链路的一次闭合：要锁的契约 + 实现 + 正反例测试 + 所有者文档落点。

真机：PR Test plan 与 [章节真机清单](docs/runbooks/chapter-device-check.md) 本刀必须是编号步骤（怎么开、点什么、期望 / 失败）；禁止只写「真机再看一眼」。无 GUI 的章写「无」加原因。人工检查，不是 CI 门禁。口径见 [CD-52 §3.2](Confirmed-docs/50-engineering/52-ai-workflow.md)。

- 做：M3 第二十五章「对局快照插值」——`MatchSnapshotFollow` 在 tick 前进时保留上一份玩家位姿；`MatchSnapshotInterp.try_sample` 用 Q48.16 `t`（0～`Fixed.SCALE`）对 x/y/z 线性插值、对 `yaw_bam` 按最短 BAM 弧插值；`accepted_count` / `finish_tick` 取最新权威；无上一份或任一轴位移 ≥ `Fixed.SCALE`（1 表现米格子，不是对账阈值）则贴最新；溢出贴最新；箱子 / 赛道 / 连线 / 顺序 gizmos 不插值；大厅 `play_interp_step` 是表现桩，可见窗口才推进 `t`，隐藏不推进；名次文字用最新权威、标签位姿跟插值；同 tick 替换不建 previous；不预测、不校正、不锁插值窗口 / 快照频率。作为一章。
- 不做：账号绑定、本地移动预测、远端外推碰撞、平滑对账、可达性叠加 gizmos、走路可达/合法路径距离、检查点到达时间、本机最佳轨迹幽灵、已缓存 UGC/草稿试玩、个人试玩记录落盘、墙钟发送速率、MMR、限时未全员冲线结算、Shove/Interact 线上 id、Tick/快照频率锁定、2 秒云端上传、5 分钟定时、BASTION 面板、批量生成、性能分析、时延自动门禁、重力/下落、二进制签名、产品速度、复活硬直；不开 M4 / M5 新章节；不为了两域并行而各交半章。
- 仍不做：把预算数字、走路可达、多人试玩房、Rule VM 真应用和整个 M3 塞进一个 PR。

---
description: Submit complete chapters for human review, not half-finished PRs
alwaysApply: true
---

# 完整章节再提交

所有者：[CD-52 §3.1](Confirmed-docs/50-engineering/52-ai-workflow.md)。决策：`pr_scope = complete_chapter`。

能做成完整一章，就按完整一章开 PR，再请人类审查。短 PR 仍然要短，但必须能独立验收。

一章 = 同一条链路的一次闭合：要锁的契约 + 实现 + 正反例测试 + 所有者文档落点。

冻结期内**章粒度下限与审查分级另有约束**，见 [章粒度下限与审查分级](.cursor/rules/chapter-granularity-and-review.mdc)；可开工范围见 [纠偏冻结令](.cursor/rules/course-correction-freeze.mdc)。

真机：PR Test plan 与 [章节真机清单](docs/runbooks/chapter-device-check.md) 本刀必须是编号步骤（怎么开、点什么、期望 / 失败）；禁止只写「真机再看一眼」。无 GUI 的章写「无」加原因。人工检查，不是 CI 门禁。口径见 [CD-52 §3.2](Confirmed-docs/50-engineering/52-ai-workflow.md)。

- 做（本刀 = 纠偏 C4：按袋类型把 5 个已烘焙占用视觉接到大厅与 Preview）：检查点垫 + 检查点门、终点门、箱子、滚柱入库并接线。垫走 `fit_tile_on_cell`（顶面对齐），门 / 终点 / 箱 / 滚柱走 `fit_prop_on_cell`（脚底对齐占位盒底）。占位盒 `layers = 0` 退出渲染，进度色与 D4 危险色走 `material_overlay`。箱与滚柱**保留**橙色 / 洋红 overlay（人类 2026-09-02 拍板）。传送门仍是色块。`--package-check` 对 5 个资产做实例化 + 贴合判定。**数值一个不改**：GUT 全绿 + `--bot-run` 三张课步数与动作序列逐项不变（5 / 5 / 12）。
- 不做：按 `asset_id` 解析；改 Schema / 官方课 JSON；`spawn_grid` / `block_slope` / `bridge`；传送门 GLB；靠拉伸修「门比人矮 / 滚柱超格」；字体与本地化键；D7 输入抽象；C5 拆壳；角色胶囊进资产表。
- 仍欠着（不因本章合入而消失）：按 `asset_id` 解析视觉；传送门模型；远端协议层 RTT 回填；C3 网络参数锁定；E6 有美术之后的可玩性签署；扫掠取样代价无上限（宪法第十七条缺口）；`match_lobby_shell.gd` 超 E9 行数上限。
- 仍不做：把 `assets` 袋当成视觉清单。视觉永远不进 bundle，客户端按袋类型（将来按 `asset_id`）取 `latest`。

纠偏归属：C4
是否新增冻结常量引用：否
审查级别：轻审（表现层接线；权威占用半长与官方课 JSON 一个字节没动）
隔离方式：`feat/c4-occupancy-visuals`

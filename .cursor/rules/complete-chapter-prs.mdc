---
description: Submit complete chapters for human review, not half-finished PRs
alwaysApply: true
---

# 完整章节再提交

所有者：[CD-52 §3.1](Confirmed-docs/50-engineering/52-ai-workflow.md)。决策：`pr_scope = complete_chapter`。

能做成完整一章，就按完整一章开 PR，再请人类审查。短 PR 仍然要短，但必须能独立验收。

一章 = 同一条链路的一次闭合：要锁的契约 + 实现 + 正反例测试 + 所有者文档落点。

冻结期内**章粒度下限与审查分级另有约束**，见 [章粒度下限与审查分级](.cursor/rules/chapter-granularity-and-review.mdc)；可开工范围见 [纠偏冻结令](.cursor/rules/course-correction-freeze.mdc)。

真机：PR Test plan 与 [章节真机清单](docs/runbooks/chapter-device-check.md) 本刀必须是编号步骤（怎么开、点什么、期望 / 失败）；禁止只写「真机再看一眼」。无 GUI 的章写「无」加原因。人工检查，不是 CI 门禁。口径见 [CD-52 §3.2](Confirmed-docs/50-engineering/52-ai-workflow.md)。

- 做（本刀 = 纠偏 C2 第一章：赛道走路可达判定 + 占位桩收敛）：`game/src/games/traprush/course_completion_probe.gd` 在真实 `TraprushMatchSession` 上做加权 A\* 搜索（重放式恢复，预算按仿真 tick 计），`completable` 附可重放动作序列并由 `try_replay` 复验，`not_completable` 必须区分 `search_exhausted` 与 `budget_exhausted`、给出最远进度与目标坐标；`game/src/games/traprush/bot_run_cli.gd` + `main.gd` 的 `--bot-run` 分支照 `--package-check` 模式短路，每课一行 JSON、汇总一行、任一张走不通 exit 1，只收官方课 id 不收 `res://` 路径；`game/src/games/traprush/play_stubs.gd` 收敛动作与胶囊占位桩（MATCH 组给对局 / Solo / bot，PREVIEW 组保留手动 Advance 一次落一整格的差异），改 `match_server.gd`、`match_lobby_shell.gd`、`match_offline_session.gd`、`match_local_predict.gd`、`authoring_preview_shell.gd` 五处消费方；GUT 覆盖正反例（官方课可完成且路线可重放；无终点 / 够不到的检查点 / 预算先耗尽三种走不通）。本章不改 `Confirmed-docs/`（留到 C5 一次性同步）。
- 不做：一切被 [纠偏冻结令](.cursor/rules/course-correction-freeze.mdc) §1 列为「不得开工」的内容——M3 剩余表现层增强、M4、M6 / M7、第 4 张及以后官方赛道、新增冻结常量引用点（本章相反：把既有引用点收敛掉）；本章另外不做 `tools/bot-runner/` 的 Node 包装（`--bot-run` 已自带结论与退出码，再加一层只是转发），不把 `--bot-run` 接进 CI（45 秒的东西要不要进门禁是单独的决定），不按实测改任何玩法数值——收敛只搬家不改值，改值是 C3；以及原先各章已列的不做项（账号绑定、远端外推碰撞、平滑对账、MMR、Rule VM、BASTION、生产 CA 信任链、把 `godot_ai` 插件目录或 `_mcp_game_helper` 入库等）。
- 仍欠着（不因本章合入而消失）：C1 第二章的 B 段——`infra/compose/` 至今未在任何机器上真构建过，双机对局与网络 / 资源基线未采，`server-deploy.md` §12 那张表仍是空的。那是人类门禁，本章与它互不依赖。
- 仍不做：把 C0 与 C1 / C2 塞进同一个 PR；不为了两域并行而各交半章。

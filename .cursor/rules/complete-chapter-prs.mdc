---
description: Submit complete chapters for human review, not half-finished PRs
alwaysApply: true
---

# 完整章节再提交

所有者：[CD-52 §3.1](Confirmed-docs/50-engineering/52-ai-workflow.md)。决策：`pr_scope = complete_chapter`。

能做成完整一章，就按完整一章开 PR，再请人类审查。短 PR 仍然要短，但必须能独立验收。

一章 = 同一条链路的一次闭合：要锁的契约 + 实现 + 正反例测试 + 所有者文档落点。

冻结期内**章粒度下限与审查分级另有约束**，见 [章粒度下限与审查分级](.cursor/rules/chapter-granularity-and-review.mdc)；可开工范围见 [纠偏冻结令](.cursor/rules/course-correction-freeze.mdc)。

真机：PR Test plan 与 [章节真机清单](docs/runbooks/chapter-device-check.md) 本刀必须是编号步骤（怎么开、点什么、期望 / 失败）；禁止只写「真机再看一眼」。无 GUI 的章写「无」加原因。人工检查，不是 CI 门禁。口径见 [CD-52 §3.2](Confirmed-docs/50-engineering/52-ai-workflow.md)。

- 做（本刀 = 纠偏 C4 第 1 章：占位几何与色板收敛到单一配置源）：新建 `game/src/shared/placeholder_spec.gd`，把 1 米占位盒、相机/灯光偏移、二十来个 albedo、`play_move_step` / `play_interp_step`、`CAPSULE_RADIUS/HEIGHT`、`SPAWN_STRIDE` 的**字面量**收敛到一处；8 套对局映射、Preview 映射、两条壳、`match_server`、`play_stubs` 全部改为读它。公开常量名保留为别名（测试按名字引用），私有常量删除后直接读 spec。**数值一个不改**——既有 GUT 用例保持全绿就是本章的验收信号。新增源码扫描用例，防止下一章又把 `Color(...)` 写回某个 map。
- 不做：接线 D4 的相机斜 45° 与 UI 基准 1920×1080（会改可见行为，留后续章）；权威碰撞与视觉网格分离（要 `GameplayAsset` 契约）；任何 `.glb`；给占位表现新增强色值断言。
- 仍欠着（不因本章合入而消失）：24 小时 ICMP 回填；远端协议层 RTT 回填；C3 网络参数锁定；E6 有美术之后的可玩性签署；扫掠取样代价无上限（宪法第十七条缺口）；`main` 分支保护为何放行了 PR #176–#180 那五次合并。
- 仍不做：把 Schema 变更（`GameplayAsset` / SimulationBundle v2）塞进本 PR——那要先出 ADR 走人类门禁。

纠偏归属：C4
是否新增冻结常量引用：否（净减少：字面量从 13 个文件收敛到 1 个，冻结清单里的每一项此后只有一处定义）
审查级别：深审（新文件落在 `shared/`，且是 D4 数值此后唯一的落点）
隔离方式：`feat/c4-placeholder-spec`

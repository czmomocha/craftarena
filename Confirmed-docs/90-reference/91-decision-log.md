# CD-91 决策记录

> 文档 ID：CD-91
> 单一事实源：每项决策的选择来源、覆盖关系与延期标记
> 加载建议：只在需要追溯"某个设计为什么是这样"或确认某项是否真的拍板过时读取。日常实现不需要
> 上位约束：[CD-00 宪法](../00-constitution/CONSTITUTION.md) 第二十六条
> 相关：[CD-63 开发期决策清单](../60-plan/63-open-decisions.md)、[CD-62 风险登记册](../60-plan/62-risk-register.md)
> 派生自：初稿 v0.2 附录 D

## 当前生效值

本文件不拥有口径。它只记录选择来源与覆盖关系；现行规则在各所有者文档的「当前生效值」节。冲突时以时间较新的明确决策为准，再回写所有者。

## 使用与追加规则

本文件记录选择来源，防止 AI 把推荐项误当决定。

- 若正文与本文件冲突，以**时间较新的明确决策**为准；
- `延期` 表示**不得推断默认值**；
- 出现新拍板或推翻旧决策时，必须在对应小节追加一行，并注明被覆盖的旧值；
- 只记录"选了什么"和"覆盖了什么"，具体规则写进所有者文档。

---

## D.1 产品、平台与账号

- `project_name = craft_arena`（2026-08-20）：正式定名 **Craft Arena／工坊竞技场**，GitHub 仓库名 `craftarena`。此前无正式名称，文档中仅以工作目录名 `AIGame` 临时指代，该临时称呼作废。命名规范见 [CD-11 §1](../10-product/11-scope-and-platforms.md)。
- `primary_validation_path = hybrid_web_gate`：桌面原生开发，测试者优先 Web，共享功能持续过 Web 门禁。
- `web_editor_scope = light_editor`：Web 游玩 + 轻量编辑；完整规则图留在桌面。该句被 `web_light_edit = browser_closed_loop`（2026-09-09）补充：玩家包入口已在浏览器导出包上闭环——`/play/?edit=1`、摆放 / 移动 / Preview、返回大厅、草稿 `force_fs_sync`。规则模板仍属 M4a。口径见 [CD-32](../30-ugc/32-editor-and-preview.md)。
- `authoring_identity`：允许匿名编辑，云端自动保存并保留本地缓存。
- `guest_draft_lifecycle = guest_30_claim`：Guest ID + 恢复密钥，云端 30 天，登录后认领。
- `product_packaging = one_platform_modular`：同品牌、账号、大厅，双玩法按需资源模块。
- `module_delivery_boundary = assets_content_only`：可信代码随基础包，模块只下发资源与内容。
- `minimum_device_tier = midrange_2020`；`fps_targets = pc60_mobile30`。
- `browser_support = chromium_desktop`：桌面 Chrome/Edge 最新两个大版本。
- `web_offline = native_only`：安装版离线；Web 在线但保留本地草稿。该句被 `web_offline = test_period_allow_solo`（2026-09-07）覆盖：**测试期允许 Web Solo**（与桌面同一套本地内嵌权威，仍不回写）。外人联机用链接 + 端口打自备 VPS（明文见 D.1 / [CD-11 §9](../10-product/11-scope-and-platforms.md)）。接线属 Web 游玩分发第一刀。公开运营前是否收回「Web 必须联网」，到一期收尾重审。口径见 [CD-13 §3](../10-product/13-account-and-session.md#3-离线单人模式)。该句中「接线属 Web 游玩分发第一刀」被 `web_play_distribute = browser_solo_host_port`（2026-09-08）覆盖：第一刀已落地——Web Solo 不再 `web_locked`；大厅 `主机` 或 `主机:控制面端口`；查询串 `server` / `control-plane` / `gateway`；控制面 CORS `*` 与可选 `/play/`。公开 TLS 与 PR Web 沙盒仍未做。口径见 [CD-13 §3](../10-product/13-account-and-session.md#3-离线单人模式) 与 [CD-44](../40-technical/44-deployment.md)。
- `mobile_editor_scope = play_only`；`pc_input_support = keyboard_only`。
- `localization_scope = zh_en`；`accessibility_baseline = defer_accessibility`。该句被 `ui_copy = csv_keys_zh_en_no_font`（2026-09-02，纠偏 C4 产出 6）补充：UI 句子走 `craft_arena.ui.*` 键，表在 `game/content/locale/craft_arena.csv`，`UiCopy` 自己解析（不走 Godot CSV 翻译导入）。`zh*` 归一到 `zh_CN`，其余落到 `en`。CD-13 离线横幅的 `zh_CN` 原文不变。开发期状态行 / 节点名 / 课程 id / 错误码 / 动画状态名不翻译。**字体仍未入包**（D4 的思源黑体 / Noto Sans SC 子集范围待人类拍，宪法第十八条）。口径见 [CD-11 §8](../10-product/11-scope-and-platforms.md#8-产品表现与设备基线) 与 [CD-13 §3](../10-product/13-account-and-session.md#3-离线单人模式)。该句中「`UiCopy` 自己解析」被 `ui_copy = fileaccess_get_csv_line`（2026-09-02，纠偏 C5 第 14 章 / E1）收窄：真导出证明手写按字符拆行在模板 `.gdc` 里拆出 0 行，文件却在包里；生产路径只走 `FileAccess.get_csv_line`。不覆盖键表、locale 归一与「不入字体」。证据见 [desktop-export-check.md §5 第 6 条](../../docs/runbooks/desktop-export-check.md)。该句中「不入字体」与「子集范围待人类拍」被 `font_packaging = defer_to_phase1_end`（2026-09-03）覆盖：入包推迟到一期收尾；选型仍是思源黑体 / Noto Sans SC；当前接受任意中文缺字。不挡解冻、不挡 M-Export / M4a。该句中「推迟到一期收尾」与「子集范围待人类拍」被 `font_packaging = subset_common_3500_shipped`（2026-09-13，人类拍板子集范围后执行）覆盖：Noto Sans SC（SIL OFL 1.1）常用 3500 字子集已入包并接线，按 OFL 的保留字体名约束改名 `CraftArena Sans SC`；覆盖策略是三层并集——常用 3500 字 ∪ 项目补集 ∪ 可打印 ASCII。许可证那一项 2026-09-12 已由人类确认，宪法第十八条的这两项就此关闭。**未覆盖**：只有 Regular 一个字重、emoji 不在子集内、公开未过滤用户名在子集外的汉字仍可能缺字。口径与已知边界见 [CD-11 §8.2 第 3 条](../10-product/11-scope-and-platforms.md#82-表现规格d42026-08-26-拍板当前生效值)，重跑步骤在 `tools/font-subset/README.md`，来源与条款见 [ATTRIBUTION.md §1.1](../../game/content/assets/ATTRIBUTION.md)。
- `art_direction = stylized_lowpoly`；`target_age = teen_plus`。该行被 `asset_budget_v1 = tri_3k_6k_tex_512_size_2mb`（2026-08-30）补充：单个平台 3D 资产上限为静态无绑定物件 **3,000** 三角面、带动画绑定角色 **6,000** 三角面、贴图边长 **512**（全用途）、单个文件 **2 MB**。覆盖 [CD-63 §1.7](../60-plan/63-open-decisions.md) 中"资产面数 / 贴图 / 体积"这一段空白；§1.7 其余工程性能预算（场景总量、Draw call、材质数、骨骼上限）与香港真实网络门禁**不被覆盖，仍延期**。同时覆盖 [烘焙试验记录](../../docs/plans/asset-bake-trial-2026-08.md) §6 的"推荐预设"作为建议的地位（面数由单档 3000 改为按是否带绑定分两档；`max_total_bytes` 由 1 MB 参考线改为 2 MB 预算），但那份文档的观察值仍只是一次本机试验，不是门禁。该文 §7 第 2 项（烘焙工具与依赖）与第 3 项（烘焙流水线是否立为 C4 一章）**未拍板**。数字只在 [CD-11 §8.1](../10-product/11-scope-and-platforms.md) 维护。
- `presentation_spec_v1 = cell_1m_cam45_ui_1920x1080`（人类 2026-08-26 拍板 D4，2026-08-31 落文档并接线）：1 格 = 1 表现米；角色高 / 半径各 0.125 格；相机水平方位角与俯角均 **45°**；TRAPRUSH 危险色 = 洋红；UI 分辨率基准 **1920×1080**（`canvas_items` 等比缩放）；字体 = 思源黑体 / Noto Sans SC 子集化入包；描边不做（D8）。覆盖此前"这些值是 AI 自选占位常量、只在代码注释里声明'不是产品数值'"的状态，以及相机的实际俯角 43.3°。**未覆盖**的三项仍空：相机距离与 FOV（D4 未问，维持接线前值）、UI 安全区（D4 未答，等 D7 触控立项）、BASTION 色板（M6/M7 未开工）。该句中「尚未入包」被 `font_packaging = defer_to_phase1_end`（2026-09-03）覆盖：入包推迟到一期收尾，不挡 E8。该句中「相机距离……维持接线前值」被 `f_line_readable_course = spread_hud_zoom`（2026-09-07）补充：默认距离仍是 √136，新增滚轮 8–20 m 调距与中键平移；角度仍 45°。数字只在 [CD-11 §8.2](../10-product/11-scope-and-platforms.md) 维护，实现侧单一配置源是 `game/src/shared/placeholder_spec.gd`。
- `platform_asset_generation = ai_generated_allowed`（2026-08-30）：TRELLIS、混元 3D 等 AI 生成工具的产物只要过 `asset_budget_v1` 即可作为**正式**平台资产入库，不需要人手重做。这是把 D.6 `ai_asset_policy = auto_publish_if_technical` 从"公开策略"落到"平台自有美术生产"上，不覆盖 [CD-11 §5](../10-product/11-scope-and-platforms.md) 的"玩家不得上传自定义 3D 模型、音频和任意贴图"，也不覆盖 [CD-63 §4](../60-plan/63-open-decisions.md) 阻断清单第 6 项（AI 资产来源、投诉和下架流程）。口径见 [CD-11 §8](../10-product/11-scope-and-platforms.md)。该句被 `asset_budget_gate = core_only_gate_npx_bake`（2026-08-30）补充为可执行形式：`asset_budget_v1` 由 `tools/asset-budget/` 机械判定并进 CI（`npm run asset-budget`），面数档位**由 glTF 是否含 `skin` 决定而不看文件名**，认不出的贴图格式 / 非三角 primitive / LFS 指针**判失败而非放过**，0 个 `.glb` 时明确输出"什么都没查"。依赖拆两层：门禁只用 `@gltf-transform/core` 4.4.2（2 个包、`npm audit` 0 漏洞、无 native），烘焙用 `@gltf-transform/cli` 4.4.2 **经 `npx` 显式按版本拉起、不进 `package.json`**——它拖 205 个包且 `sharp@~0.34.5` 挂 4 个 libvips CVE（`GHSA-f88m-g3jw-g9cj`），上游未跟进，`npm audit fix --force` 会降到 2.5.1。代价：烘焙工具不在 lock 里、传递依赖不保证逐字节可复现（入库物是产物 `.glb`，由门禁把关）。**该句后半段被 `asset_bake_tool = gltf_transform_cli_4_5_0`（人类 2026-09-01 拍板，[资产烘焙 runbook](../../docs/runbooks/asset-bake.md) §2）覆盖**：烘焙版本由 `@gltf-transform/cli` 4.4.2 升到 **4.5.0**，本行上面"拖 205 个包且 `sharp@~0.34.5` 挂 4 个 libvips CVE"的论述**自此失效**（4.5.0 是 173 个包、`sharp@0.35.4` dedupe、`npm audit` 0 漏洞）。升级理由不是新特性而是**修一个真实失败**：4.4.2 的 `@gltf-transform/functions@^4.4.2` 会解析到 4.5.0 并带进第二份 `sharp`（0.35.4），与 cli 自己的 `sharp@~0.34.5` 在依赖树里共存，两份 libvips 枚举对不上，Windows 上 `resize` 直接失败（`error: colourspace: parameter space not set`，exit 1，无产物）；该失败可对本仓库已入库的资产复现，与待烘焙文件无关。`ndarray-pixels@5.2.0` 发布于 2026-08-30 之后，所以"写死直接依赖版本"**钉不住 `^` 区间下的传递依赖漂移**——这一点是本行"不保证逐字节可复现"那个已知代价的实例，不是新风险。实测（Windows 11 / Node 24.19.0 / npm 11.17.0）：7 个混元 3D 产物 150.60 MB → 4.19 MB，`npm run asset-budget` 7/7 `ok`，几何逐字不变。**未覆盖**：烘焙工具仍不进 `package.json`、不进 lock、不进 CI（该立场不变，理由从"避开 CVE / 包太多"改为"不该成为每次 `npm install` 与 CI 都要付的成本"）；macOS 上 4.5.0 **未实测**，Windows 之外不算已验证。数字与命令只在 [CD-51 §1](../50-engineering/51-dev-environment.md) 与 runbook 维护。覆盖 [烘焙试验 §7](../../docs/plans/asset-bake-trial-2026-08.md) 第 2 项（人类 2026-08-30 批准引入）与「本文不提供可复现的工具链」这一句：跨平台命令已存在，硬编码 `sips` 的一次性脚本退役。第 3 项（自动化流水线立为 C4 一章）人类**明确否决**，按用途分档参数与批量烘焙不在 C4。同日明确本章**只做单资产准入**：场景总量 / Draw call / 材质数 / 骨骼上限仍属 [CD-63 §1.7](../60-plan/63-open-decisions.md) 延期。工具版本见 [CD-51 §1](../50-engineering/51-dev-environment.md)，入库规范见 [CD-51 §5.1](../50-engineering/51-dev-environment.md)，门禁状态见 [CD-53 §4.1](../50-engineering/53-testing-and-ci.md)。
- `monetization_phase1 = none_no_hooks`。
- `character_select_screen = planned_after_main_lobby_shell`（2026-09-10 人类拍板）：一期新增**角色选择界面**，作为大厅顶级入口，排在主大厅壳（双玩法入口，随 M6 BASTION 落地）之后实施，当前不开工。此前文档无此设定——这是填补计划空白，不覆盖任何旧决策。本决策只定「有这个界面」与排期：可选角色池内容与素材入库范围仍受 D-F9 未拍约束，皮肤商业化仍属 `monetization_phase1 = none_no_hooks` 的不做项。口径见 [CD-12](../10-product/12-product-structure.md) 与 [CD-61](../60-plan/61-milestones.md)。
- `m5_audio_module = engine_agnostic_reusable_layer`（2026-09-10 人类拍板）：**音频系统挂入 M5**，并要求做成独立模块、游戏无关、业务层与底层解耦，BASTION 直接复用同一模块而不重开号。此前文档只有 F 线 FB 的 `PlaySfx`（8 个内部临时 WAV、静态类、一次性 `AudioStreamPlayer`、无池化 / 无节流 / 无 BGM / 无音量设置），也没有任何"音频模块归谁"的归属——本决策同时填补 M5 产出与模块归属两处空白，不覆盖旧决策。落成可检查的形式：模块在 `game/src/audio/`（目录归 [CD-41 §5](../40-technical/41-architecture.md)），模块内**零玩法词汇**由 `tools/redline-scanner/` 机械保证并进 CI；玩法事件到 cue id 的映射留在业务层；只有 `audio_backend.gd` 碰 `AudioServer`。音频永不进 `SimulationWorld` / `hash_state` / 协议帧，Headless 与 CI 静默（宪法第五条）。同日一并拍板：素材格式为 **OGG**（音效与背景音乐都是，人类持续补充，因此 bank 与 catalog 必须支持增量登记）；模块目录 `game/src/audio/` **自愿等同宪法第二十三条**的严格类型强度（只在 CD-41 §5 声明，**不改宪法条文**）；**不新增 Autoload**（显式实例 + 宿主节点挂载）；总线为 Master / Music / Sfx / Ui / Ambience 五条，默认 Master 0 dB、Music −6 dB、其余 0 dB；音频资产预算（格式 / 采样率 / 声道 / 时长 / 单文件体积 / 总量）落 [CD-11 §8.3](../10-product/11-scope-and-platforms.md)，那里是所有者，本行不复述数值。**未覆盖**：素材来源与许可（宪法第十八条，人类逐案确认，B2 入库前必须有答案）；运行时并发声部数、内存与解码开销（属实现侧，由声部池与节流承担，数字未拍）。TRAPRUSH 没有击杀与 gameover 概念，角色选择界面亦未开工（`character_select_screen`）：对应素材按计划 §3.4 映射为推击命中 / 他人环境失败 / 环境失败复位 / 结算收束 / 通用 UI 选择，**不因为有音效就发明机制**（宪法第一条）。v1 快照帧无 `vy` / `stun` / 事件字段，联机跳跃与硬直音只能靠相邻快照位姿近似，会漏播也会误播；修它要改协议帧，本号不改。章节拆分与待拍板清单见 [M5 章节计划](../../docs/plans/m5-traprush-vertical-slice.md)，里程碑口径见 [CD-61 §2 M5](../60-plan/61-milestones.md)。该句被 `audio_bank_ci = content_validator`（2026-09-11）补充：A2 把 bank 校验并入已有 `tools/content-validator/`，不新建 `tools/audio-bank/`。这是执行 [M5 计划](../../docs/plans/m5-traprush-vertical-slice.md) §5.2 问题 5 的推荐项以开工 A2；`tools/bot-runner/` 仍属 C5，不是本行拍板。catalog 在 `game/src/shared/schema/audio_cue_catalog.gd`，bank JSON 在 `game/content/audio/banks/`。格式 / 采样率 / 声道 / 时长仍属 B2。该句再被 `traprush_audio_router = offline_event_table`（2026-09-11）补充：B1 把 TRAPRUSH 事件→cue 的唯一映射表落到 `game/src/games/traprush/traprush_audio_router.gd`，Solo / Preview 从本地会话 diff 派生事件并接 3D 循环环境音；`AudioListener3D` / `AudioStreamPlayer3D` 只出现在 `audio_backend.gd`。不改玩法数值，不入库新素材（新 cue id 复用 F 线 WAV），联机快照 diff 仍属 B2。该句再被 `match_audio_online = snapshot_diff_ogg_budget`（2026-09-11）补充：人类确认本地音频授权可用于正式入库（宪法第十八条）。B2 把联机快照 diff 落到 `game/src/client/match_audio_source.gd`（坏帧 / 乱序 / 重复 tick 不播；远端带米制坐标走 3D）；正式流文件为 OGG Vorbis，落 `game/content/audio/sfx/` 与 `music/`，走 Git LFS；`tools/content-validator` 按 [CD-11 §8.3](../10-product/11-scope-and-platforms.md) 查格式 / 采样率 / 声道 / 时长 / 单文件 / 总量，0 个文件明确输出「什么都没查」。覆盖该句中「素材来源与许可……B2 入库前必须有答案」「格式 / 采样率 / 声道 / 时长仍属 B2」与「联机快照 diff 仍属 B2」。**未覆盖**：运行时并发声部数；v1 快照仍无 `vy` / `stun` / 事件字段。
- `client_signing = platform_minimum`；`steam_integration_timing = after_core_adapter`。

## D.2 UGC、发现与发布

- `ugc_visibility = automatic_public`；仅指长期测试环境。
- `ugc_asset_scope = platform_assets_only`。
- `publication_safety = technical_only`：只做技术、结构和预算验证。
- `public_text_scope = no_free_text`：系统名/词库 + 结构化标签。
- `ugc_playability_label = publish_with_status`：机器人未完成仍公开并标记。
- `ugc_feedback = ratings`；`ugc_rating_dimensions = overall_plus_tags`。
- `ugc_sorting = simple_tabs`：最新、评分、游玩次数、已验证。
- `ugc_publish_rate = no_limits`；`ugc_feed_dedup = manual_cleanup`。
- `ugc_validation_concurrency = one_concurrent_queue`。
- `ugc_validation_time = tiered_validation`：60 秒快检 + 10 分钟后台抽样。
- `draft_autosave = command_log_checkpoints`。
- `collaborative_editing = single_editor_fork`：单写者；他人可只读或复制私有草稿；设备租约互斥。
- `ugc_forking = never_allowed`。
- `ugc_withdrawal = hard_delete`；`hard_delete_audit_exception = minimal_tombstone`。
- `asset_versioning = latest_alias`，后由 `asset_latest_gameplay_boundary = visual_latest_gameplay_immutable` 限定：仅视觉/音效自动 latest。该句被 `gameplay_asset_contract = catalog_reference_plus_bundle_geometry`（2026-08-29，[ADR-0006](../../docs/adr/0006-gameplay-asset-contract.md) 整包采纳推荐 Q1=B / Q2=A / Q3=A / Q4=A / Q5=A / Q6=A）落实为可执行形状：新增第 19 个组件 `gameplay_asset`（只含 `asset_id` + `gameplay_version`，创作者**不能自填尺寸**）；平台内置资产清单在 `game/src/shared/schema/gameplay_asset_catalog.gd`，编译期要求 id 已登记且版本为当前版本，所以目录改几何会让旧内容重编译失败、必须发新内容版本；SimulationBundle 升 v2（P4，宪法第十八条人类门禁），新增 `assets` 袋携带权威碰撞与挂点，每个占用袋带资产引用，已发布内容与旧回放按各自发布时的形状裁决；`TraprushTopologyLoader` 的 `half = cell / 2` 退场，半长来自 `assets`，只接受 `box`（sphere/capsule/platform_prefab 在 Schema 白名单内但无权威静态实现，直接拒）；`zone.shape` 回归"触发与查询区域"，此前它被校验却被编译期丢弃。占地由碰撞 AABB 派生、挂点只留字段位、视觉网格不进 bundle、一期不含导航。v1 仍解码并迁移到内置"占满一格"资产，占用逐字节不变（任意 `cell`）。一期 bundle 未签名，"不可变"靠字段与编译期准入，不是密码学不可变。数字与字段只在 [CD-42 §1.2 / §1.3](../40-technical/42-contracts-and-rulevm.md) 与 [CD-31 §5](../30-ugc/31-ugc-principles.md) 维护。该句中"视觉网格不进 bundle"被 `visual_asset_resolution = client_side_latest_catalog`（2026-08-30）落实为可执行形状：视觉由 `game/src/shared/visual_asset_catalog.gd` 在客户端按 `latest` 解析，`client` 的对局映射与 `creator` 的 Preview 映射读同一份（否则两条壳会漂成两套美术）；解析失败（缺资产 / 路径非场景 / 空路径）一律回退占位盒，不让"美术没到位"变成"大厅不出人"；席位色由 `material_overlay` 半透明薄膜承担，占位盒本体改用 `layers = 0` 退出渲染而**保留网格与色值**，所以名次、朝向标记与本席预测读的仍是同一节点。第一批入库资产是角色占位视觉与地块占位视觉（`game/content/assets/{characters,terrain}/`），至此 [ADR-0006 §7](../../docs/adr/0006-gameplay-asset-contract.md) 那条"视觉数值今天仍与权威碰撞相等"的边界**解除**：角色 0.74×1.03×0.61 m、地块等比缩到一格宽的薄板、权威胶囊 0.125 格、占位盒 1 米。**接线按袋类型而非 `asset_id`**：`solids` 袋（踩得到的固体）铺地块、玩家节点用角色，因为那是玩法语义；机关与可破坏箱保留 D4 危险色，检查点垫 / 传送门 / 终点因此仍无独立视觉。地块贴合规则：按资产**自身 AABB** 等比缩到水平最长边一格、水平居中、顶面对齐**占位盒**顶面（不写死系数；对齐占位盒而非权威 AABB，因为视觉不读裁决数据，耦合必须单向）。**未覆盖**：`asset_id → 视觉`的逐资产解析仍未实装，角色胶囊仍不是资产。同时 `asset_intake_form = embedded_texture_single_file`（2026-08-30）补上入库形态：`.glb.import` 必须设 `gltf/embedded_image_handling=3`（Embed as Uncompressed，枚举由引擎自证），默认 `1`（Extract）会解包外部贴图、同一份像素入库两遍；每个资产只入库 `.glb`（LFS）+ `.glb.import` 两个文件；新资产必须被 `--package-check` 覆盖（角色按**实例化**判，地块**连贴合一起判**，因为退化网格会先过 load 再静默回退）。内嵌未压缩只解决入库形态，**不省显存**，KTX2 / Basis 仍未测。目录与规范只在 [CD-41 §5](../40-technical/41-architecture.md#5-monorepo-目录) 与 [CD-51 §5.1](../50-engineering/51-dev-environment.md) 维护。
- `ugc_data_format = json_source_binary_bundle`。
- `content_sign = sidecar_hmac_sha256`（2026-09-09）：M4b 第 1 章落地 `ContentSign` sidecar。ContentHash 是 StateHasher 规范编码 `SimulationBundle.to_dictionary()`（v2 wire）的 SHA-256；平台签名是 HMAC-SHA256，覆盖 `content_id` + 换行 + `version` + 换行 + hash。信封恰好五键，多余键 / 错 id / 错钥 / 篡改 bundle 拒绝。公开对局开局锁定哈希；官方课仍不要求信封。不改 Bundle Schema、不改匹配 HTTP 课表。该句中「无 `latest`、无发布 HTTP」被 `content_publish = signed_latest_pointer`（2026-09-10）覆盖：控制面 `POST /content/publish` 校验信封 HMAC（不重算 ContentHash），一事务写入 `content_versions` 并切 `content_latest`；`GET /content/:id/latest` 与 `GET /content/:id/versions/:version` 读回。Godot `ContentCatalog` 按 `latest` 开新房，已开对局锁开局哈希。版本不可覆盖、无 latest 时必须 v1、有 latest=N 时必须 N+1。密钥走 `CONTENT_SIGN_KEY`，未设用文档化测试钥。匹配 HTTP 课表仍只 `course_01`/`course_02`/`course_03`。无广场、无账号。该句中「无 P0/P1 运行房补丁、无回滚 HTTP」被 `content_live_patch = p0p1_sidecar_plus_latest_rollback`（2026-09-10）覆盖：P0/P1 是 sidecar 补丁，不覆盖基础 ContentHash；PatchHash = StateHasher 规范编码 ops 数组的 SHA-256；HMAC 覆盖 `content_id` + LF + `base_version` + LF + `seq` + LF + hash；信封七键。运行房只收分类后的 P0/P1（白名单 `visual.fx_revision` / `destructibles.durability` / `hazards.cooldown_ticks`），P2/P3 拒绝。全量下发给同一 `content_id` + 锁住 `base_version` 的房间，开局 latch 或检查点垫验收后生效。`note_fault` 立刻追加反向 PatchHash。`latest` 可切回已签名旧版本，下一号 = 已存 max+1。结算 Godot payload 带哈希序列，控制面结算 HTTP 不改。官方课仍不要求信封。覆盖 D.2 `gameplay_asset_contract` 中「一期 bundle 未签名」这一句的哈希/签名半边；几何不可变仍靠字段与编译期准入。落点 `game/src/ugc/content_sign.gd`、`game/src/ugc/content_catalog.gd`、`game/src/ugc/content_patch.gd`、`game/src/games/traprush/match_session_patch.gd`、`backend/control-plane/src/server_content.ts`、`backend/contracts/src/content_publish.ts`、`backend/contracts/src/content_patch.ts`。口径见 [CD-33](../30-ugc/33-hot-publish.md)、[CD-42 §3.4](../40-technical/42-contracts-and-rulevm.md#34-实现落点) 与 [CD-61](../60-plan/61-milestones.md)。该句中「无广场、无账号」被 `content_plaza = public_tabs_wordbank_tags`（2026-09-10）覆盖广场半边：发布成功自动进公共列表；四标签 `newest` / `rating` / `plays` / `verified`；系统名 = FNV-1a32 词库 `{adj}_{noun}`；标签只来自占用袋白名单；未完成仍公开但不进已验证；`POST /content/:id/plays` 记游玩并标已验证；结算后 1–5 星 + 结构化标签走 `POST /content/:id/ratings`（本期不把 MatchHost 结算接到广场）；大厅可 Solo 已签名 UGC，匹配 HTTP 仍只官方课。账号认领仍待。该句中「账号认领仍待」被 `account_claim = guest_username_password_claim`（2026-09-10）覆盖：Guest ID + 恢复密钥；用户名 + 密码注册 / 登录（scrypt，无邮箱 / CAPTCHA / 找回）；`PUT /drafts` 存最新 AuthoringDocument JSON；注册或 `POST /accounts/claim` 把 Guest 草稿转到账号。大厅「账号」窗走进程内 `AccountCatalog`。发布 HTTP 与入场票据仍不绑账号。落点 `backend/contracts/src/account.ts`、`backend/control-plane/src/server_accounts.ts`、`game/src/ugc/account_catalog.gd`、`game/src/client/account_entry.gd`。口径见 [CD-13](../10-product/13-account-and-session.md)。落点 `backend/contracts/src/content_plaza.ts`、`backend/control-plane/src/server_plaza.ts`、`game/src/ugc/content_plaza.gd`、`game/src/client/content_plaza_entry.gd`。口径见 [CD-12](../10-product/12-product-structure.md)、[CD-31](../30-ugc/31-ugc-principles.md) 与 [CD-33](../30-ugc/33-hot-publish.md)。该句中「发布 HTTP 与入场票据仍不绑账号」的**发布半边**被 `content_submit = authed_control_plane_cosign`（2026-09-11 人类拍板）覆盖：玩家路径 `POST /content/submit`，控制面会话鉴权（Guest 或正式号）后代签入库；客户端只上传 bundle + hash，不持 `CONTENT_SIGN_KEY`、不带 signature、不自报 id / version。现有持钥 `POST /content/publish` 保留给测试 / 工具。禁止两跳「先取 signature 再 publish」。每条内容绑主人身份。P0/P1 补丁接口不动。诚实边界：控制面代签信任客户端上报的 ContentHash；对局加载必须再算一遍。**未覆盖**：HMAC 信封算法、`latest` 指针、官方课不要求信封、入场票据仍不绑账号。接线属 M5 C3，本行只拍口径。口径见 [CD-33 §2.2](../30-ugc/33-hot-publish.md#22-玩家发布路径已交m5-c3)、[CD-13](../10-product/13-account-and-session.md) 与 [CD-42 §3.5](../40-technical/42-contracts-and-rulevm.md#35-匹配与玩家发布-httpc3-已接线c4-已拍未接线)。
- `content_submit_wired = editor_button_plaza_get`（2026-09-11）：M5 C3 接线。覆盖 `content_submit = authed_control_plane_cosign` 中「本行只拍口径」。编辑外壳发布按钮走 `POST /content/submit`；广场真拉 `GET /content/plaza`；Solo 缺 bundle 时 `GET /content/:id/latest`。该句中「C4 匹配 HTTP 仍未接线」被 `match_ugc_online_wired = pinned_content_invite`（同日）覆盖。口径见 [CD-33 §2.2](../30-ugc/33-hot-publish.md#22-玩家发布路径已交m5-c3)、[CD-13](../10-product/13-account-and-session.md) 与 [CD-42 §3.5](../40-technical/42-contracts-and-rulevm.md#35-匹配与玩家发布-httpc3-已接线c4-已接线)。
- `match_ugc_online_wired = pinned_content_invite`（2026-09-11）：M5 C4 接线。覆盖 `content_submit_wired` 中「C4 匹配 HTTP 仍未接线」。匹配 HTTP 可选 `content: { id, version }`（与 `course` 互斥；空 body 仍默认 `course_01`）；建房钉死 version；官方响应仍只 `course`；UGC 回 `course: null` + `content` + `content_hash`。MatchHost 经控制面 HTTP 取该 version 信封，不查库。MatchServer `--content-envelope=` 验签并重算 hash 后开局锁 `content_hash`。邀请：Web `?room=`，桌面 `主机[:控制面端口]` + 房间码 + 一键复制；内容引用不进 URL。结算 HTTP 不改。口径见 [CD-42 §3.5](../40-technical/42-contracts-and-rulevm.md#35-匹配与玩家发布-httpc3-已接线c4-已接线)、[CD-12](../10-product/12-product-structure.md) 与 [CD-44](../40-technical/44-deployment.md)。
- `schema_compatibility = current_plus_two`。
- `rule_vm_compatibility = versioned_semantics`。
- `creator_rule_access = templates_web_graph_desktop`。
- `dual_editor_architecture = shared_framework_mode_tools`。该句被 `authoring_document = shared_json_snapshot`（2026-08-23）收窄：桌面完整与 Web 轻量交换同一份 AuthoringDocument（`schema_version` / `cell` / `revision` / `entities`）。表面名不入库。v1 不含 Rule VM 图。覆盖 `ugc_data_format` 的 JSON 源这一侧；binary bundle 仍待。口径见 [CD-32 §1.4](../30-ugc/32-editor-and-preview.md#14-共同数据模型)。该句中「binary bundle 仍待」被 `simulation_bundle = traprush_topology_json_v1`（2026-08-24）覆盖：v1 SimulationBundle 是 TRAPRUSH 拓扑 JSON（`schema_version` / `cell` / `source_revision` / `pads` / `portals`）。AuthoringWorld 全量编译，不是增量子图。dangling 传送省略。检查点缺 transform 整份拒绝。加载为非固体垫盒 + PortalGraph。不签二进制，不编 Rule VM，不结算。该句中「pads / portals」被 `simulation_bundle_finish = zone_tag_occupancy`（2026-08-24）覆盖：v1 袋另含 `finish`（0 或 1 个终点占用袋：`entity_id` / `x` / `y` / `z`）。`zone.tags` 含 `finish` 且有 `transform` 的实体编进去；缺 transform、两份终点、或终点与检查点/传送同实体则整份拒绝。加载为非固体终点盒。占用半长仍由格子 `cell / 2` 推导。该句中「不接到 Preview 试玩」被 `preview_play = traprush_topology_session`（2026-08-24）覆盖：安全点 `try_start_play` 编译当前 Preview 世界、加载、在最小 `order` 垫生成胶囊；开玩进入 tick；Play/Stop 与玩家表现桩。不接键盘意图、不锁重力、不接检查点占用验收、不接多人试玩。签名二进制包仍待。口径见 [CD-32 §3](../30-ugc/32-editor-and-preview.md#3-从编辑到预览) 与 [CD-42 §3.4](../40-technical/42-contracts-and-rulevm.md#34-实现落点)。该句中「不接键盘意图」被 `preview_play_move = wasd_move_intent`（2026-08-24）覆盖：开玩后 `try_apply_play_intent` 只接受已有 MoveIntent；Preview 窗口可见时 WASD 按世界方向编码。位移由调用方/表现步长提供，不锁产品速度或 Tick Hz。不接 Jump/Shove、不锁重力、不接检查点占用验收、不接多人试玩。口径见 [CD-32 §3](../30-ugc/32-editor-and-preview.md#3-从编辑到预览) 与 [CD-42 §3.4](../40-technical/42-contracts-and-rulevm.md#34-实现落点)。该句中「不接检查点占用验收」被 `preview_play_pads = occupancy_accept`（2026-08-24）覆盖：开玩后用已有 PadAccept，胶囊与垫相交才推进有序进度。占用由 Preview 观察，不是完成断言。不接传送落地、冲线、重力、Jump/Shove、多人试玩。口径见 [CD-32 §3](../30-ugc/32-editor-and-preview.md#3-从编辑到预览) 与 [CD-42 §3.4](../40-technical/42-contracts-and-rulevm.md#34-实现落点)。该句中「不接传送落地」被 `preview_play_portals = occupancy_land_exit`（2026-08-24）覆盖：开玩后用已有 `TraprushPortalLanding.try_land_exit`，胶囊与传送源点占用盒相交才单跳落地。传送袋含源点 `x/y/z`；加载为非固体源点盒。`two_way` dest 体积门闩直到离开。出口占用则等待且本帧不位移。传送不代验收检查点。状态 `floor=n`。不接冲线、重力、Jump/Shove、多人试玩。口径见 [CD-32 §3](../30-ugc/32-editor-and-preview.md#3-从编辑到预览) 与 [CD-42 §3.4](../40-technical/42-contracts-and-rulevm.md#34-实现落点)。该句中「不接冲线」被 `preview_play_finish = occupancy_cross`（2026-08-24）覆盖：开玩后用已有 `TraprushFinishAccept.try_cross`，全部强制检查点完成后胶囊与终点占用盒相交才记 `finish_tick`。占用由 Preview 观察，不是客户端冲线断言。无 FinishIntent。`zone.tags` 含 `finish` 编进 bundle `finish`（0 或 1）。不结算、不在线写。状态 `finish=n`（未冲线为 -1）。不接重力、Jump/Shove、多人试玩。口径见 [CD-32 §3](../30-ugc/32-editor-and-preview.md#3-从编辑到预览) 与 [CD-42 §3.4](../40-technical/42-contracts-and-rulevm.md#34-实现落点)。该句再被 `authoring_editor_shell = code_created_window`（2026-08-23）覆盖：内部开发编辑器当前落点是 `AuthoringEditorShell` 代码创建独立 `Window`，只发已有 EDIT `op`。关闭只隐藏。打开 Preview 是当时世界的快照，后续编辑不自动跟。不是 EditorPlugin，不改 `main.tscn`。口径见 [CD-32 §1.1](../30-ugc/32-editor-and-preview.md#11-内部开发编辑器)。该句再被 `authoring_editor_map = transform_box_placeholders`（2026-08-23）覆盖：Editor 窗口挂同一套 `AuthoringPreviewMap`，按 AuthoringWorld 重建 1 米占位盒与 gizmos。编辑写入后立即重建；Preview 仍是快照。占位盒不是碰撞体。口径见 [CD-32 §3](../30-ugc/32-editor-and-preview.md#3-从编辑到预览)。该句再被 `traprush_editor_tools = panel_on_shared_shell`（2026-08-23）覆盖：`TraprushEditorPanel` 挂在共用 Editor 外壳上，检查点 / 传送门 / 删除最后实体只发已有 EDIT `op`。连续两次 Place portal 把悬空对成 `two_way`。Floor up/down 只改下一次 `cell_y`。BASTION 面板与 EditorPlugin 仍待。口径见 [CD-32 §3](../30-ugc/32-editor-and-preview.md#3-从编辑到预览)。该句再被 `authoring_validator_panel = issue_list_focus`（2026-08-23）覆盖：`AuthoringValidatorPanel` 在编辑外壳上只读 `evaluate_reachability`，列出已有问题码。Focus 把 Editor 相机对到有 `transform` 的实体。无实体问题只进列表。不是写入门禁，不是新 `op`。走路可达仍待。口径见 [CD-32 §3](../30-ugc/32-editor-and-preview.md#3-从编辑到预览)。该句再被 `official_course_1 = authoring_document_json`（2026-08-23）覆盖：第一张官方 TRAPRUSH 赛道是 `game/content/official/traprush/course_01.json`（AuthoringDocument）。两层检查点用 `two_way` 传送对连通，发布检查 ok。不是新 op。第二张赛道仍待。口径见 [CD-32 §3](../30-ugc/32-editor-and-preview.md#3-从编辑到预览)。该句中「第二张赛道仍待」被 `official_course_2 = lateral_two_way_authoring_document`（2026-08-23）覆盖：第二张官方 TRAPRUSH 赛道是 `game/content/official/traprush/course_02.json`。同层检查点用跨层且侧向（+Z）的 `two_way` 传送对连通，发布检查 ok。与第一张（纯竖直、z=0）布局不同。不是新 op。第三张仍待。口径见 [CD-32 §3](../30-ugc/32-editor-and-preview.md#3-从编辑到预览)。该句再被 `authoring_editor_plugin = tools_menu_opens_shell`（2026-08-23）覆盖：内部开发 EditorPlugin 落在 `game/addons/authoring_editor/`。Project > Tools > Authoring Editor 经 `AuthoringEditorPluginHost` 打开已有 `AuthoringEditorShell`。关闭只隐藏。不是新 op。覆盖 `authoring_editor_shell` 的「不是 EditorPlugin」与 `traprush_editor_tools` 的「EditorPlugin 仍待」。BASTION 面板仍待。已提交工程启用 GUT 与 `authoring_editor`，不含 `godot_ai` / `_mcp_game_helper`。口径见 [CD-32 §1.1](../30-ugc/32-editor-and-preview.md#11-内部开发编辑器)。该句再被 `authoring_draft = local_latest_checkpoints`（2026-08-23）覆盖：内部开发编辑器成功写入后把最新 AuthoringDocument 写入 `user://authoring_draft.json`，检查点每 50 条（含第 1 条）最多 30 个。空会话打开恢复 `latest`。损坏或多余键拒绝。不写 `res://`。不是新 op。该句再被 `authoring_draft_editor_flush = globalize_flush_rebind`（2026-08-23）收窄：EditorPlugin 用全局 `class_name` 构造 store 并在 `add_child` 后绑定；落盘 `flush`/`close` 后回读；恢复后工具条同步下一个 id。该句再被 `authoring_draft_editor_io = plugin_tool_file_access`（2026-08-23）覆盖：Godot 编辑器内由 `@tool` plugin.gd 响应 `world_committed` 读写 `user://authoring_draft.json`；非 tool 外壳在 `Engine.is_editor_hint` 时不自己 FileAccess。GUT/Headless 仍走 store。2 秒上传与 5 分钟定时仍待。口径见 [CD-32 §2](../30-ugc/32-editor-and-preview.md#2-草稿持久化与协同)。该链中 `authoring_editor_shell` 的「打开 Preview 是当时世界的快照，后续编辑不自动跟」被 `authoring_preview_follow = forward_committed_patch`（2026-08-24）覆盖：成功的 `try_apply` / Undo / Redo 把同一条 EDIT `op` 按 **Preview 世界** 算出的等级在安全点转发给已连接 Preview，`expected_revision` 取写入前的编辑世界 revision。失败写入不转发；转发被拒只脱同步、不回滚编辑；`import_document` 是整份替换故一律脱同步，须重新 `open_preview`。窗口隐藏不影响跟随。这是 `preview_patch_mode = persistent_incremental` 与 `multiplayer_preview_editing = author_live_patch` 的单人侧落地，不新增 `op`、不结算。多人试玩房、云端上传与时延自动门禁仍待。口径见 [CD-32 §3](../30-ugc/32-editor-and-preview.md#3-从编辑到预览)。
- `authoring_lattice = scale_cell_xyz`（2026-08-23）：编辑吸附格默认边长 = `Fixed.SCALE`（1 表现米，[ADR-0005](../../docs/adr/0005-fixed-point-numeric-model.md)），楼层索引 = `y / cell` 向零。传送连线从 `portal.target_id` 派生，分类 `two_way` / `one_way` / `dangling`。编辑期允许悬空；禁止自环与指向无 `portal` 的已存在实体。不新增第四个 EDIT `op`。可达性、连线可视化与 Preview 仍待。口径见 [CD-32 §3](../30-ugc/32-editor-and-preview.md#3-从编辑到预览)。该句中「可达性仍待」被 `authoring_reachability = publish_path_and_cycle`（2026-08-23）覆盖：发布前检查悬空传送、`one_way` 跟随链循环、检查点 `order` 唯一、以及按楼层+传送的必经通路。`two_way` 配对视为落点终止，不是循环。不在 `try_apply` 上跑。不锁 hop 上限、预算、Preview、连线可视化或走路可达。口径见 [CD-32 §3](../30-ugc/32-editor-and-preview.md#3-从编辑到预览)。

## D.3 TRAPRUSH

- `traprush_camera = isometric_soft_follow`。
- `traprush_movement = single_jump_autostep`。
- `traprush_player_collision`：实体碰撞、阻挡推挤，道具/工具可眩晕。
- `traprush_combat_intensity = nonlethal_control`。
- `traprush_anti_block = no_protection`。
- `traprush_chokepoint_rule = hard_choke_allowed`。
- `traprush_stalemate = baseline_shove`。
- `traprush_item_distribution = fixed_seeded_spawns`。
- `traprush_destructible_scope = shared_world`。
- `traprush_failure_penalty = unlimited_respawn`。
- `traprush_match_duration = creator_defined`；`traprush_time_bounds`：内容级不设上限。
- `match_server_lease = renewable_lease`。
- `traprush_finish_requirement`：默认终点，创作者可组合自定义结束条件。
- `custom_end_validation = typed_bounded_terminal`。
- `traprush_player_count`：在线 1～8 人，单人也走服务器。
- `traprush_web_player_count = same_eight`。
- `player_collision_prediction = predict_remote_capsules`。
- `authoritative_character_physics = custom_kinematic`。
- `traprush_offline_opponents = local_ghost`。
- `authoritative_motion_dof = upright_3d_kinematic`。
- `traprush_inventory = 延期`。
- `traprush_respawn_stun = 1_second`（2026-08-28）：纠偏 D5 中档。一期环境失败（出界 / 踩实心机关）固定硬直 **1.0 s**。不锁 Tick Hz。对局 / Solo 经 `TraprushPlayStubs.RESPAWN_STUN_MS` 与 `PHYSICS_TICKS_PER_SECOND_PLACEHOLDER`（当前 60）换成会话 tick；Preview 仍 1 次 Advance。覆盖 CD-63 §1.3 中「复活硬直」秒数空白，以及 D.8 `match_oob_reset` 里「不写复活硬直」的产品秒数空白。§1.3 其余数值仍延期。口径见 [纠偏方案 D5](../../docs/plans/course-correction-2026-08.md) 与 [CD-21 §6](../20-gameplay/21-traprush.md#6-单局流程)。该句中「不锁 Tick Hz」被 `e3_lock = promote_existing_stubs_2026_09_02`（2026-09-02）覆盖：对局 / Solo 60 physics tick/s 升为锁定值，见 D.8。
- `traprush_min_items = bomb_and_sprint`（2026-08-26 拍板，2026-09-02 C5 回写）：一期最小道具 = **爆破球 + 冲刺**。服务端裁决拾取 / 使用 / 命中；伪造命中被拒。覆盖 CD-63 §1.2 中「正式道具清单」的一期最小集空白，以及 CD-21 旧「不是已锁定的正式道具表」表述。完整道具 / 炮塔 / 单位 / 障碍表与槽位规则仍延期。口径见 [CD-21 §5.3](../20-gameplay/21-traprush.md#53-道具一期最小集已锁)。
- `f_line_jump_gravity = scale_five_four_snappy`（2026-09-07）：F 线 D-F2 / D-F3。`JUMP_DY = SCALE*5/4`（当拍冲量 1.25 格，能上一格台阶）；`FALL_DY = -JUMP_DY`（下一拍到顶）。接线属 F 线 FD。覆盖 CD-63 §1.3 中跳跃高度与重力空白；推击 / 爆破 / 重生仍延期（D-F4 / D-F5 / D-F6 保持现值）。口径见 [CD-21](../20-gameplay/21-traprush.md) 文首与 [CD-61 F 线](../60-plan/61-milestones.md)。该句中「一拍到顶」被 `jump_arc = quarter_cell_impulse_scale_32_accel`（2026-09-09）覆盖：人类验收同层 hop 整屏跟着抖、编辑器原地跳被电梯当乘客复位。冲量改 `SCALE/4`、加速度 `-SCALE/32`（约 8 拍到顶、峰值约 1.125 格，仍够上一格台阶）；跟随相机空中冻结 Y，换层落地或乘电梯（仍接地）才改高度；`vy > 0` 不当移动平台乘客。不改相机距离/FOV、不改 SimulationCore 定点数。口径见 [CD-21](../20-gameplay/21-traprush.md) 文首。
- `traprush_portal_switch = occupancy_gated_landing`（2026-09-09）：可玩性深化第六批。传送占用 + `zone.tags` 的 `portal_switch` + 已有 `interactable.link_group`。可选 `portal_switches` 袋（省略 ≡ 空）。关上不落地；传送门自己的占用不开组。不接线 `InteractIntent`，不改命令帧 / 快照帧。口径见 [CD-21](../20-gameplay/21-traprush.md) 文首与 [CD-61](../60-plan/61-milestones.md)。
- `f_line_hud_clock = tick_splits_settlement_table`（2026-09-07）：F 线 FA。大厅 / Solo / Preview 局时由权威 `tick` 格式化；过垫分段只在客户端记 `accepted_count` 上升沿，不进 `hash_state`、不改 [CD-21 §6.1](../20-gameplay/21-traprush.md#61-排序优先级) 排序。结算表画已有 `rows[]` / `mvp_slot`：在线仍 GET，Solo 本地 `try_build`，客户端仍不 POST。覆盖 D.8 `match_settlement_panel` 里「HUD 只有 `settled=` token」的表现缺口，不覆盖写库路径。该句中「大字局时」与结算表居中被 `f_line_readable_course = spread_hud_zoom`（2026-09-07）覆盖：人类窗口验收示范课拥挤、HUD/世界标签挡视线、镜头全固定。示范课主路拉开到约 8 格（出界桩 ±8 格内）；跳台 / 道具 / 机关 / 平台分到 ±Z 侧廊，不再堆在出生点周围；局时 / 状态行 / Label3D 字号从 `placeholder_spec` 读；结算表改贴窗口右上；滚轮 8–20 m 调距、中键平移 ≤4 m，**方位角与俯角仍 45°**，默认距离仍是 D4 未答的 √136。不改跳跃 / 推击 / 爆破 / 协议。口径见 [CD-11 §8.2](../10-product/11-scope-and-platforms.md)、[CD-12](../10-product/12-product-structure.md) 与 [CD-21](../20-gameplay/21-traprush.md)。
- `play_anim_visual = clip_four_plus_pose_four`（2026-09-05，人类拍板路线 A）：C4 表现动画状态从「只锁名字」推进到「驱动视觉」。`game/src/shared/play_anim_visual.gd` 把 `PlayAnimState` 的裁决结果接到角色 `visual` 节点：`idle` / `run` / `hit` / `portal` 播 GLB 内置 clip（`idle` / `run` / `gesture-negative` / `dance`），`jump` / `land` / `shove` / `break` 用程序化姿态偏移（pitch + lift）补；两张表条数之和恰好等于八态，由测试钉住「不允许一个状态两边都有、也不允许漏」。clip 态先把 visual transform 归零回 `CHARACTER_FOOT_LIFT` 基准再播，姿态态停 clip 并在基准上**叠加**偏移——所以 attach 定位与本文件必须同源，改一处要动另一处。缺 `AnimationPlayer`、缺 clip 名、visual 为空一律降级为静止且不报错（与 `SharedVisualAssetCatalog` 的回退哲学一致）。同刀 `CHARACTER_SCENE_PATH` 由 `char_runner_base.glb` 换为 `animal-cat.glb`（Kenney Cube Pets，**CC0**，7 网格 / skins=0 / 684 面 / 512 贴图 / 171.9 KB）：**换它的理由不是审美，是前两个生成产物都是静态网格、连 `AnimationPlayer` 都没有**，接线无处落地。它因多网格而走 `instantiate` 而非共享 Mesh 快路径，这是必须的（快路径只保留 `Mesh`，会丢掉 `AnimationPlayer` 与部件层级）；每帧成本不受影响，因为 `_sync_players` 复用席位节点。**覆盖** CD-21 §3.4 原「**不播 clip**」与 CD-61 M-Art 剩余产出里的「clip / 绑定动画」。**未覆盖**：`hit` 用摇头、`portal` 用跳舞是语义近似（Cube Pets 无受击 / 传送动作），属路线 A 已接受的折衷；姿态角度与升降是占位数值，未定稿；不引入时间轴、clip 间无 blend，**动画时长与过渡仍属 [CD-63](../60-plan/63-open-decisions.md)**；只在 **Solo 与 Preview** 接线，v1 快照没有 `vy` / `stun_remaining` / 库存，在线远端算不出 `airborne` 与 `hit`，接它要改协议帧（宪法第十八条）；猫的 AABB 1.25 × 1.586 × 1.806 m **明显超一格**（尾巴与四足伸展），视觉不参与裁决（权威胶囊仍 0.125 格）但真机可见，与「门比人矮」「滚柱超一格」同类记账。**新增人类门禁事实**：cat 是仓库里第一个第三方资产，来源与条款归档在新增的 `game/content/assets/ATTRIBUTION.md`（此前全是自生成资产，无第三方条款可归档），**入库范围仍待人类拍板**（当前只取 24 只里的 1 只）。`--bot-run` 步数仍为 5 / 5 / 12，`expansions` / `search_ticks` 逐字段不变。口径见 [CD-21 §3.4](../20-gameplay/21-traprush.md#34-表现动画状态) 与 [CD-61 M-Art](../60-plan/61-milestones.md)。

- `character_visual_fit = aabb_span_zero_point_seven`（2026-09-07，人类看真机后「动物有点大」）：补上**角色贴合规则**。此前角色是唯一没有贴合的资产类别——地块走 `fit_tile_on_cell`、门 / 箱 / 滚柱走 `fit_prop_on_cell`（都按自身 AABB 等比缩到一格），只有 `attach_visual` 把资产原始尺寸直接摆上去；前两个角色水平最长边 0.75 / 0.74 m 本就小于一格，缺陷因此一直没暴露，换成猫（水平最长 1.806 m）后一眼可见。新增 `SharedVisualAssetFit.fit_character_on_cell`：按 AABB 等比缩到 `METERS_PER_CELL * CHARACTER_VISUAL_CELL_SPAN`、水平居中、脚底落在**权威胶囊底面**（不是 1 米占位盒底面——盒比胶囊高，对齐盒底会让角色重力落地后陷进固体顶面）。贴合后猫是 0.48 × 0.62 × 0.70 m，不再越格。**新增表现规格** `PlaceholderSpec.CHARACTER_VISUAL_CELL_SPAN = 0.7`：**D4 从没问过这一项**，是占位值而非产品比例，落 [CD-11 §8.2](../10-product/11-scope-and-platforms.md) 表并由 spec 单一配置源承载，改它只动一行。**不是 1.0** 的理由：门 / 箱那类静物占满整格是对的，角色不是——权威胶囊直径只有 0.25 格，视觉占满一格会贴边并与相邻格物件穿插。按 AABB 算而不写死系数，理由与 `terrain_tile` 那条相同：Cube Pets 24 只动物尺寸各异，写死系数等于每换一只都要重算；代价是贴合后高矮不一，那是保留动物本身比例的结果。**连带改动**：基准 transform 现在含 scale，无法再由常量重建，故新增 `character_base_transform(visual)`（贴合结果存 visual meta，未贴合回落到不缩放的老基准），`PlayAnimVisual` 与两条壳的 attach 都经它取基准——否则姿态态直接写 `Transform3D` 会抹掉缩放，表现是**角色一起跳就突然变大**；单测 `test_pose_keeps_the_fitted_scale` 钉住这条。**覆盖** `play_anim_visual` 那条里「基准由 `CHARACTER_FOOT_LIFT` 常量重建」的实现描述；该常量降级为未贴合路径的回退基准。**未覆盖**：视觉仍比权威胶囊大（0.48×0.62×0.70 vs 直径 0.25 / 高 0.375），属 [ADR-0006](../../docs/adr/0006-gameplay-asset-contract.md) Q4 = A 明确允许，不是待修缺陷；0.7 未定稿。缩放**只改表现**，`--bot-run` 三张课步数 5 / 5 / 12 与 `expansions` / `search_ticks` 逐字段不变即为证据。口径见 [CD-11 §8.2](../10-product/11-scope-and-platforms.md) 与 [CD-21 §3.4](../20-gameplay/21-traprush.md#34-表现动画状态)。
- `official_course_04 = vertical_tower`（2026-09-11 人类拍板）：M5 §5.2 问题 2 的 **04 半边**。`course_04` = 垂直塔（电梯 + 弹射 + 压板，考验上下层）。匹配 HTTP 白名单纳入 `course_04`；`course_f_playable` 仍不是匹配课。该句中「05 主题仍待」被 `official_course_05 = dual_route_energy_wall`（同日）覆盖。该句中「C3 密钥去向；C4 匹配 HTTP 扩到已签名 UGC」被同日 `content_submit` / `match_ugc_online` 覆盖。口径见 [CD-21](../20-gameplay/21-traprush.md) 文首、[CD-61 §2 M5](../60-plan/61-milestones.md) 与 [M5 章节计划](../../docs/plans/m5-traprush-vertical-slice.md) §5.2。
- `official_course_05 = dual_route_energy_wall`（2026-09-11 人类拍板）：M5 §5.2 问题 2 的 **05 半边**。`course_05` = 双路线竞速：安全长路绕行，短路须站能量墙 −Z 侧 Q 打碎（UseItem reach 只探 +Z）。匹配 HTTP 白名单纳入 `course_05`，官方课数量落到 CD-61 的 5 张上限。`--route=safe` 仍只对 `course_01`。`course_f_playable` 仍不是匹配课。该句中「C3 密钥去向；C4 匹配 HTTP 扩到已签名 UGC」被同日 `content_submit` / `match_ugc_online` 覆盖。口径见 [CD-21](../20-gameplay/21-traprush.md) 文首、[CD-61 §2 M5](../60-plan/61-milestones.md) 与 [M5 章节计划](../../docs/plans/m5-traprush-vertical-slice.md) §4 C2 / §5.2。

## D.4 BASTION

- `bastion_player_count = one_and_two`：官方先 1v1，再开放 2v2。
- `bastion_team_building = personal_sector_gold`。
- `bastion_obstacle_draft = simultaneous_hidden`。
- `bastion_obstacle_ownership = captain_places`。
- `bastion_captain_selection = volunteer_vote_fallback`。
- `bastion_path_blocking = path_always_required`。
- `bastion_tower_placement = slot_based`。
- `bastion_tower_control = fully_automatic`。
- `bastion_offense_model = shared_waves_score`。
- `bastion_win_condition = core_first_or_score`。
- `bastion_wave_symmetry = identical_seeded`。
- `bastion_economy = base_plus_kill_capped`。
- `bastion_obstacle_destruction = mixed_by_archetype`。
- `bastion_build_timing = creator_defined`。
- `bastion_build_policy_authoring = whitelisted_policies`。
- `bastion_wave_count = creator_unbounded`。
- `bastion_balance_authoring = bounded_multipliers`。
- `bastion_asymmetric_teams = creator_defined_teams`。
- `bastion_max_players = max_eight`。
- `asymmetric_compensation = platform_presets`。
- `bastion_pathfinding = waypoint_graph`。
- `tower_target_priority = player_selects_whitelist`。
- `bastion_offline_opponent = template_bot`。
- `hidden_state_sync = 延期`。

## D.5 热修改、Preview 与内容版本

- `ugc_version_activation = all_next_safe_tick`，后由补丁等级决策限定。
- `live_match_patch_level = p0_p1_only`：P2/P3 只用于新对局。
- `live_p1_safe_point = phase_boundary_notify`。
- `live_patch_rollout = all_immediately`。
- `live_patch_rollback = technical_auto_rollback`。
- `edit_play_workflow = separate_preview_window`。该句被 `authoring_preview = persistent_safe_tick_patch`（2026-08-23）**收窄解释、并未推翻**：产品仍是独立窗口/标签，本刀先锁独立 `AuthoringPreview` 会话。P0–P2 在安全点增量应用已有 EDIT `op`，失败整份回滚；P3 待 Rule VM；P4 须重新连接。Preview 永不结算或在线写。窗口、多人试玩与 SimulationBundle 仍待。该句中「SimulationBundle 仍待」被 `simulation_bundle = traprush_topology_json_v1`（2026-08-24）覆盖：v1 拓扑 JSON 已落地；签名二进制包仍待。该句中「Preview 试玩接线仍待」被 `preview_play = traprush_topology_session`（2026-08-24）覆盖：安全点试玩已落地；键盘意图、重力、检查点占用验收与多人试玩仍待。该句中「键盘意图」被 `preview_play_move = wasd_move_intent`（2026-08-24）覆盖：开玩后只接受已有 MoveIntent；窗口可见时 WASD 按世界方向编码。不锁产品速度或 Tick Hz。重力、检查点占用验收与多人试玩仍待。口径见 [CD-32 §3](../30-ugc/32-editor-and-preview.md#3-从编辑到预览) 与 [CD-42 §3.4](../40-technical/42-contracts-and-rulevm.md#34-实现落点)。该句中「检查点占用验收」被 `preview_play_pads = occupancy_accept`（2026-08-24）覆盖：开玩后用已有 PadAccept，胶囊与垫相交才推进有序进度。占用由 Preview 观察，不是完成断言。不接传送落地、冲线、重力、Jump/Shove、多人试玩。口径见 [CD-32 §3](../30-ugc/32-editor-and-preview.md#3-从编辑到预览) 与 [CD-42 §3.4](../40-technical/42-contracts-and-rulevm.md#34-实现落点)。该句中「不接传送落地」被 `preview_play_portals = occupancy_land_exit`（2026-08-24）覆盖：开玩后用已有 `TraprushPortalLanding.try_land_exit`，胶囊与传送源点占用盒相交才单跳落地。传送袋含源点 `x/y/z`；加载为非固体源点盒。`two_way` dest 体积门闩直到离开。出口占用则等待且本帧不位移。传送不代验收检查点。状态 `floor=n`。不接冲线、重力、Jump/Shove、多人试玩。口径见 [CD-32 §3](../30-ugc/32-editor-and-preview.md#3-从编辑到预览) 与 [CD-42 §3.4](../40-technical/42-contracts-and-rulevm.md#34-实现落点)。该句中「不接冲线」被 `preview_play_finish = occupancy_cross`（2026-08-24）覆盖：开玩后用已有 `TraprushFinishAccept.try_cross`，全部强制检查点完成后胶囊与终点占用盒相交才记 `finish_tick`。占用由 Preview 观察，不是客户端冲线断言。无 FinishIntent。`zone.tags` 含 `finish` 编进 bundle `finish`（0 或 1）。不结算、不在线写。状态 `finish=n`（未冲线为 -1）。不接重力、Jump/Shove、多人试玩。该句中 Preview 试玩重置仍待被 `preview_play_reset = checkpoint_spawn_intent`（2026-08-24）覆盖：开玩后接受已有 `ResetToCheckpointIntent`，经 `TraprushCheckpointSpawn` 与已编译垫的 `respawn_dx/dy/dz` 回到最近已验收落点；尚未验收则回起点偏移。不读客户端坐标。不回退进度，不清除已记下的 `finish_tick`。传送门闩在重置后清掉。窗口可见时 Reset 按钮与 R 上升沿是表现桩，不锁长按时长或复活硬直。不接 Jump/Shove、不锁重力、不接多人试玩。该句中「不接道具」被 `preview_play_use_item = occupancy_break`（2026-08-24）覆盖：开玩后接受已有 `UseItemIntent`，经 `TraprushDestructibleBreak` 在调用方 reach 姿态与编译出的固体箱盒相交时才扣耐久。伤害与 reach 是表现桩，不锁爆破表。摧毁后箱盒非固体。不读客户端命中或摧毁断言。Interact / Jump / Shove 仍拒绝。不锁重力、不接道具栏或重生、不接多人试玩。口径见 [CD-32 §3](../30-ugc/32-editor-and-preview.md#3-从编辑到预览) 与 [CD-42 §3.4](../40-technical/42-contracts-and-rulevm.md#34-实现落点)。该句中「Jump 仍拒绝」被 `preview_play_jump = grounded_intent_stepper`（2026-08-24）覆盖：开玩后接受已有 `JumpIntent`，经 `TraprushIntentStepper` 接地检查（`is_supported_by_solid`，调用方 `play_support_dy`）后按调用方 `play_jump_dy` 上移直到阻挡；未接地不位移仍回 ok。`play_jump_dy` / `play_support_dy` 是表现桩，不锁跳跃高度、重力或 coyote。不读客户端高度字段。窗口可见时 Jump 按钮与 `jump` 动作上升沿采样，隐藏不采样。Shove / Interact 仍拒绝。不接重力或下落、不接多人试玩。口径见 [CD-32 §3](../30-ugc/32-editor-and-preview.md#3-从编辑到预览) 与 [CD-42 §3.4](../40-technical/42-contracts-and-rulevm.md#34-实现落点)。覆盖 `authoring_lattice` / `authoring_reachability` 中「Preview 仍待」。口径见 [CD-32 §4](../30-ugc/32-editor-and-preview.md#4-preview-行为)。该句中「窗口仍待」被 `authoring_preview_window = independent_godot_window`（2026-08-23）覆盖：`AuthoringPreviewShell` 用代码创建独立 `Window`，关闭只隐藏，编辑会话保持打开。浏览器 `tab` 名保留但本刀拒绝打开。不编 SimulationBundle、不映射 3D。多人试玩仍待。口径见 [CD-32 §4](../30-ugc/32-editor-and-preview.md#4-preview-行为)。该句中独立窗默认最大化盖住编辑被 `authoring_preview_panes = windowed_side_by_side`（2026-09-03）覆盖：Editor / Preview 为 `MODE_WINDOWED` 左右并排嵌入子窗，大厅仍最大化；`gui_embed_subwindows` 仍为 true，彻底拆壳未做。该句中按开发机 1600 写死导致重叠被 `authoring_preview_panes = viewport_half_lock`（2026-09-03）覆盖：按主视口对半锁死；Editor 地板网格与点选拖拽走已有 `set_component`，不新增 op。触控不做。口径见 [CD-32 §4](../30-ugc/32-editor-and-preview.md#4-preview-行为) 与 [CD-11 §8.2](../10-product/11-scope-and-platforms.md)。该句中「不映射 3D」被 `authoring_preview_map = transform_box_placeholders`（2026-08-23）覆盖：`AuthoringPreviewMap` 把带 `transform` 的 Preview 实体画成 1×1×1 米占位盒；浮点只在 creator 表现边界；每次 Patch（含失败回滚）按 Preview 世界重建。占位盒不是碰撞体。连线可视化、检查点可视化、多人试玩与 SimulationBundle 仍待。口径见 [CD-32 §3](../30-ugc/32-editor-and-preview.md#3-从编辑到预览)。该句中「连线可视化仍待」被 `authoring_preview_portal_gizmos = classified_link_stubs`（2026-08-23）覆盖：Preview 按 `portal_links()` 画 `two_way` / `one_way` 线段与 `dangling` 源点标；悬空不画到原点。检查点可视化、可达性叠加、多人试玩与 SimulationBundle 仍待。口径见 [CD-32 §3](../30-ugc/32-editor-and-preview.md#3-从编辑到预览)。该句中「检查点可视化仍待」被 `authoring_preview_checkpoint_gizmos = unique_order_sequence_stubs`（2026-08-23）覆盖：Preview 给带 `transform` 的 `checkpoint` 标 `order`；唯一 order 升序连线；重复 order 只打标。可达性叠加、多人试玩与 SimulationBundle 仍待。口径见 [CD-32 §3](../30-ugc/32-editor-and-preview.md#3-从编辑到预览)。该句中「可达性叠加仍待」被 `authoring_preview_reachability_overlay = issue_code_stubs`（2026-08-23）覆盖：Preview rebuild 只读 `evaluate_reachability`，把问题码标到有 `transform` 的实体；`unreachable_checkpoint` 另画断段；`missing_mandatory_path` 只进状态。叠加不是写入门禁。走路可达、预算数字、多人试玩与 SimulationBundle 仍待。口径见 [CD-32 §3](../30-ugc/32-editor-and-preview.md#3-从编辑到预览)。
- `preview_patch_mode = persistent_incremental`。
- `multiplayer_preview_editing = author_live_patch`。

## D.6 AI、协作与项目治理

- `ai_autonomy = edit_test_no_commit_release` 已被 `ai_autonomy_scope = isolated_branch_commit_ok`（2026-08-21）**收窄解释、并未推翻**：禁止的是向 `main` 或受保护分支提交/推送，以及部署、发布与线上回滚；允许在隔离的 agent 分支或 worktree 上 commit/push。人类门禁落在 PR 合并（`pr_merge_gate`）与 GitHub 分支保护。**分支保护落地前仍按原字面执行**——Agent 不得创建任何提交。口径见 [CD-52 §1.1](../50-engineering/52-ai-workflow.md)，来源 [ADR-0004](../../docs/adr/0004-multi-agent-adoption-timing-and-architecture.md)。该句再被 `ai_autonomy_scope = main_after_human_auth`（2026-09-09）覆盖：默认在当前 checkout（通常 `main`）上完成一章，人类验证并授权后 commit / push 到 `main`；未经本回合确认仍不得提交。`isolated_branch_commit_ok` 只保留为「人类要求开 PR / Cloud Agent 隔离分支」的例外。宪法第十八条不变。
- `human_review_granularity = task_and_gate`。该句被 `pr_scope = complete_chapter`（2026-08-23）**收窄解释、并未推翻**：任务仍是可验证切片，但提交给人类的 PR 必须是完整一章（契约 + 实现 + 测试 + 所有者文档），不得把同一链路拆成不能独立验收的半成品。`git_workflow = trunk_short_pr` 不被推翻，仍禁止整里程碑巨型 PR。口径见 [CD-52 §3.1](../50-engineering/52-ai-workflow.md)。该句再被 `chapter_device_check = numbered_runbook_and_pr`（2026-08-25）补充：完整章节 PR 必须把人类窗口验收写成编号步骤，写入 [开发机窗口验收](../../docs/runbooks/dev-window-check.md) 本刀并在 PR Test plan 给出同一份清单；禁止只写「真机再看一眼」。无开发机可见行为的章写「无」加原因。这是人工检查，不是 CI 门禁。口径见 [CD-52 §3.2](../50-engineering/52-ai-workflow.md)。该句再被 `dev_window_check_not_export = runbook_rename`（2026-09-02）补充：文件从 `chapter-device-check.md` 更名为 `dev-window-check.md`，显示名「开发机窗口验收」；「真机」一词留给导出包（[desktop-export-check.md](../../docs/runbooks/desktop-export-check.md)）。不覆盖编号步骤义务本身。
- `ai_parallelism = lead_isolated_domains`。
- `multi_agent_runtime = cursor_native_worktree`（2026-08-21）：一期用 Cursor 原生 `/worktree` + `.cursor/agents/` + `.cursor/hooks.json`。不引入 Multica 类平台，不用 Cursor Automations。启用判据、并行度上限与角色表见 [CD-52 §5](../50-engineering/52-ai-workflow.md)。
- `code_review_assist = bugbot`（2026-08-21）：审查角色不建 Cursor subagent，由 Bugbot 承担。本地 `/review-bugbot` 先行，第一个 PR 走通后再开 PR 侧。不启用 fail-on-unresolved-issues，故不是 CI 门禁。配置与边界见 [CD-52 §5.2](../50-engineering/52-ai-workflow.md)，状态见 [CD-53 §4.1](../50-engineering/53-testing-and-ci.md)。该句被 `bugbot_agents_md = local_injected_pr_rules_exclude`（2026-08-21）补充：本地 `/review-bugbot` 子会话注入了完整 `AGENTS.md`（`always_applied_workspace_rules`）。GitHub PR Bugbot 官方规则源不含 `AGENTS.md`。该句再被 `bugbot_md = gate2_and_cd00_links`（2026-08-21）覆盖：`.cursor/BUGBOT.md` 已入库。PR 侧仍未开。该句再被 `bugbot_pr_side = deferred_ci_human`（2026-08-21）覆盖：GitHub PR 侧 Bugbot 因 SCM 安装对不上而跳过；合入靠 CI + 人类批准。本地 `/review-bugbot` 仍可用。
- `test_first_scope = core_tdd_presentation_after`。
- `ai_asset_policy = auto_publish_if_technical`。
- `ai_asset_sources = no_traceability`。
- `ai_asset_takedown = manual_case_review`。
- `repository_hosting = github_private`。
- `git_workflow = trunk_short_pr`。该句被 `git_workflow = trunk_direct_after_auth`（2026-09-09）覆盖：默认提交 `main`；只有人类主动说「开 PR」才拉分支开 PR。大改动先做完、人类验证、授权后才提交。`pr_scope = complete_chapter` 与章粒度 5× 仍有效，对象从「PR」改成「提交」。shell-guard 不再拦向 `main` 的普通 commit/push，仍拦 force push、删除 `main`、`worktree remove --force`、以及把 Godot AI 本机条目写进 `project.godot`。该句再被 `branch_protection = trunk_direct_no_required_pr`（2026-09-09）覆盖：GitHub `main` 保护已改为允许普通直推、不要求 PR；仍禁 force push / 删除 `main`。必过 status check 不再作为 push 前置。CI 仍在推送后跑。Agent 不改 GitHub。该句再被 `submit_includes_push = unless_human_says_no`（2026-09-09）覆盖：人类说「提交」= `git commit` 并 `git push`；只有明确说不要 push 才只落本地。口径见 [CD-52 §1.1](../50-engineering/52-ai-workflow.md) 与 [CD-53 §4.5](../50-engineering/53-testing-and-ci.md)。
- `ci_environment = github_plus_selfhosted`。
- `pr_merge_gate = required_ci_one_human`。
- `web_preview_frequency = per_pr_preview`。
- `preview_access = public_preview`。
- `preview_backend_isolation = ephemeral_sandbox`。
- `project_tracking = github_projects_adrs`。
- `engineering_language = chinese_docs_english_code`。
- `commit_convention = conventional_commits`。
- `dependency_updates = locked_scheduled`。
- `roadmap_style = milestone_no_dates`。
- `human_team_size = solo_owner`。
- `project_scaffold = minimal_custom`。
- `repo_structure = monorepo`。
- `asset_source_control = lfs_sources_and_runtime`。
- `godot_mcp_choice = 延期专项调研` 已被 `godot_mcp_choice = defer_to_m2`（2026-08-20）覆盖：专项调研完成后曾采纳 [ADR-0003](../../docs/adr/0003-godot-mcp-selection.md) 选项 A。该值再被 `godot_mcp_choice = godot_ai_v3_1_5`（2026-08-21）覆盖：M1 启动前第二轮调研选定 **Godot AI**（MIT，GDScript 插件 + Python/uv，~43 工具 / 120+ 操作）为唯一主 MCP。匿名遥测强制关；插件不入库；接入烟测与 M1 仿真拆开，M2 启动前生产级启用。该句再被 `godot_mcp_stage_c = production_enabled`（2026-08-23）覆盖：[ADR-0003](../../docs/adr/0003-godot-mcp-selection.md) 阶段 C 通过，不重选 MCP。依据是 Windows / macOS 接入烟测签字、遥测关闭核实、已提交 `project.godot` 无 `godot_ai` / `_mcp_game_helper`。大型 `.tscn` 默认走 MCP / UndoRedo，见 [CD-52 §7.1](../50-engineering/52-ai-workflow.md)。版本与开关见 [CD-51 §7](../50-engineering/51-dev-environment.md)。该句中「无 `godot_ai`」指已提交 `editor_plugins`；被 `godot_ai_editor_enable = auto_if_installed`（2026-08-26）覆盖解释：打开编辑器时 Authoring Editor 若发现本机 gitignore 的 `game/addons/godot_ai/` 则自动启用，CI 缺目录不得把该项写入已提交列表。
- `godot_version_lock = 4_7_2_stable`（2026-08-20）：精确锁定 Godot 4.7.2-stable Standard，覆盖原先只写到次版本的 "Godot 4.7 stable"。4.7-stable 首发于 2026-06-18，4.7.2 为官方推荐的兼容维护版；两台开发机与 CI 必须使用同一精确版本与配套导出模板。
- `human_playtest_cadence = internal_only`。该句被 `traprush_first_play_look = color_blocks_playable_awaiting_art`（2026-08-28）补充：纠偏 D10 非正式试玩已做。色块阶段无法定义「好不好玩」；人类 + 熟人简单玩后，脑补未来美术认为可玩、值得期待。不是 E6 最终签署，也不视为已验证创作者体验。口径见 [纠偏方案 D10](../../docs/plans/course-correction-2026-08.md)。该句中「不是 E6 最终签署」被 `e6_playability = fun_after_first_art_2026_09_02`（2026-09-02）覆盖：C4 第一批 `.glb` 入库后，人类给出首次可玩性结论 = **好玩**。不是外部测试，也不视为已验证创作者体验。**不解除冻结令**。口径见 [纠偏方案 E6](../../docs/plans/course-correction-2026-08.md) 与 [CD-61](../60-plan/61-milestones.md)。
- `internal_acceptance = owner_signoff_checklist`。
- `milestone_m0 = exited`（2026-08-20）：[CD-61](../60-plan/61-milestones.md) M0 验收通过并退出。依据是 Linux CI 跑绿、人类按 [环境烟测清单](../../docs/runbooks/environment-smoke-test.md) 复跑全绿，以及编辑器 GUI 独立确认 Compatibility 与 17 个输入动作。下一阶段为 M1，**尚未启动**。该句被 `milestone_m1 = phase_a_started`（2026-08-21）覆盖：M1 阶段 A 启动；L0 信封（ID / 命令 / 事件 / 状态哈希）开工；定点运算仍待 [ADR-0005](../../docs/adr/0005-fixed-point-numeric-model.md) 拍板。Windows Godot AI 接入烟测第 7 步剩余两项（Vision Routing 关、无第二套 Godot MCP）同日由人类确认。该句再被 `milestone_m1 = phase_a_a1_a4`（2026-08-21）覆盖：定点已拍板并合入；[CD-52 §5.1](../50-engineering/52-ai-workflow.md) 的 A1 与 A4 成立（[PR #1](https://github.com/czmomocha/craftarena/pull/1)）；A2 / A3 未成立，并行仍禁止。该句再被 `milestone_m1 = exited`（2026-08-23）覆盖：[CD-61](../60-plan/61-milestones.md) M1 验收通过并退出（[PR #76](https://github.com/czmomocha/craftarena/pull/76)）。依据是 GUT 402/402（[PR #75](https://github.com/czmomocha/craftarena/pull/75) 合入时）、`TraprushGrayboxAcceptance` 覆盖单人上下/侧向传送与障碍破坏、本节产出清单均已合入。不纳入 §4.1 的 2p Headless 与单局名次（M3）、Component Schema v1 / OpenAPI、推击冲量数值。下一阶段为 M2，**尚未启动**；须先完成 [ADR-0004](../../docs/adr/0004-multi-agent-adoption-timing-and-architecture.md) 决策 8 工具链评审。该句再被 `m2_toolchain_review = mcp_pass_keep_two_domains`（2026-08-23）覆盖：工具链评审通过（[PR #77](https://github.com/czmomocha/craftarena/pull/77)）。MCP 阶段 C 成立；并行保持 2 域，第 3 域仍未开；M2 功能任务尚未开工，第一刀仍须串行锁 Component Schema v1。该句再被 `m2_component_schema = v1_entity_bag`（2026-08-23）覆盖：M2 第一刀落地 Component Schema v1（[CD-42 §1.2](../40-technical/42-contracts-and-rulevm.md#12-字段标识符v1)）。合入后才按域并行 AuthoringWorld / EditCommand。第 3 域未开。
- `pr_loop_a4 = first_pr_merged`（2026-08-21）：走通「Agent 产出 → PR → CI 全绿 → 人类 review → 人类合并」。证据：[PR #1](https://github.com/czmomocha/craftarena/pull/1)。不因此开启多域并行。
- `redline_scan = constitution_subset`（2026-08-21）：[ADR-0004](../../docs/adr/0004-multi-agent-adoption-timing-and-architecture.md) §4.2 门禁 2 落入 `tools/redline-scanner/` 与 CI step `npm run redline-scan`。覆盖第五条 SceneTree/`float`、第七条 `.gdextension` 与 `.cs`、第十一条 Godot 3 高信号子集。不因此把 A3 报全绿。
- `codeowners_file = four_contract_paths`（2026-08-21）：`.github/CODEOWNERS` 覆盖 `game/src/shared/`、`backend/contracts/`、`Confirmed-docs/`、`.github/`，所有者 `@czmomocha`。GitHub `require_code_owner_reviews` 仍关，不把 A2 报全绿。该句被 `branch_protection = pr_ci_codeowners`（2026-08-21）覆盖：人类已保存 `main` 保护；API 确认 code owner 审查、两个 CI status check，以及要求与 `main` 同步。A2 成立。`enforce_admins` 仍关。A3 的 worktree 仍缺，并行仍禁止。该句再被 `worktree_setup = cursor_json_and_loadenv`（2026-08-21）覆盖：`.cursor/worktrees.json` + DevLauncher `loadEnvFile` 落地；A1–A4 成立。阶段 B 未启动，不开启多域并行。hooks 仍缺。该句再被 `shell_guard_hook = fail_closed_git`（2026-08-21）覆盖：`.cursor/hooks.json` `beforeShellExecution` + `failClosed: true`；判定逻辑在 `tools/shell-guard/`。拦向 `main` 的提交/推送与 `git worktree remove --force`。阶段 B 仍未启动。该句再被 `bugbot_agents_md = local_injected_pr_rules_exclude`（2026-08-21）覆盖：本地 `/review-bugbot` 已跑且注入了 `AGENTS.md`；PR 侧官方规则源仍不含 `AGENTS.md`。`.cursor/BUGBOT.md` 仍未建，PR 侧 Bugbot 仍未开，不开启多域并行。该句再被 `cursor_agents = six_roles_no_reviewer`（2026-08-21）覆盖：`.cursor/agents/` 六份角色定义入库，无审查 Agent。仍不开启多域并行。该句再被 `bugbot_md = gate2_and_cd00_links`（2026-08-21）覆盖：`.cursor/BUGBOT.md` 入库，只写门禁 2 机械红线并链接 CD-00 / `AGENTS.md`。该句再被 `bugbot_pr_side = deferred_ci_human`（2026-08-21）覆盖：跳过 GitHub PR 侧 Bugbot；合入靠 CI + 人类批准。多域并行不再等 PR Bugbot。该句再被 `two_domain_round1 = sim_world_and_traprush_gates`（2026-08-21）覆盖：待办 14 第一轮已合入（[PR #13](https://github.com/czmomocha/craftarena/pull/13) 检查点/传送、[PR #14](https://github.com/czmomocha/craftarena/pull/14) SimulationWorld 骨架）；人类确认审查节奏可继续两域。第 3 域未开。该句再被 `two_domain_round2 = xz_block_and_move_intents`（2026-08-21）覆盖：待办 14 第二轮已合入（[PR #16](https://github.com/czmomocha/craftarena/pull/16) XZ 目的地阻挡、[PR #17](https://github.com/czmomocha/craftarena/pull/17) MoveIntent/重置落点/推击冷却）；第 3 域未开。该句再被 `two_domain_round3 = y_move_and_jump_shove`（2026-08-21）覆盖：待办 14 第三轮已合入（[PR #19](https://github.com/czmomocha/craftarena/pull/19) Jump/Shove 解码、[PR #20](https://github.com/czmomocha/craftarena/pull/20) Y 轴目的地阻挡）；第 3 域未开。该句再被 `two_domain_round4 = intent_step_and_static_box`（2026-08-22）覆盖：待办 14 第四轮已合入（[PR #22](https://github.com/czmomocha/craftarena/pull/22) 意图驱动仿真、[PR #23](https://github.com/czmomocha/craftarena/pull/23) 静态盒阻挡）；第 3 域未开。该句再被 `two_domain_round5 = destructible_and_sweep`（2026-08-22）覆盖：待办 14 第五轮已合入（[PR #25](https://github.com/czmomocha/craftarena/pull/25) 可破坏耐久、[PR #26](https://github.com/czmomocha/craftarena/pull/26) 位移扫掠）；第 3 域未开。该句再被 `two_domain_round6 = shove_apply_and_try_set_pose`（2026-08-22）覆盖：待办 14 第六轮已合入（[PR #28](https://github.com/czmomocha/craftarena/pull/28) 推击应用、[PR #29](https://github.com/czmomocha/craftarena/pull/29) 占用感知落地）；第 3 域未开。该句再被 `two_domain_round7 = portal_land_and_box_solid`（2026-08-22）覆盖：待办 14 第七轮已合入（[PR #31](https://github.com/czmomocha/craftarena/pull/31) 传送落地等待、[PR #32](https://github.com/czmomocha/craftarena/pull/32) 关闭静态盒阻挡）；第 3 域未开。该句再被 `two_domain_round8 = box_overlap_and_graybox`（2026-08-22）覆盖：待办 14 第八轮已合入（[PR #34](https://github.com/czmomocha/craftarena/pull/34) 静态盒重叠查询、[PR #35](https://github.com/czmomocha/craftarena/pull/35) 单人灰盒跑道夹具）；第 3 域未开。该句再被 `two_domain_round9 = replay_tape_and_pad_accept`（2026-08-22）覆盖：待办 14 第九轮已合入（[PR #37](https://github.com/czmomocha/craftarena/pull/37) 命令回放带、[PR #38](https://github.com/czmomocha/craftarena/pull/38) 占用检查点垫）；第 3 域未开。该句再被 `two_domain_round10 = snapshot_ring_and_graybox_pads`（2026-08-22）覆盖：待办 14 第十轮已合入（[PR #40](https://github.com/czmomocha/craftarena/pull/40) 周期关键快照环、[PR #41](https://github.com/czmomocha/craftarena/pull/41) 灰盒垫盒接线）；第 3 域未开。该句再被 `two_domain_chapter1 = occupancy_queries_and_graybox_replay`（2026-08-22）覆盖：细轮收尾 [PR #43](https://github.com/czmomocha/craftarena/pull/43) 重叠盒枚举、[PR #44](https://github.com/czmomocha/craftarena/pull/44) 灰盒命令磁带与首章 [PR #45](https://github.com/czmomocha/craftarena/pull/45) 胶囊占用查询、[PR #46](https://github.com/czmomocha/craftarena/pull/46) 灰盒快照与周期 hazard 已合入；阶段 B 改为章节节奏（每域更大功能 PR，文档章末补一次）；第 3 域未开。该句再被 `two_domain_chapter2 = pose_overlap_and_interact`（2026-08-22）覆盖：第二章已合入（[PR #48](https://github.com/czmomocha/craftarena/pull/48) 候选姿态占用查询、[PR #49](https://github.com/czmomocha/craftarena/pull/49) InteractIntent 灰盒伤害）；第 3 域未开。该句再被 `two_domain_chapter3 = solid_occupancy_and_use_item`（2026-08-22）覆盖：第三章已合入（[PR #51](https://github.com/czmomocha/craftarena/pull/51) UseItemIntent 爆破 stub、[PR #52](https://github.com/czmomocha/craftarena/pull/52) 仅 solid 占用查询）；第 3 域未开。该句再被 `two_domain_chapter4 = support_probe_and_finish_pad`（2026-08-22）覆盖：第四章已合入（[PR #54](https://github.com/czmomocha/craftarena/pull/54) 固体支撑探测、[PR #55](https://github.com/czmomocha/craftarena/pull/55) 灰盒终点垫）；第 3 域未开。该句再被 `two_domain_chapter5 = y_contact_and_grounded_jump`（2026-08-22）覆盖：第五章已合入（[PR #57](https://github.com/czmomocha/craftarena/pull/57) Y 轴直到阻挡、[PR #58](https://github.com/czmomocha/craftarena/pull/58) 接地跳跃）；第 3 域未开。该句再被 `two_domain_chapter6 = xz_contact_and_apply_fall`（2026-08-22）覆盖：第六章已合入（[PR #60](https://github.com/czmomocha/craftarena/pull/60) 调用方下落、[PR #61](https://github.com/czmomocha/craftarena/pull/61) XZ 直到阻挡）；第 3 域未开。该句再被 `two_domain_chapter7 = range_query_and_commit_fall`（2026-08-22）覆盖：第七章已合入（[PR #63](https://github.com/czmomocha/craftarena/pull/63) tick 内下落、[PR #64](https://github.com/czmomocha/craftarena/pull/64) 掉出范围查询）；第 3 域未开。该句再被 `two_domain_chapter8 = volume_block_and_move_contact`（2026-08-22）覆盖：[PR #66](https://github.com/czmomocha/craftarena/pull/66) 无 id 体积占用、[PR #67](https://github.com/czmomocha/craftarena/pull/67) MoveIntent 水平接触已合入；第 3 域未开。该句再被 `two_domain_arc_a = jump_shove_contact_and_range_reset`（2026-08-22）覆盖：阶段 B 改为按弧开 PR；弧 A 为 Jump/Shove 直到阻挡与掉出范围复位（[PR #68](https://github.com/czmomocha/craftarena/pull/68)）；无独立仿真切片时允许单域；第 3 域未开。该句再被 `two_domain_arc_b = graybox_full_run_replay`（2026-08-22）覆盖：弧 B 为单人灰盒整段可回放（[PR #69](https://github.com/czmomocha/craftarena/pull/69)）；无独立仿真切片，单域；第 3 域未开。该句再被 `two_domain_arc_c = graybox_tape_replay`（2026-08-22）覆盖：弧 C 为 PLAYER 磁带回放进灰盒（[PR #70](https://github.com/czmomocha/craftarena/pull/70)）；无独立仿真切片，单域；第 3 域未开。该句再被 `two_domain_arc_d = graybox_system_occupancy_log`（2026-08-22）覆盖：弧 D 为 SYSTEM 占用日志进灰盒（[PR #71](https://github.com/czmomocha/craftarena/pull/71)）；无独立仿真切片，单域；第 3 域未开。该句再被 `two_domain_arc_e = graybox_reset_break_log`（2026-08-22）覆盖：弧 E 为出界复位与 try_break_crate 入 SYSTEM 带（[PR #72](https://github.com/czmomocha/craftarena/pull/72)）；无独立仿真切片，单域；第 3 域未开。该句再被 `two_domain_arc_f = graybox_commit_tick_log`（2026-08-22）覆盖：弧 F 为 try_commit_tick 入 SYSTEM 带（[PR #73](https://github.com/czmomocha/craftarena/pull/73)）；无独立仿真切片，单域；第 3 域未开。该句再被 `two_domain_arc_g = graybox_apply_fall_log`（2026-08-23）覆盖：弧 G 为独立 try_apply_fall 入 SYSTEM 带（[PR #74](https://github.com/czmomocha/craftarena/pull/74)）；try_commit_tick 内部下落不另记 apply_fall；无独立仿真切片，单域；第 3 域未开。该句再被 `two_domain_arc_h = graybox_shove`（2026-08-23）覆盖：弧 H 为灰盒基础推击入 PLAYER 带（[PR #75](https://github.com/czmomocha/craftarena/pull/75)）；layout 可选第二胶囊；dx/dz/冷却由调用方传入，不锁定 CD-63；TapeReplay 可回放；Acceptance 仍不含推击；无独立仿真切片，单域；第 3 域未开。该句再被 `two_domain_m1 = graybox_exited`（2026-08-23）覆盖：M1 灰盒切片退出（[PR #76](https://github.com/czmomocha/craftarena/pull/76)）；不再按弧开仿真/灰盒 PR；第 3 域未开；M2 未启动。该句再被 `m2_gate = toolchain_review_passed`（2026-08-23）覆盖：与 `m2_toolchain_review = mcp_pass_keep_two_domains` 同一裁定（[PR #77](https://github.com/czmomocha/craftarena/pull/77)），见 [ADR-0004 §8.1](../../docs/adr/0004-multi-agent-adoption-timing-and-architecture.md)。该句再被 `m2_first_cut = component_schema_v1`（2026-08-23）覆盖：第一刀 Component Schema v1 已落入 [CD-42 §1.2](../40-technical/42-contracts-and-rulevm.md#12-字段标识符v1)；合入后才按域并行 AuthoringWorld / EditCommand。
- `ci_green_claim = job_conclusion_success`（2026-08-28）：PR 正文写「CI 绿」之前必须核对每个 job 的最终 `conclusion` 是 `success`；`cancelled` / `skipped` / `neutral` 都不算，合并当刻 `gh pr checks` 的 `pending` 也不算。来源是 [2026-08-28 门禁失守记录](../../docs/audits/2026-08-28-ci-gate-timeout.md)：`godot` job 因单个 GUT 用例把胶囊半径写成 Q48.16 的 `1`（真实值 `Fixed.SCALE / 8`）而使竖直扫掠取样 65536 次，从 PR #176 起连续在 15 分钟 `timeout-minutes` 处被终止，五个 PR 带着从未跑完的 Godot 门禁合入 `main`。配套：GUT step 自己掐 10 分钟预算使超时变红而非灰、新增最慢测试脚本耗时打印。`SimulationWorld._sweep_step_count` 无取样上限（代价正比于 `1/radius`，属宪法第十七条缺口）不在该章解决，另立深审章。口径见 [CD-52 §3.3](../50-engineering/52-ai-workflow.md)。
- `macos_second_machine_smoke = verified`（2026-08-20）：覆盖原先「macOS 命令列尚未在真机验证」。人类按 CD-51 §4.1 在 macOS 26.5.2 arm64 跑通十步与编辑器 GUI；固定命令见 [README.md](../../README.md)，签字记录见烟测清单第 10 步。顺带确认 `--check-only` 在 macOS 上类型错误仍打印但退出码为 0，与 Windows 的退出码 1 不同。

## D.7 后端、数据与部署

- `match_server_runtime = godot_headless_shared`。
- `control_plane_stack = typescript_fastify`。
- `primary_database = postgresql` 已被后续 `postgres_deployment = sqlite_control_plane` 覆盖：一期 SQLite，PostgreSQL 为迁移目标。
- `sqlite_ownership = control_plane_only`。
- `primary_region = hong_kong`；`cloud_provider = tencent_cloud_hk`，其中托管 PostgreSQL 部分被 SQLite 决策覆盖。
- `phase1_ccu = ccu_50`；`capacity_overflow = fifo_queue`。
- `match_process_model = one_process_per_match`。
- `match_orchestration = match_host_supervisor`。
- `realtime_gateway = single_gateway_proxy`。
- `gateway_service_boundary = separate_ts_service`。
- `deployment_environments = local_staging_only`：本地 + 长期测试环境，无正式 Production。
- `monthly_infra_budget`：具体月度上限未锁定；已知已有腾讯云香港服务器和域名，一期无邮件服务。
- `phase1_auth = email_magic_link` 已被 `auth_without_email = username_password_now` 覆盖。
- `password_recovery = no_recovery`。
- `test_registration = open_registration`。
- `registration_abuse_control = no_protection`。
- `public_player_names = free_username_public`。
- `in_game_communication = none`。
- `secrets_management = plain_env_repo` 后由 `plain_secret_scope = disposable_dev_only` 限定：只允许可销毁沙盒假凭据入库。
- `data_retention = balanced_retention`。
- `telemetry_policy = pseudonymous_optout`。
- `match_history_ui = backend_only`。
- `backup_policy = no_backup`。
- `production_monitoring = logs_only`。

## D.8 网络、仿真与测试

- `network_serialization = json_control_binary_match`。该句中「对局二进制」被 `match_wire_protocol = versioned_binary_v1`（2026-08-24）具体化：命令帧定长 35 字节、快照帧变长，版本字节 + 类型字节，严格拒绝截断/尾随/保留字段非零；Shove/Interact 暂无线上 id。帧布局单一事实源在 [CD-43 §1](../40-technical/43-networking-and-replay.md#1-序列化分工)。
- `realtime_transport = websocket_all_first`。
- `simulation_numeric_model = fixed_point_core`。该值被 `fixed_point_contract = q48_16_trunc_reject_lut4096`（2026-08-21）具体化：[ADR-0005](../../docs/adr/0005-fixed-point-numeric-model.md) 选项 1。空间 `SCALE = 65536`；经济/伤害尺度 1；向零截断；溢出拒绝 + 64×64→128；BAM + 4096 整数 LUT + 整数插值。数字只在 [CD-42 §1.1](../40-technical/42-contracts-and-rulevm.md) 维护。
- `authoritative_collision_shapes = primitive_compound`。
- `gdscript_typing = strict_core_typed_ui_flexible`。
- `rule_vm_execution = versioned_bytecode_interpreter`。该句被 `rule_vm_v1 = envelope_whitelist_gas`（2026-09-09）落实：M4a 首章落地 `ruleset_version=1` 小端信封 + 白名单 `HALT` / `NOP` / `LOAD_I64` / `GET_VAR` / `COMPARE` / `SET_VAR` + 单图 / tick / chain gas。未知 opcode / 版本 / 保留字段拒绝。超 gas 中止该次解释、不提交槽位。未挂 Preview / 对局 / 规则图编译。P3 仍拒绝。落点 `game/src/ugc/rule_vm.gd`。口径见 [CD-42 §2](../40-technical/42-contracts-and-rulevm.md)。该句被 `rule_vm_v1 = typed_graph_compiler`（2026-09-09）补充：第 2 章把 `OnMatchStarted` 图编成同一套 v1 字节码；恰好四键 `ruleset_version` / `graph_gas` / `event` / `nodes`；白名单 `GetVariable` / `Compare` / `SetVariable` / `LoadConst`（字面量，不是 §2.1 产品节点）。多余键、未知事件、未知节点拒绝。`run()` 仍只吃字节码。未挂 Preview / 对局 / Event 分发。P3 仍拒绝。落点 `game/src/ugc/rule_vm_compiler.gd`。口径见 [CD-42 §2](../40-technical/42-contracts-and-rulevm.md)。该句被 `rule_vm_v1 = event_dispatch_start_and_tick`（2026-09-09）补充：第 3 章接受 `OnEveryTicks` 与 `OnMatchStarted`；`RuleVmDispatch` 按事件 bind 已编译字节码，开局一次 / 每 tick 调已有解释器；共享槽位；超 gas 记可定位原因、不拖垮对局。其它 §2.1 事件仍编译拒绝。空默认挂钩在 Preview / 对局；不把规则图写入 AuthoringDocument / SimulationBundle。P3 仍拒绝。落点 `game/src/ugc/rule_vm_dispatch.gd`。口径见 [CD-42 §2](../40-technical/42-contracts-and-rulevm.md)。该句被 `rule_vm_v1 = query_action_logic_subset`（2026-09-09）补充：第 4 章落地 `GetField` / `CountInZone`（按结果数加 gas）/ `Logic` / `Spawn` / `Despawn` / `ApplyEffect` / `EmitGameEvent` 最小白名单，经 `RuleVmHost`；超 gas 回滚 host 写入。不接 SimulationWorld 实体。落点 `game/src/ugc/rule_vm_host.gd` / `rule_vm_apply.gd`。口径见 [CD-42 §2](../40-technical/42-contracts-and-rulevm.md)。该句被 `rule_vm_v1 = preview_p3_recompile_public_forbid`（2026-09-09）补充：第 5 章 Preview 安全点 `try_replace_rule_graphs` 重编译生效；声明 P3 可上提已有 P0–P2 补丁；公开对局同一 API 恒拒绝。P4 仍置重启。规则图仍不入库。M4a 五章收口。口径见 [CD-33 §1](../30-ugc/33-hot-publish.md) 与 [CD-32](../30-ugc/32-editor-and-preview.md)。
- `api_contract = json_schema_openapi`。L0 信封 JSON Schema 已于 2026-08-21 落入 `backend/contracts/schemas/`，由 `tools/content-validator/` 做正反例；未引入 Ajv。该句被 `component_schema = v1_entity_bag`（2026-08-23）补充而非覆盖：Component Schema v1 已落入 [CD-42 §1.2](../40-technical/42-contracts-and-rulevm.md#12-字段标识符v1)、`game/src/shared/schema/` 与 `component_record.schema.json`。inventory 不含槽位语义（[CD-63](../60-plan/63-open-decisions.md) §1.1）。OpenAPI 与 Rule VM 图 JSON Schema 仍未落地，本键不被覆盖。
- `network_targets = region_real_data`。
- `provisional_network_gate = no_gate`。
- `network_correctness_tests = manual_network_tests`。
- `manual_network_cadence = ad_hoc`。
- `performance_regression = optimize_when_bad`。
- `client_anticheat = server_validation_only`。
- `cheat_enforcement = log_only`。
- `bot_fill = no_bot_fill`。
- `match_sim_loop = shared_world_multi_track`（2026-08-24）：M3 首章落地。对局会话把一份编译拓扑装进共享权威 SimulationWorld，1~8 名玩家各自独立 CheckpointTrack / finish_tick / 传送门闩；可破坏箱全员共享。出生偏移与跳跃/支撑/道具数值由调用方传入，不锁产品数值。占用扫描顺序与 Preview 试玩逐字对齐（垫→门→垫→终点）。无网络、无结算、不在线写。名次排序、MatchHost 拉起、快照广播与预测校正仍待。
- `match_server_process = headless_course_session_entry`（2026-08-24）：对局进程入口从 M0 占位升级为真仿真：`--course` 编成 bundle 启动 `TraprushMatchSession`，随引擎 physics tick 推进（非产品 Tick Hz 锁定），心跳 JSON 含状态哈希且不续租，`--max-ticks` 自退、坏配置 exit 1。该句中「不续租」被 `match_lease_renew = valid_input_tick_heartbeat`（2026-08-26）收窄：心跳本身仍不续租；上报 `valid_input_tick`，MatchHost 仅在该值前进时 `renew`。MatchHost 以 `--course`/`--players` 传参（开发期占位，真源是控制面对局配置）。出生偏移/胶囊尺寸/心跳节奏为进程内占位桩。进程内 socket 监听仍待。
- `match_realtime_loop = ws_listen_slot_queue_snapshot`（2026-08-24）：对局进程实时回路落地。`match_server.gd` 在本场端口监听 WebSocket（`--bind` 占位 0.0.0.0，公网暴露由部署层与网关拓扑阻止）；`MatchRealtime` 把连接映射到槽位（按需占用最小空槽、断开释放、满员拒绝），二进制命令帧解码后 FIFO 排队、在 commit_tick 边界按到达顺序应用——命令帧 tick 只解码不信任，服务端 tick 权威（CD-43 §3）。每 2 个引擎 tick 广播一帧二进制快照（占位节奏，非产品快照频率）。快照含全部已配置槽位（含未占用）与可破坏箱耐久。运行入口是 `--scene res://src/server/match_server.tscn`：`-s` 直跑 extends Node 的脚本在 4.7 不会实例化场景（表现为主循环不启动）。防伪造门禁、网关代理与票据对接仍待。
- `gateway_proxy = ticket_verdict_upstream_passthrough`（2026-08-24）：实时网关从 M0 握手骨架升级为真代理。票据裁决（`TicketVerifier.verify`）必须携带上游对局地址——网关不记账、不查库（宪法第二十一条），票据→对局的解析权在控制面。升级后连接与上游一对一绑定，双向原样转发（二进制/文本标志保留），任一侧关闭/出错即关闭另一侧；裁决无上游或上游不可达一律 502。开发期 `DevTicketVerifier` 以 `GATEWAY_DEV_UPSTREAM` 为固定上游（所有非空票据同一场），真票据签发/校验的控制面接口仍待。M0 占位的 `gateway_hello`/`gateway_not_implemented` 退役：通道即上游协议本身。该句中「真票据签发/校验仍待」与「DevTicketVerifier 为默认权威」被 `match_tickets = control_plane_issue_verify_once`（2026-08-25）覆盖：控制面登记对局上游并签发一次性不透明票据（库内只存 SHA-256），`POST /tickets/verify` 消费后返回上游；网关默认 `ControlPlaneTicketVerifier` 只调控制面。`GATEWAY_DEV_UPSTREAM` 降为显式开发旁路。不锁账号绑定、重连补票、MatchHost 自动登记。代理语义本身不被覆盖。该句中「MatchHost 自动登记」被 `match_host_register = control_plane_post_after_launch`（2026-08-25）覆盖：MatchHost 拉起对局后调用控制面 `POST /match-sessions` 登记同一 `matchId` 与 `ws` 上游；登记失败则杀掉子进程，`POST /matches` 回 502。MatchHost 不查库。不等 listen、停止时不注销控制面会话。不锁真匹配/房间码、账号绑定、重连补票。该句中「不等 listen」被 `match_host_listen_wait = tcp_probe_before_register`（2026-08-25）覆盖：MatchHost 拉起后对本场端口做 TCP 探测（连 `127.0.0.1`，与广告主机无关），可连后再 `POST /match-sessions`。超时或进程先退出则杀进程、不登记、`POST /matches` 回 502。超时与轮询间隔是实现默认，不是产品锁定值。停止时仍不注销控制面会话。不锁真匹配/房间码、账号绑定、重连补票。该句中「停止时仍不注销」被 `match_host_unregister = delete_session_on_stop`（2026-08-25）覆盖：MatchHost 在显式停止、租约回收、进程退出或关机后调用控制面 `DELETE /match-sessions/:matchId`；控制面同时删除该场未用票据，同 `matchId` 可再登记。404 视为已注销。注销失败时本地已停，显式 `DELETE /matches/:id` 回 502。不查库。不锁真匹配/房间码、账号绑定、重连补票。该句中「真匹配/房间码」被 `matchmaking = control_plane_quick_and_room_code`（2026-08-25）覆盖：控制面 `POST /matchmaking/quick` 加入最旧未满公开房或拉起新场，`POST /matchmaking/rooms` 建房发码，按码加入；席位随 MatchHost 登记写入，已签发票据数达到席位即满。容量满回 503。房间码字表与长度是开发期占位。控制面只调 MatchHost HTTP，MatchHost 不查库。不锁 FIFO 等待队列与预计等待、账号绑定、重连补票。该句中「不锁 FIFO 等待队列与预计等待」被 `matchmaking_queue = control_plane_fifo_wait_estimate`（2026-08-25）覆盖：MatchHost 容量满时控制面把快速游戏/建房写入 FIFO，回 202 与位次、预计等待；`GET /matchmaking/queue/:token` 查询，`DELETE` 取消；会话注销后按 FIFO 出队，快速游戏可进最旧未满公开房，建房只拉新场。预计等待 = 位次 × `CONTROL_PLANE_QUEUE_SLOT_ESTIMATE_MS`，队列 TTL 为 `CONTROL_PLANE_QUEUE_TTL_MS`，二者都是开发期占位，不是产品局时。就绪行暂存已签发票据明文直到过期、取消或对局注销；票据表仍只存 SHA-256。MatchHost 对控制面仍回 503，不查库。不锁账号绑定、重连补票。该句中「大厅 UI 仍待」被 `match_client_join = lobby_queue_ticket_latest_snapshot`（2026-08-25）覆盖：Godot `MatchJoinSession` 用注入式 HTTP 解释已锁匹配/队列 JSON；就绪后 `MatchPlaySession` 拼网关 `/ws?ticket=`，`MatchSnapshotFollow` 只跟从最新已解码快照（旧 tick 忽略）；`MatchLobbyShell` 是机关狂奔入口（代码创建 Window，关闭只隐藏）。命令 tick 为 0。不插值、不预测。双塔要塞入口、账号绑定、重连补票与名次仍待。口径见 [CD-12 §1](../10-product/12-product-structure.md#1-入口结构) 与 [CD-43 §2](../40-technical/43-networking-and-replay.md#2-传输)。该句中「只跟从最新已解码快照」被 `match_snapshot_map = latest_pose_placeholders`（2026-08-25）覆盖：`MatchSnapshotMap` 把最新快照玩家位姿映射为 1 米占位盒；旧 tick / 坏帧不重建；v1 箱子只有耐久、没有位姿，故不画 3D。不插值、不预测。赛道几何、名次、账号绑定与重连补票仍待。口径见 [CD-12 §1](../10-product/12-product-structure.md#1-入口结构) 与 [CD-43 §2](../40-technical/43-networking-and-replay.md#2-传输)。该句中「赛道几何仍待」被 `match_course_map = compiled_occupancy_placeholders`（2026-08-25）覆盖：`MatchCourseMap` 把本场编译拓扑的检查点垫、传送门源点与终点占用映射为 1 米占位盒；大厅默认编译与 MatchHost 相同的官方 `course_01`；缺课 / 坏袋不重建。可破坏袋本刀仍不画 3D。不插值、不预测。名次、账号绑定、重连补票与课程选择 API 仍待。口径见 [CD-12 §1](../10-product/12-product-structure.md#1-入口结构) 与 [CD-43 §2](../40-technical/43-networking-and-replay.md#2-传输)。该句中「可破坏袋本刀仍不画 3D」被 `match_crate_map = topology_pose_snapshot_durability`（2026-08-25）覆盖：`MatchCrateMap` 用编译拓扑可破坏袋位姿画 1 米占位盒；快照耐久 ≤0 或未列入则撤盒；快照不移动箱子。缺课 / 坏袋 / 坏快照不重建。不插值、不预测。名次、账号绑定、重连补票与课程选择 API 仍待。口径见 [CD-12 §1](../10-product/12-product-structure.md#1-入口结构) 与 [CD-43 §2](../40-technical/43-networking-and-replay.md#2-传输)。该句中「传送连线仍待」被 `match_portal_link_map = compiled_source_dest_gizmos`（2026-08-25）覆盖：`MatchPortalLinkMap` 用编译拓扑 portal 源点与 `dest_*` 画 `two_way` / `one_way` gizmo 条；`one_way` 加方向点；dangling 已省略不画。缺课 / 坏袋不重建。`MatchCourseMap` / `MatchCrateMap` / `MatchSnapshotMap` 仍不画连线。不插值、不预测。名次、账号绑定、重连补票、课程选择 API 与检查点顺序 gizmos 仍待。口径见 [CD-12 §1](../10-product/12-product-structure.md#1-入口结构) 与 [CD-43 §2](../40-technical/43-networking-and-replay.md#2-传输)。该句中「检查点顺序 gizmos 仍待」被 `match_checkpoint_order_map = compiled_unique_order_gizmos`（2026-08-25）覆盖：`MatchCheckpointOrderMap` 用编译拓扑检查点垫 `order` 打标；唯一 `order` 按升序画顺序条；重复 `order` 只打标不进顺序链。缺课 / 坏袋不重建。`MatchCourseMap` / `MatchCrateMap` / `MatchPortalLinkMap` / `MatchSnapshotMap` 仍不画顺序 gizmos。不插值、不预测。账号绑定、重连补票、课程选择 API 与可达性叠加 gizmos 仍待。口径见 [CD-12 §1](../10-product/12-product-structure.md#1-入口结构) 与 [CD-43 §2](../40-technical/43-networking-and-replay.md#2-传输)。该句中「名次仍待」被 `match_standing = snapshot_live_board`（2026-08-25）覆盖：`TraprushStanding` 按 [CD-21 §6.1](../20-gameplay/21-traprush.md#61-排序优先级) 第 1、2 条从最新快照排出直播名次；已冲线按 `finish_tick` 再按槽位；未冲线按 `accepted_count` 再按槽位；`MatchStandingMap` 把名次标到玩家位姿上方，大厅 HUD 写出 `standings=` / `mvp=`。缺字段 / 坏快照不重建。路径距离、到达当前检查点时间、结算写库、账号绑定、重连补票、课程选择 API 与可达性叠加 gizmos 仍待。口径见 [CD-12 §1](../10-product/12-product-structure.md#1-入口结构)、[CD-13 §4](../10-product/13-account-and-session.md#4-单局排名) 与 [CD-43 §2](../40-technical/43-networking-and-replay.md#2-传输)。该句中「离线单人试玩仍待」被 `traprush_offline_solo = local_embedded_match_session`（2026-08-25）覆盖：大厅 `Solo play` 启动 `MatchOfflineSession`，把默认官方 `course_01` 编进与线上相同的 `TraprushMatchSession`；命令 tick 为 0，快照只跟从本地最新帧；HUD 持续「离线试玩，成绩不上传」；不发 HTTP/WS；Web 拒绝。不锁本机幽灵、草稿试玩、结算写库、账号绑定、重连补票与课程选择 API。口径见 [CD-12 §1](../10-product/12-product-structure.md#1-入口结构) 与 [CD-13 §3](../10-product/13-account-and-session.md#3-离线单人模式)。该句中 `match_realtime_loop` 的「防伪造门禁仍待」被 `match_command_gate = one_intent_per_slot_per_tick`（2026-08-25）覆盖：`MatchRealtime` 每占用槽位每个 commit_tick 至多入队一条命令（先到先得），后到拒绝；断开丢弃该槽排队，避免旧意图落到新连接；快照帧不能当命令。官方 `course_01` 2 人 Headless 冲线夹具经同一回路验收，同磁带同快照/哈希。墙钟发送速率、插值窗口仍待（CD-63）。不锁结算写库、账号绑定、重连补票。口径见 [CD-43 §3](../40-technical/43-networking-and-replay.md#3-回放与确定性)。该句中「不锁结算写库」被 `match_settlement = all_finished_control_plane_once`（2026-08-25）覆盖：全部配置玩家冲线后 `TraprushMatchSettlement` 用 [CD-21 §6.1](../20-gameplay/21-traprush.md#61-排序优先级) 第 1、2 条生成单局名次记录；`match_server` 心跳带上；MatchHost 停止前 `POST /match-sessions/:matchId/settlement` 由控制面写库一次，409 视为已写入；注销会话后记录仍在。离线与客户端不写。不生成 MMR。限时未全员冲线结算、账号绑定与重连补票仍待。口径见 [CD-13 §4](../10-product/13-account-and-session.md#4-单局排名) 与 [CD-14](../10-product/14-data-and-telemetry.md)。该句中「重连补票仍待」被 `match_reconnect = consumed_ticket_same_seat_reissue`（2026-08-25）覆盖：入场票据绑定席位；`POST /tickets/verify` 返回 `seat`；网关把 `slot` 接到上游 URL，不解析帧；`MatchRealtime.occupy_slot` 占用该席，断开释放占用、仿真保留，同一席可再占；控制面 `POST /match-sessions/:matchId/tickets/reconnect` 用已消费票补发同席位新票，旧票 `superseded_at`，不占额外席；已消费旧票忽略 `expires_at`。客户端断线后补票再入场，快照只跟从最新帧。注销后不能补票。不锁账号绑定、插值/预测。口径见 [CD-13 §1](../10-product/13-account-and-session.md#1-在线模式) 与 [CD-43 §2](../40-technical/43-networking-and-replay.md#2-传输)。该句再被 `lobby_cancel = local_leave_no_reissue`（2026-08-25）补充：大厅 Cancel 在 connecting / in_match / closed 时本地离开（立即断开网关、join/play 回 idle、清玩家盒），不走补票；队列 DELETE 仍只取消 WAITING。不锁离开对局 HTTP。口径见 [CD-12 §1](../10-product/12-product-structure.md#1-入口结构) 与 [CD-43 §2](../40-technical/43-networking-and-replay.md#2-传输)。该句中「课程选择 API 仍待」被 `match_course_select = official_id_per_match`（2026-08-25）覆盖：HTTP JSON 只用官方 id `course_01` / `course_02` / `course_03`；空 body 默认 `course_01`；快速游戏只进同一赛道未满公开房；队列记住 `course`；控制面按场把 id 交给 MatchHost，Godot 读 `res://content/official/traprush/{id}.json`；大厅跟从响应编译同一赛道，Solo 复用选择器。不接受 `res://` 路径或 UGC 课。不锁人数按场下发、账号绑定、插值/预测。口径见 [CD-12 §1](../10-product/12-product-structure.md#1-入口结构) 与 [CD-44 §3](../40-technical/44-deployment.md#3-进程隔离与租约)。该句中「不接受 UGC 课」被 `match_ugc_online = official_or_signed_content_ref`（2026-09-11 人类拍板）覆盖：匹配请求在现有 `{ course?, seats? }` 上增加可选 `content: { id, version }`；与 `course` 互斥；都不带仍默认 `course_01`。建房钉死 version，不跟 `latest`。官方响应仍只回 `course`；UGC 回 `course: null` + `content` + `content_hash`。不把课表塞成 `id@version` 字符串。`course_f_playable` / 官方 id 当 `content.id` / 未签名一律 400。邀请串只带房间码，内容引用不进 URL。MatchHost 仍不查库。接线属 M5 C4，本行只拍口径。**未覆盖**：二进制帧、结算 HTTP、持钥 publish。口径见 [CD-42 §3.5](../40-technical/42-contracts-and-rulevm.md#35-匹配与玩家发布-httpc3-已接线c4-已拍未接线)、[CD-12](../10-product/12-product-structure.md) 与 [CD-44](../40-technical/44-deployment.md)。该句中「人数按场下发」被 `match_seats_select = per_match_1_to_8`（2026-08-25）覆盖：HTTP JSON `seats` 为 1～8 整数；空 body 默认 2；快速游戏只进同一赛道且同一人数未满公开房；队列记住 `seats`；控制面按场把人数交给 MatchHost，Godot `--players=N`；大厅选人数。不接受 `players` 别名。Solo 仍为 1 人本地会话。不锁账号绑定、插值/预测。口径见 [CD-12 §1](../10-product/12-product-structure.md#1-入口结构) 与 [CD-44 §3](../40-technical/44-deployment.md#3-进程隔离与租约)。该句中「插值/预测」被 `match_snapshot_interp = previous_latest_q48_sample`（2026-08-25）覆盖：`MatchSnapshotFollow` 在 tick 前进时保留上一份玩家位姿；`MatchSnapshotInterp` 用调用方 Q48.16 `t`（0～SCALE）采样 x/y/z 与最短 yaw 弧；进度字段取最新权威；无上一份或任一轴 ≥ 1 格（`Fixed.SCALE`）贴最新。`play_interp_step` 是表现桩，不是插值窗口。不预测、不校正。不锁账号绑定、本地预测/远端外推碰撞/平滑对账。口径见 [CD-43 §2](../40-technical/43-networking-and-replay.md#2-传输)。该句中「本地预测」被 `match_local_predict = own_slot_move_jump_overlay`（2026-08-25）覆盖：入场就绪 JSON 带本票 `seat`；`MatchLocalPredict` 把 Move/Jump overlay 叠在最新权威本席位姿上，本席不插值；更新 tick 硬贴权威。传送/重置/道具/冲线与远端胶囊不预测。不锁账号绑定、远端外推碰撞、平滑对账。口径见 [CD-43 §2](../40-technical/43-networking-and-replay.md#2-传输)。
- `match_action_stubs = preview_aligned_process_placeholders`（2026-08-25）：对局进程 boot 把跳跃/支撑/道具伤害与触达套成与 Preview 相同的占位值（`jump_dy` / `support_dy` / `use_item_reach_dz` = `Fixed.SCALE`，伤害 1，reach dx/dy = 0），不是产品数值。官方 `course_01` 出生点 UseItem 打碎 +Z 箱；官方赛道无固体立足点，Jump 仍为空操作。大厅 Solo 占位桩与进程对齐。不锁爆破表、远端外推碰撞、平滑对账。口径见 [CD-44 §3](../40-technical/44-deployment.md#3-进程隔离与租约)。
- `match_own_slot_camera = preview_offset_follow`（2026-08-25）：大厅 SnapshotCamera 跟随本席表现位姿（线上预测 overlay / Solo 本地权威），偏移与 `AuthoringPreviewMap` 相同；`follow_slot < 0` 或缺席位看原点；远端不拉镜头。不是产品镜头或 FOV。不锁远端外推碰撞、平滑对账、离开对局 HTTP。口径见 [CD-12 §1](../10-product/12-product-structure.md#1-入口结构)。
- `match_move_facing = wasd_discrete_8way_yaw_bam`（2026-08-25）：大厅 WASD 把水平朝向写入已有 Move 命令 `yaw_bam`（8 向离散 BAM，W=0 为世界 -Z）；省略哨兵仍是 `-1`，`0` 是合法朝前。立方体玩家盒加 local -Z 面向标记。线上 overlay 立即改本席朝向，Solo 走本地权威。不发明 atan2，不锁产品转向速度，不改 Preview WASD。口径见 [CD-21 §3](../20-gameplay/21-traprush.md#33-权威运动模型) 与 [CD-12 §1](../10-product/12-product-structure.md#1-入口结构)。该句中「大厅 WASD」与「不改 Preview WASD」被 `play_input = vector_plus_rising_edge_events`（2026-09-02，纠偏 D7）补充：玩法壳消费 `PlayInput` 快照（`move_x`/`move_z` + 上升沿动作），键盘是适配器；`shove` / `sprint` / `reset_checkpoint` 进 Input Map。8 向量化仍是 `MatchMoveFacing`；模拟量按符号变成整步，不把未锁定的模拟速度写进 MoveIntent。Preview Move 仍不写 `yaw_bam`。触控 UI / 虚拟摇杆 / 按键重映射界面仍未做。口径见 [CD-21 §3.2](../20-gameplay/21-traprush.md#32-基础操作)。
- `play_anim_state = presentation_priority_table`（2026-09-02，纠偏 C4 产出 4）：`idle` / `run` / `jump` / `land` / `shove` / `hit` / `break` / `portal` 单一优先级表，`PlayAnimState` 派生。`airborne` 用半格接触探针（`PlaceholderSpec.CELL / 2`），不复用 Jump 的 1 格 `support_dy`。Solo 与 Preview 写角色 metadata 与 Label3D `anim`。不播 clip。v1 快照无 vy/stun，在线远端不接线。时长仍属 CD-63。口径见 [CD-21 §3.4](../20-gameplay/21-traprush.md#34-表现动画状态)。
- `lobby_l4_split = chrome_net_sampler_stage_hud_director`（2026-09-02，纠偏 C5 产出 1）：`MatchLobbyShell` 只做门面（Window + `_process` / `_physics_process` + 公开 API）。L4 协作者是 `match_lobby_chrome`（platform 窗体，**不得**设 `content_scale_*`）/ `match_lobby_net`（platform HTTP+WS）/ `match_lobby_sampler`（input 上升沿）/ `match_lobby_stage`（presentation 八套映射）/ `match_lobby_hud`（presentation 状态行）/ `match_lobby_director`（匹配 / Solo / 离场 / 结算动词）。Solo 占位桩注入挪到 `MatchOfflineSession.apply_play_stubs()`。协议帧、Schema、官方课 JSON、冻结占位数值一个不改。`match_join_session.gd` 仍超 400 行，E9 批次退出仍欠。口径见 [CD-41 §4 / §5](../40-technical/41-architecture.md)。该句中「`match_join_session.gd` 仍超 400 行」被 `join_session_split = codec_accept_facade`（2026-09-02）覆盖：`MatchJoinSession` 只做注入 HTTP 状态机门面（`try_*` / pending / `fail_*` / 公开字段）。协作者是 `match_join_codec`（JSON / URL / 房间码 / 键集 / 结算行）与 `match_join_accept`（status 分发与字段拷贝）。公开静态 `normalize_room_code` / `http_url` / `parse_json_object` 仍在门面上。协议 JSON、Schema、官方课、冻结占位数值一个不改。匹配三文件均 < 400 行。诚实边界：`match_snapshot_map.gd` 仍约 404 行；`match_session.gd` / Preview 等其它层仍超 E9。口径见 [CD-41 §5](../40-technical/41-architecture.md)。该句中「`match_session.gd` 仍超 E9」被 `match_session_split = bootstrap_intents_scan_view`（2026-09-02，纠偏 C5 第 3 章）覆盖：`TraprushMatchSession` 只做门面（公开查询 / `apply_player_intent` / `commit_tick` / 步长门禁）。协作者是 `match_session_bootstrap`（装载拓扑与出生）/ `match_session_intents`（权威命令）/ `match_session_scan`（占用扫描与硬直）/ `match_session_view`（读模型与 `hash_state`）。公开 API 仍在门面上。协议帧、Schema、官方课 JSON、冻结占位数值一个不改。对局会话五文件均 < 400 行。诚实边界：Preview / 控制面 / `course_completion_probe.gd` / `graybox_course.gd` / `simulation_world.gd` 仍超 E9。口径见 [CD-41 §5](../40-technical/41-architecture.md)。该句中「Preview 仍超 E9」被 `authoring_preview_split = bootstrap_intents_scan_view`（2026-09-02，纠偏 C5 第 4 章）覆盖：`AuthoringPreview` 只做门面（`connect_from` / 安全点 Patch / `try_start_play` / `try_apply_play_intent` / `try_advance_play` / 公开查询）。协作者是 `authoring_preview_bootstrap`（装载拓扑与出生）/ `authoring_preview_intents`（试玩意图）/ `authoring_preview_scan`（占用扫描与硬直）/ `authoring_preview_view`（读模型）。公开 API 仍在门面上。协议帧、Schema、官方课 JSON、冻结占位数值一个不改。Preview 会话五文件均 < 400 行。诚实边界：`authoring_preview_map.gd` / `authoring_preview_shell.gd` / 控制面 / `course_completion_probe.gd` / `graybox_course.gd` / `simulation_world.gd` 仍超 E9。口径见 [CD-41 §5](../40-technical/41-architecture.md)。该句中「`authoring_preview_map.gd` 仍超 E9」被 `authoring_preview_map_split = occupancy_gizmos_overlay_player`（2026-09-02，纠偏 C5 第 5 章）覆盖：`AuthoringPreviewMap` 只做门面（脏检查 `rebuild` / 公开查询 / 玩家标记）。协作者是 `authoring_preview_map_convert`（定点换算与节点名）/ `authoring_preview_map_occupancy`（占用占位）/ `authoring_preview_map_gizmos`（传送 / 检查点 / 终点 gizmos）/ `authoring_preview_map_overlay`（可达性叠加）/ `authoring_preview_map_player`（试玩角色标记）。公开 API 仍在门面上。协议帧、Schema、官方课 JSON、冻结占位数值一个不改。Preview 映射六文件均 < 400 行。诚实边界：`authoring_preview_shell.gd` / 控制面 / `course_completion_probe.gd` / `graybox_course.gd` / `simulation_world.gd` / `simulation_bundle.gd` / `authoring_editor_shell.gd` 仍超 E9。口径见 [CD-41 §5](../40-technical/41-architecture.md)。该句中「`authoring_preview_shell.gd` 仍超 E9」被 `authoring_preview_shell_split = chrome_sampler_hud_play_view`（2026-09-02，纠偏 C5 第 6 章）覆盖：`AuthoringPreviewShell` 只做门面（Window + `_process` + 公开 API）。协作者是 `authoring_preview_shell_chrome`（platform 窗体，**不得**设 `content_scale_*`）/ `authoring_preview_shell_sampler`（input 上升沿）/ `authoring_preview_shell_hud`（presentation 状态行）/ `authoring_preview_shell_play`（试玩桩拷贝与意图 payload）/ `authoring_preview_shell_view`（Preview 映射挂载与开停玩重建）。公开 API 仍在门面上。协议帧、Schema、官方课 JSON、冻结占位数值一个不改。Preview 壳六文件均 < 400 行。诚实边界：控制面 / `course_completion_probe.gd` / `graybox_course.gd` / `simulation_world.gd` / `simulation_bundle.gd` / `authoring_editor_shell.gd` 仍超 E9。口径见 [CD-41 §5](../40-technical/41-architecture.md)。该句中「`simulation_world.gd` / `simulation_bundle.gd` 仍超 E9」被 `sim_world_bundle_split = query_move_decode_bags`（2026-09-02，纠偏 C5 第 9 章）覆盖：`SimulationWorld` 只做门面（spawn / pose / tick / hash / 公开查询与移动）。协作者是 `simulation_world_query`（占用 / 支撑 / 出界 / 体积）与 `simulation_world_move`（离散扫掠）。`SimulationBundle` 只做门面（字段、`from_dictionary` / `to_dictionary` / `asset_collision`）。协作者是 `simulation_bundle_decode`（v1/v2 解码与资产闭合）与 `simulation_bundle_bags`（各占用袋解析）。公开 API 仍在门面上。协议帧、Schema、官方课 JSON、冻结占位数值一个不改。仿真世界三文件与 bundle 三文件均 < 400 行。诚实边界：控制面 / MatchHost `registry.ts` / `course_completion_probe.gd` / `graybox_course.gd` / `authoring_editor_shell.gd` / `traprush_topology_compiler.gd` 仍超 E9。口径见 [CD-41 §5](../40-technical/41-architecture.md)。该句中「`course_completion_probe.gd` / `graybox_course.gd` 仍超 E9」被 `graybox_probe_split = layout_assemble_play_heuristic_search`（2026-09-02，纠偏 C5 第 10 章）覆盖：`TraprushGrayboxCourse` 只做门面（`assemble` / 公开 `try_*` / 命令入带）。协作者是 `graybox_course_layout`（layout 字段读取）/ `graybox_course_assemble`（从 layout 装世界）/ `graybox_course_play`（意图与占用动词）。`TraprushCourseCompletionProbe` 只做门面（`run_path` / `run_bundle` / `try_replay` / `without_portals` / 结果字典）。协作者是 `course_completion_probe_heuristic`（目标、格距、地面动作排序）与 `course_completion_probe_search`（贪心 / 脚本重放 / 会话与状态键）。公开 API 仍在门面上。协议帧、Schema、官方课 JSON、冻结占位数值一个不改。灰盒四文件与探针三文件均 < 400 行。`--bot-run` 步数仍为 5 / 5 / 12。诚实边界：控制面 / MatchHost `registry.ts` / `authoring_editor_shell.gd` / `traprush_topology_compiler.gd` 仍超 E9。口径见 [CD-41 §5](../40-technical/41-architecture.md)。该句中「`authoring_editor_shell.gd` / `traprush_topology_compiler.gd` 仍超 E9」被 `editor_compiler_split = chrome_place_follow_bags_fields`（2026-09-02，纠偏 C5 第 11–12 章）覆盖：`AuthoringEditorShell` 只做门面（Window + 公开 EDIT API）。协作者是 `authoring_editor_shell_chrome`（platform 窗体，**不得**设 `content_scale_*`）/ `authoring_editor_shell_place`（place payload）/ `authoring_editor_shell_follow`（Preview 跟随与安全点转发）。`TraprushTopologyCompiler` 只做门面（`compile` / 区标签常量）。协作者是 `traprush_topology_compiler_bags`（占用与传送袋）与 `traprush_topology_compiler_fields`（资产引用、位姿、组件体）。公开 API 仍在门面上。协议帧、Schema、官方课 JSON、冻结占位数值一个不改。编辑壳四文件与编译器三文件均 < 400 行。`--bot-run` 步数仍为 5 / 5 / 12。诚实边界：控制面 `server.ts` / `database.ts`、MatchHost `registry.ts`、`visual_asset_catalog.gd`、`match_snapshot_map.gd` 仍超 E9。口径见 [CD-41 §5](../40-technical/41-architecture.md)。该句中「控制面 / registry / 视觉目录 / 快照映射仍超 E9」被 `e9_remaining_split = server_db_registry_catalog_map`（2026-09-02，纠偏 C5 第 13 章）覆盖：`buildServer` 只做 Fastify 装配门面，会话路由在 `server_sessions`、匹配在 `server_matchmaking`。`ControlPlaneDatabase` 只做 SQLite 入口门面，协作者是 `database_rows` / `database_sessions` / `database_tickets` / `database_queue`。`MatchRegistry` 把 listen / 启动失败整理交给 `registry_launch`。`SharedVisualAssetCatalog` 只做路径常量与公开 API，协作者是 Instantiate / Fit。`MatchSnapshotMap` 把生成玩家 / 视觉 / 镜头交给 `match_snapshot_map_players`。公开 API 仍在门面上。协议帧、Schema、官方课 JSON、冻结占位数值一个不改。上述生产文件均 < 400 行。`--bot-run` 步数仍为 5 / 5 / 12。诚实边界：测试 / addons / GUT 不在 E9 本刀；触控 UI、按 `asset_id` 解析视觉、传送门模型、字体入包、扫掠步数无上限仍欠。口径见 [CD-41](../40-technical/41-architecture.md)。
- `match_own_slot_tint = follow_slot_albedo`（2026-08-25）：大厅 `follow_slot` 把本席玩家盒涂成 `OWN_ALBEDO`（青），远端仍 `REMOTE_ALBEDO`（海军蓝）；名次标本席前缀 `*`。`follow_slot < 0` 时全员远端色。不是产品皮肤或槽位色盘。不锁账号绑定、远端外推碰撞、平滑对账、离开对局 HTTP。口径见 [CD-12 §1](../10-product/12-product-structure.md#1-入口结构)。
- `match_pad_progress = own_accepted_count_tint`（2026-08-25）：大厅本席 `accepted_count` 把编译拓扑检查点垫涂成已验收 / 当前目标 / 未到；`accepted_count < 0` 保持原垫色。不是走路可达或合法路径距离。不锁账号绑定、远端外推碰撞、平滑对账、离开对局 HTTP。口径见 [CD-12 §1](../10-product/12-product-structure.md#1-入口结构)。
- `match_finish_loop = own_finish_hud_result`（2026-08-25）：大厅本席 `finish_tick` 把终点涂成未到 / 当前目标 / 已冲线；HUD 写 `pads=n/m` 与 `finish=n`；快照全员 `finish_tick>=0` 时加 `result=`。本地表现，不是结算写库或 GET 结算面板。不锁账号绑定、远端外推碰撞、平滑对账、离开对局 HTTP。口径见 [CD-12 §1](../10-product/12-product-structure.md#1-入口结构)。
- `match_lobby_reset_hud = floor_crates_reset`（2026-08-25）：大厅开玩 HUD 写 `floor=n`（本席权威 `y / Fixed.SCALE` 向零，不用插值采样）与 `crates=n/m`（活着的箱 / 编译袋总数）；R 上升沿把已有 ResetToCheckpointIntent 接到可见复位，进度不回退。不是长按时长、走路可达或结算写库。不锁离开对局 HTTP。口径见 [CD-12 §1](../10-product/12-product-structure.md#1-入口结构)。
- `match_settlement_panel = lobby_get_after_host_write`（2026-08-25）：MatchHost 对 running 场解析心跳，一旦有合法 settlement 就 POST（409 已写入）；停止前再 POST 一次。大厅在线上全员冲线后轮询 GET，200 后 HUD `settled=`。Solo 不 GET。客户端 `allows_settlement` 仍为 false（不 POST）。404 `settlement_not_found` 不把 join 打成 FAILED。不锁离开对局 HTTP、MMR、限时未全员冲线结算、账号绑定。口径见 [CD-12 §1](../10-product/12-product-structure.md#1-入口结构)、[CD-13 §4](../10-product/13-account-and-session.md#4-单局排名) 与 [CD-44 §3](../40-technical/44-deployment.md#3-进程隔离与租约)。该句中 `match_local_predict` 的「远端胶囊不预测」被 `match_predict_solids = latest_crate_and_remote_capsules`（2026-08-26）收窄：线上 overlay 若与最新活箱（编译拓扑 + 快照耐久 >0，半长 cell/2）或其它席位最新快照胶囊重叠，本帧不叠 overlay；几何对齐对局进程占位桩（半径/柱高 8192），不用 1 米表现盒。远端不外推、不平滑对账、不扫掠。Solo 仍不叠 overlay。不锁离开对局 HTTP、账号绑定、产品碰撞。口径见 [CD-21 §8](../20-gameplay/21-traprush.md#8-网络与仿真基线) 与 [CD-43 §2](../40-technical/43-networking-and-replay.md#2-传输)。
- `match_lease_renew = valid_input_tick_heartbeat`（2026-08-26）：对局心跳带 `valid_input_tick`（最近一次已入队、`apply_player_intent` 成功、且应用后权威标记相对应用前变化的权威 tick；从未发生为 -1。标记 = 会话哈希 + 箱耐久）。心跳本身不续租。MatchHost 扫描最近一条 `match_tick`，仅当该 tick ≥0 且相对该场上次续租前进时 `renew`。缺字段、非整数、负数、垃圾行、以及只有 settlement 不续。不改 30 分钟租约与 10 分钟 idle 数字。`POST /matches/:id/renew` 仍给测试/运维。不锁墙钟发送速率、离开对局 HTTP、Bot 流量。口径见 [CD-44 §3](../40-technical/44-deployment.md#3-进程隔离与租约)。
- `gateway_tls = in_process_wss_optional_pem`（2026-08-26）：`GATEWAY_TLS_CERT` 与 `GATEWAY_TLS_KEY` 成对指向 PEM 时网关在本进程终结 TLS（Fastify `https`，客户端 `wss`）；只设一个拒绝启动；都不设仍明文 `ws`，只许本机开发。`/healthz` `/readyz` 与 `/ws` 同一端口。对局进程上游仍内网明文 `ws`。Godot `wss://` 使用 `TLSOptions.client_unsafe()`；大厅 HUD 常驻 `tls=on|off`。`npm run dev` 默认不启用。不锁生产 CA 信任链、控制面 HTTPS、MatchHost 子进程 TLS。口径见 [CD-43 §2](../40-technical/43-networking-and-replay.md#2-传输)。该句中「明文只许本机、不得暴露公网」被 `c1_test_remote = plaintext_http_ws_generic_runbook`（2026-08-27）覆盖：纠偏 D11。测试开发阶段允许人类把明文 `http`（控制面）与 `ws`（网关）打到自备远端测试机。C1 部署手册为 `docs/runbooks/server-deploy.md`，只写人类可操作步骤与占位符，不入库真实 IP、域名、VPS 规格、发行版、SSH 落点。不把 `wss` / Let's Encrypt / 域名作为 C1 必须项。覆盖签字时 D2「C1 内完成域名 + Let's Encrypt」与「手册必须写死香港 VPS 规格/域名/SSH」。`primary_region = hong_kong`、D2 选 A（Web 是一期持续入口）与宪法第二十二条（一期产品形态为 TLS 网关）不被覆盖。公开运营前仍须 TLS。风险接受见 [CD-62](../60-plan/62-risk-register.md)。口径见 [纠偏方案 §4.0.1 / D11](../../docs/plans/course-correction-2026-08.md) 与 [CD-43 §2](../40-technical/43-networking-and-replay.md#2-传输)。
- `protocol_rtt_remote_sample = last_session_2026_09_02`（2026-09-02）：人类采 `user://protocol_rtt.jsonl`（不入库），AI 按 `seq` 复位切开后取最后一场回填 [server-deploy.md §13](../../docs/runbooks/server-deploy.md#13-协议层-rttc3)。n=106，丢失 0，P50/P90/P95=16/19/25 ms，min 6 / max 164。同路径 ICMP P50 仍为 3ms。jsonl 另含更早会话（含 212 次丢失与两段 P50≈1065ms），未并入该格。不覆盖 [CD-63 §1.5](../60-plan/63-open-decisions.md)；不改 `SNAPSHOT_EVERY_TICKS`。数字只在手册 §13 维护。覆盖此前「远端协议层 RTT 仍空」。口径见 [CD-43](../40-technical/43-networking-and-replay.md) 文首与 [CD-62](../60-plan/62-risk-register.md)。该句中「不覆盖 CD-63 §1.5」被 `e3_lock = promote_existing_stubs_2026_09_02`（2026-09-02）覆盖：近端样本未证伪现桩，人类把现桩升为锁定值，**仍不改代码常量**。Tick 60、`SNAPSHOT_EVERY_TICKS = 2`、`HEARTBEAT_EVERY_TICKS = 60`、`INTERP_STEP = Fixed.SCALE / 2`、输入每帧采样、对账只跟最新快照。数字只在 [CD-43 §4](../40-technical/43-networking-and-replay.md#4-已锁定的网络参数) 维护。ICMP / 该场 P50=16ms **不是**改数字的依据。迁出 CD-63 §1.5。
- `match_move_cap = one_cell_per_command_reject`（2026-08-26）：对局会话拒绝 |dx| 或 |dz| 超过 `Fixed.SCALE` 的 Move，整条不应用、不裁剪。这是防瞬移门禁（M3 验收：客户端不能伪造位置），不是产品速度。诚实大厅仍发 `play_move_step = SCALE/16`；Headless 一格步进仍通过。线上超限帧可入队，apply 失败则该 tick 作废且不续租。Preview `IntentStepper` 不经此门。口径见 [CD-21 §8](../20-gameplay/21-traprush.md#8-网络与仿真基线) 与 [CD-43 §3](../40-technical/43-networking-and-replay.md#3-回放与确定性)。
- `match_shove = nearest_capsule_no_wire_id`（2026-08-26）：命令 `intent_id=5` 为 `ShoveIntent`，无线上目标 id，保留字段仍须为零。服务端在一格邻域内选最近其它胶囊，沿 XZ 远离施术者用调用方 `shove_step` 推开，冷却为调用方 tick。超限 step 整条拒绝。大厅 F 上升沿采样。不预测、不锁产品力度。Interact 仍不接线。口径见 [CD-21 §8](../20-gameplay/21-traprush.md#8-网络与仿真基线) 与 [CD-43 §1](../40-technical/43-networking-and-replay.md#1-序列化分工)。
- `match_oob_reset = caller_aabb_checkpoint_respawn`（2026-08-26）：环境失败后无限复活到最近检查点。`TraprushOutOfRangeReset` 在调用方 AABB（闭区间）外写回 `CheckpointSpawn.pose_for`。对局默认关闭；boot / Solo / Preview 壳打开 ±8 格开发桩，不是产品场地。先复位再占用扫描。不计数掉出次数 N，不接重力/下落，不写复活硬直。空区间拒绝。口径见 [CD-21 §6](../20-gameplay/21-traprush.md#6-胜负与排名) 与 [CD-32 §3](../30-ugc/32-editor-and-preview.md#3-从编辑到预览)。
- `hazard_cycle = cooldown_ticks_toggle_solid`（2026-08-26）：v1 SimulationBundle 必含 `hazards` 数组（可空）。袋为 `entity_id` / `x` / `y` / `z` / `cooldown_ticks`（已有组件字段，不发明 `period`，不编 damage/knockback）。编译自带 `hazard`+`transform` 的实体；与检查点/传送/终点/可破坏同实体则整份拒绝。加载为固体盒。`TraprushHazardCycle` 在 `commit_tick` / `try_advance_play` 于 `world.tick()` 之后按 `((tick_index / cooldown_ticks) % 2) == 0` 切换固体（`cooldown_ticks < 1` 始终固体）。意图不推进 tick，故不切换。不挤出、不伤害。官方三张赛道 0 个机关。Preview 壳 **Advance tick** 调用已有 `try_advance_play`。不锁产品秒数、预警动画。该句中「大厅机关图仍待」被 `hazard_lobby_map = compiled_bag_tick_visibility`（2026-08-26）覆盖：`MatchHazardMap` 把编译袋画成 1 米占位盒；显隐跟同一 `is_solid(快照 tick, cooldown_ticks)`，不改协议；固体半周期进入本席 overlay `live_solid_boxes`；Preview 占位盒用同一洋红，开玩后非固体隐藏；HUD `hazards=n/m`。官方赛道仍 0 个。不锁预警动画、产品秒数、编辑器 Place hazard、官方赛道塞机关、伤害/击退。口径见 [CD-21 §5.1](../20-gameplay/21-traprush.md#51-障碍分类)、[CD-32 §3](../30-ugc/32-editor-and-preview.md#3-从编辑到预览) 与 [CD-43 §2](../40-technical/43-networking-and-replay.md#2-传输)。该句之后的固定不可破坏占用被 `solid_occupancy = zone_tag_always_solid`（2026-08-26）覆盖：v1 SimulationBundle 必含 `solids` 数组（可空）。袋为 `entity_id` / `x` / `y` / `z`。编译自 `zone.tags` 含 `solid` 且有 `transform` 的实体（与 `finish` 同一套标签，不新增 Component Schema 字段）；缺 transform、与检查点/传送/终点/可破坏/机关同实体、或 `finish`+`solid` 同实体则整份拒绝。加载为始终固体盒，不随 tick 切换。`MatchSolidMap` 画石色 1 米占位；计入本席 overlay `live_solid_boxes`；Preview 同色；HUD `solids=n/m`。官方三张赛道 0 个 solid。Jump 接地位移用合成固体袋单测。不锁编辑器 Place solid、官方赛道塞固体、重力/下落。口径见 [CD-21 §5.1](../20-gameplay/21-traprush.md#51-障碍分类)、[CD-32 §3](../30-ugc/32-editor-and-preview.md#3-从编辑到预览) 与 [CD-43 §2](../40-technical/43-networking-and-replay.md#2-传输)。该句中「编辑器 Place solid」与 hazard 章留下的「编辑器 Place hazard」被 `editor_place_occupancy = place_op_solid_hazard_crate`（2026-08-26）覆盖：工具条用已有 EDIT `place` 摆固定固体、周期机关与可破坏箱；`cooldown_ticks=1` 与耐久 1 是开发桩，不是产品秒数或爆破表。Editor / Preview 占位石色 / 洋红 / 橙色。不新增 `op`，不改官方赛道，不锁预警动画、伤害/击退、重力/下落。口径见 [CD-32 §3](../30-ugc/32-editor-and-preview.md#3-从编辑到预览)。该句中「不改官方赛道」被 `official_course_occupancy = spawn_adjacent_solid_and_hazard`（2026-08-26）覆盖：三张官方赛道各 1 个始终固体（entity 70，出生点 −X 一格）与 1 个周期机关（entity 60，出生点 −Z 两格，`cooldown_ticks=1` 桩；两格避开双人 Shove 邻域）；不挡 +X 必经路；大厅/Preview 石色/洋红。不锁 Place finish、产品秒数、预警动画、伤害/击退、重力/下落。口径见 [CD-21 §5.1](../20-gameplay/21-traprush.md#51-障碍分类) 与 [CD-32 §3](../30-ugc/32-editor-and-preview.md#3-从编辑到预览)。该句中「Place finish」被 `editor_place_finish = place_op_finish_zone`（2026-08-26）覆盖：工具条用已有 EDIT `place` 摆 `zone.tags` 含 `finish` 的终点占用盒；Editor / Preview 金色占位（`FINISH_ALBEDO`，与大厅 pending 金同色）。第二份终点仍可写入（不是写入门禁），编译整份拒绝。不新增 `op`，不改官方赛道，不锁预警动画、产品秒数、伤害/击退、重力/下落。口径见 [CD-21 §5.1](../20-gameplay/21-traprush.md#51-障碍分类) 与 [CD-32 §3](../30-ugc/32-editor-and-preview.md#3-从编辑到预览)。该句中「不改官方赛道」与 `match_action_stubs` 中「Jump 仍为空操作」被 `official_spawn_footing = spawn_underfoot_solid_downward_support`（2026-08-26）覆盖：三张官方课各加 entity 80（出生点正下一格始终固体）；对局 / Solo / Preview 壳 `support_dy = -Fixed.SCALE`（向下探测，与灰盒同号）；`jump_dy = Fixed.SCALE / 4`（占位 hop，避免 `course_01` 上楼传送盒）；出生点 Jump 接地 hop；在线 overlay `play_jump_dy` 仍为 0。不锁重力/下落、产品跳跃高度。口径见 [CD-21 §5.1](../20-gameplay/21-traprush.md#51-障碍分类) 与 [CD-32 §3](../30-ugc/32-editor-and-preview.md#3-从编辑到预览)。该句中「不锁重力/下落」被 `match_authoritative_fall = stub_cell_until_blocked`（2026-08-26）覆盖：`TraprushMatchSession.fall_dy` 默认 0；boot / Solo 占位 `-Fixed.SCALE / 16`（跟引擎 tick 走）；Preview 壳占位 `-Fixed.SCALE`（手动 Advance）；`MatchRealtime.commit_tick` 先 `apply_player_falls` 再意图再 `advance_sim_tick`；Solo `_process` 先 `try_advance` 再采样空格；Preview 只在 `try_advance_play` 下落；纯下落不续租。不锁产品重力加速度、不铺官方沿路地板。口径见 [CD-21 §3.3](../20-gameplay/21-traprush.md#33-权威运动模型)、[CD-32 §3](../30-ugc/32-editor-and-preview.md#3-从编辑到预览) 与 [CD-44 §3](../40-technical/44-deployment.md#3-进程隔离与租约)。

- `gut_tiering = fast_and_slow_both_on_pr`（2026-09-01）：GUT 按目录分 fast（`tests/unit` + `tests/integration` + `tests/replay`）与 slow（`tests/slow`，官方赛道在权威仿真上的完整 `CourseCompletionProbe` 搜索）两层，**两层都是每次 PR 的门禁**；分层只为让本地能少跑一层。动机是耗时集中而非用例总数：Windows 开发机 354.6 s 里三个搜索脚本占 63%。三条措施：`game/tests/support/course_probe_cache.gd` 按入参记忆化探针（同一组参数整场只真搜一次，交出去的是 `duplicate(true)` 副本；前提是探针为纯函数，它一旦引入随机重启或时间预算这个缓存必须同时废掉）；**按用例而非按文件**分层（沿路地板、必经格支撑、待机不复位、走路不掉、走下路面复位等 C3 核心断言仍留 fast 层）；以及同日的 `fixed_mul_div_fast_path`，它让搜索本身快了一个数量级。实测全量 **354.6 s → 24.4 s**（fast 18.5 s + slow 7 s）。另落 `tools/test-selector/`（`npm run test:gut:affected`）：按 `preload` / `load` 的 `res://` 字面量反向依赖图选受影响脚本，**只服务本地循环、永远不是门禁**，CI 仍跑全量；非 `.gd`、`tests/support/`、`addons/`、图外路径与非字面量 `load()` 一律偏向多跑。`--bot-run` 仍不进 PR CI（C2 第一章拍板），改由新增的 `nightly.yml` 每天跑一次。分支保护必过名单不变（`godot` job 名未改）。**本条初拍时曾附带「slow 层移出 PR、改 nightly，接受合并门禁被削弱」，同日因 `fixed_mul_div_fast_path` 把 slow 层压到 7 秒而由人类改判收回；没有覆盖损失。** 口径见 [CD-53 §4.1/§4.2](../50-engineering/53-testing-and-ci.md)。该句中 nightly 「只跑裸 `--bot-run`」被 `bot_runner_tool = thin_node_shell`（2026-09-11）补充：M5 C5 把 [CD-41 §5](../40-technical/41-architecture.md) 已列的 `tools/bot-runner/` 建成薄壳，**不从 CD-41 删除该目录**。产品入口 `npm run bot-run` 调已有 Godot `--bot-run`；`--report=` 写出每课 `completable` / `steps` / `search_ticks` / `expansions` 与退出码。0 条 `bot_run_*` 行 exit 1。nightly 经该薄壳跑匹配白名单五张 + `course_01 --route=safe`，报告 upload-artifact。`--bot-run` **仍不进 PR CI**。执行 [M5 计划](../../docs/plans/m5-traprush-vertical-slice.md) §5.2 问题 5 的 bot-runner 半边（A2 已执行 audio-bank 半边）。不接受 `res://`。口径见 [CD-53 §4.2](../50-engineering/53-testing-and-ci.md)。
- `fixed_mul_div_fast_path = native_int64_when_no_overflow`（2026-09-01）：`Fixed.try_mul_div` 在 `|left * right| ≤ INT64_MAX` 时改走原生整数乘除，溢出才回退到原有的软件 128 位限位长除法。两条路径都向零截断、符号都由三个操作数异或决定，**结果逐位相同**，由 `test_fixed_mul_div_paths.gd` 的差分用例钉死（边界值 + 碰撞形状输入 + 4000 组定种子伪随机，22,318 条断言）。这不改数值合同，[ADR-0005](../../docs/adr/0005-fixed-point-numeric-model.md) 与 [CD-42 §1.1](../40-technical/42-contracts-and-rulevm.md) 的 Q48.16 / 向零截断 / 溢出拒绝一个字没动。起因是离线 Solo 掉到 20–30 FPS：`try_mul` 单次 176 µs（`_div_mags` 逐位长除 128 轮，每轮新建三个 `PackedInt32Array`），而一次 `is_pose_blocked` 要对 49 个静态盒各做 4 次，整帧 33.7 ms。改后 `try_mul` 1.4 µs、整帧 1.664 ms（20×），`--bot-run` 三张课 108.6 s → 5.7 s 且动作序列逐字段不变。同刀把离线权威推进与输入采样从 `_process` 搬到 `_physics_process`（固定 60 Hz，与 `match_server.gd` 同一节拍）——原本仿真节拍等于帧率，慢时慢放、快时两倍速；并把对局壳每帧三次 `_apply_snapshot_map` 收敛为一次，`MatchStandingMap` / `MatchCrateMap` / `MatchHazardMap` 从全清全建改为按稳定键复用节点。**仍然欠着**：扫掠步数正比于 `|dy| / radius` 且无上限（宪法第十七条缺口），`is_pose_blocked` 仍是 O(静态盒数) 全量扫描、无空间划分。数字与诚实边界见 [audit](../../docs/audits/2026-09-01-offline-frame-cost.md)。该句中「扫掠步数……无上限」被 `sweep_step_budget = reject_over_256`（2026-09-02，纠偏 C5 第 15 章）覆盖：单次离散扫掠最多 256 样本，`ceil(|d|/radius)` 超限则拒绝整段 `try_move_*` / `until_blocked`（与溢出相同），**不**粗化取样，因此预算内落点与回放哈希不变。`radius=1` 下落一整格（2026-08-28 CI 事故）立刻失败，不再取样 65536 次。生产胶囊 `SCALE/8` 单次最多走 32 格。零半径仍只查终点。不是产品速度，不改协议 / Schema / 官方课 JSON / 冻结常量数字。空间划分仍欠。数字只在 [CD-42 §1.1](../40-technical/42-contracts-and-rulevm.md) 与 `SimulationWorldMove.MAX_SWEEP_STEPS`。该句同时覆盖 `ci_green_claim` 与 `e9_remaining_split` 里「扫掠步数无上限另立深审章 / 仍欠」。该句中「空间划分仍欠 / `is_pose_blocked` 全量扫描」被 `static_box_index = uniform_cell_broadphase`（2026-09-02，纠偏 C5 第 16 章）覆盖：静态盒走均匀格阔相，窄相与 ID 顺序与全量扫描相同；溢出或单盒超过 125 格走 always-test。胶囊—胶囊仍线性。不改协议 / Schema / 官方课 JSON / 冻结常量数字。数字只在 [CD-42 §1.1](../40-technical/42-contracts-and-rulevm.md) 与 `SimulationWorldIndex`。
- `m5_exit_gate = two_manual_checklists_pending_signature`（2026-09-12，M5 C6）：M5 十一章全交后，退出门禁落成两份**人工清单**并等人类签署，AI 不得代签、不得把 M5 写成已退出（宪法第十八、二十四条）。`docs/runbooks/network-fault-check.md` 是 [CD-53 §2.5](../50-engineering/53-testing-and-ci.md#25-网络仿真人工清单非门禁) 九项的执行版：按 `net_fault_tooling = clumsy_netem_external` 引用外部工具命令行（不入库、不进 CI、不写 npm script），并把九项拆成**链路整形**与**应用层帧注入**两段——理由是 WebSocket over TCP 会修掉链路层的丢包 / 乱序 / 重复，用链路整形声称"应用层抗乱序已验证"是假陈述；v1 快照是全量帧，**基线丢失**改验"重连后贴最新权威帧"。`docs/runbooks/playability-signoff-traprush.md` 是 [CD-61 §2 M5](../60-plan/61-milestones.md#m5traprush-纵向切片验收) 产出 4 的执行版：八项（操作手感 / 机关可读性 / 路线选择 / 失败反馈 / 音频反馈 / 结算清晰度 / 创作流畅度 / 外人 5 分钟上手），其中"外人 5 分钟上手"是**负责人自评**，不组织外部真人试玩（[CD-53 §1.1](../50-engineering/53-testing-and-ci.md#11-不对称强度策略)），签"通过"不得表述为已验证外部可用性。**未覆盖**：清单内容归属仍在 CD-53 §2.5 / CD-61；不改 [CD-43 §4](../40-technical/43-networking-and-replay.md#4-已锁定的网络参数) 锁定值；不新增速率限制或作弊处置（宪法第二十五条与 [CD-62](../60-plan/62-risk-register.md) 的风险接受不变）。签署完成后由人类授权把 CD-61 改成「已退出」并在本节追加覆盖行；下一动是字体入包，不是 M6。该句被 `milestone_m5 = exited_basically_pass_2026_09_13`（2026-09-13）覆盖：人类执行完两份清单并签署，**结论原文是「基本通过」，不是无保留的「通过」**，下游一律照抄这四个字。依据：A1–C6 十一章全交；`npm run bot-run` 五张官方课全 `completable`；GUT 1645/1645；可玩性八项均「通过」（签署人 kofi 项目负责人）。**退出时带走两项已知缺口，不得表述为已解决**：(1) 网络故障九项中**乱序与重复包未严格注入**——macOS `dnctl` 无 reorder / duplicate 开关，TCP 重组去重是协议推断而非本项目实测，待 Linux `tc netem reorder / duplicate` 补；(2) 可玩性签署的对象是**当前自绘大厅 + 占位美术 + 占位数值**那一版，UI 接线三批、定稿美术或 D-F4 / D-F5 / D-F6 变动后须重签。**未覆盖**：`e6_playability`（2026-09-02 那次色块阶段结论）仍是独立记录；[CD-53 §4.4](../50-engineering/53-testing-and-ci.md#44-发布候选) 的**发布候选**清单仍未签，本行不得被当成可发布的依据；网络故障仍非 CI 门禁。下一动是字体入包（子集范围仍未拍板），然后 UI 接线第一批 S3。

## D.9 明确延期或跳过

- `font_packaging = before_ui_wiring`（2026-09-12）覆盖 `font_packaging = defer_to_phase1_end`（2026-09-03）：字体入包从一期收尾**提前**到 M5 退出之后、产品 UI 接线第一批之前。理由是 2026-09-11 落库的 UI 基础包主题未内置字体，中文靠引擎回退，不入包则任何一屏产品 UI 都无法验收。选型不变（思源黑体 / Noto Sans SC，子集化）；**许可证已由人类于 2026-09-12 确认**（均为 SIL OFL 1.1，允许嵌入 / 子集化 / 再分发），宪法第十八条这一项关闭；子集范围仍未拍板，AI 不得自选。不覆盖本地化键口径（`ui_copy = csv_keys_zh_en_no_font` 的键表与 locale 归一仍生效）。口径见 [CD-11 §8.2 第 3 条](../10-product/11-scope-and-platforms.md#82-表现规格d42026-08-26-拍板当前生效值) 与 [CD-61 §2 M-Art](../60-plan/61-milestones.md#m-art表现与美术)。
- `ui_wiring = three_batches_s3_s1_rest`（2026-09-12）：2026-09-11 落库的产品 UI 基础包分**三批**接线——第一批 S3 广场（M5 退出后），第二批 S1 主大厅（M6 主大厅壳章内，随 BASTION 双玩法入口），第三批 S2 / S4 / S5 / S6。一批一刀，不合并、不提前、**不在 M5 C6 期间接任何一屏**（C6 要签的可玩性清单描述的是现有自绘大厅）。第一批四项前置：字体入包、文案迁 `UiCopy`、`validate_theme.gd` / `validate_scene.gd` 进 CI、`ui-wiring.md` 按仓库实际落点重写。S4 / S5 / S6 尚未设计。不发明新里程碑号。口径见 [CD-61 §2 M-Art](../60-plan/61-milestones.md#m-art表现与美术)。
- `net_fault_tooling = clumsy_netem_external`（2026-09-12）解除 [M5 计划](../../docs/plans/m5-traprush-vertical-slice.md) §5.2 问题 6 对 C6 的阻断：Windows 用 clumsy、Linux 用 `tc netem`、macOS 用 `dnctl` + `pfctl`。外部工具**不入库、不进 CI、不写成 npm script**，仓库里只有 runbook 引用其命令行。不覆盖「网络故障测试非门禁」（宪法第二十四条）。清单所有者仍是 [CD-53 §2.5](../50-engineering/53-testing-and-ci.md)。
- `bug_reporting` 及 Bug 提交、客服、通知、告警展示等同类问题：跳过，后续开发再决定。
- 道具栏、具体数值、时长、动画、精确频率等实现级玩法细节：统一延期。定点数尺度已迁出，见 D.8 `fixed_point_contract`。该句中「复活硬直」秒数被 D.3 `traprush_respawn_stun = 1_second`（2026-08-28）覆盖；该句中「动画」的**状态名与优先级**被 D.8 `play_anim_state = presentation_priority_table`（2026-09-02）覆盖；该句中 Tick / 快照 / 心跳 / 插值步 / 输入采样 / 对账频率被 D.8 `e3_lock = promote_existing_stubs_2026_09_02`（2026-09-02）覆盖；clip、时长与绑定仍延期。其余具体数值仍延期。
- `hidden_state_sync`：延期，不默认采用任何推荐协议。

## D.10 文档体系

- `docs_structure = index_first_single_source`（2026-08-20）：将 2129 行一体化初稿拆分为 `Confirmed-docs/` 文档群；宪法常驻，其余按主题分章按需读取。原初稿退役为归档，不再作为依据。
- `current_values_section = overwrite_not_append`（2026-09-02，纠偏 C5）：除索引外每份所有者文档文首有「当前生效值」节，覆盖而非追加；30 秒可读完。历史覆盖链留在本文件或 ADR。CD-41 §5 巨段与 CD-21 连续 dated 实现落点在本次收敛。口径见各所有者文首与 [纠偏方案 C5](../../docs/plans/course-correction-2026-08.md)。
- `cd61_rearrangement = approved_writeback`（2026-09-02）覆盖此前的 `cd61_rearrangement = draft_pending_human`：人类接受草案 §8 全部条目，回写 [CD-61](../60-plan/61-milestones.md)。M-Export / M-Art / M4a / M4b 自此是拍板里程碑。该句中「纠偏冻结令仍优先于本文件进度压力」被 `course_correction_freeze = lifted`（2026-09-03）覆盖：E1–E14 全绿后人类解除 §1。此后本文件进度顺序生效。账号挂 M4b，不发明 M8。口径见 CD-61 文首与 §5，对照 [freeze-lift audit](../../docs/audits/2026-09-03-freeze-lift.md)。该句中解冻后立即开 M-Export TLS / PR 沙盒被 `creator_playable_insert = xyz_pickup_preview_panes`（2026-09-03）覆盖：人类拍板插入「创作者可测」章（XYZ 放置、Place bomb/dash、Preview `MODE_WINDOWED` 并排；同日收口为按主视口对半不重叠、Editor 地板、点选拖拽）；公开 TLS 与每个 PR 的 Web 沙盒仍属 M-Export 产出，开工排到 **M5 之后**。不发明 M8。口径见 [CD-61](../60-plan/61-milestones.md)、[CD-32](../30-ugc/32-editor-and-preview.md)、[CD-53 §4.1](../50-engineering/53-testing-and-ci.md)。该句中解冻后「先 M-Export 安卓/iOS 再 M-Art」被 `f_line = playability_insert_before_m4a`（2026-09-07）覆盖：开 F 线（趣味性增强，不是新里程碑号）；F1–F4 在 **M4a 之前**；**M-Export 安卓 / iOS 烟测排在 F 线之后**。示范课 `course_f_playable` 是 F 线验收对象，不计入 M5 官方课 3～5 张。不发明 M8。口径见 [CD-61](../60-plan/61-milestones.md)。该句中「安卓 / iOS 烟测排到 M5 纵向验收之后」被 `cd61_order = web_first_mobile_phase1_end`（2026-09-07）覆盖：人类确认三点——Android / iOS **一期收尾**再做；**Web 游玩分发是第一刀**（浏览器能跑、实现可验证）；外人用链接 + 端口接入自备 VPS，且 **测试期允许 Web Solo**（覆盖 D.1 `web_offline = native_only`）。公开 TLS 与每个 PR 的 Web 沙盒仍在 M5 之后。不发明 M8。口径见 [CD-61](../60-plan/61-milestones.md) 与 [CD-13 §3](../10-product/13-account-and-session.md)。该句中「Web 游玩分发是第一刀」被 `web_play_distribute = browser_solo_host_port`（2026-09-08）覆盖：第一刀已交；下一动是可玩性深化。该句中「下一动是可玩性深化」被 `playability_batch_1 = wayfind_setback_conveyor_creator_entry`（2026-09-08）补充：可玩性深化第一批已交六件——跳变镜头滑行、多层寻路读数、失败原因读数、传送带、传送门占位模型与课内动效、玩家包内创作入口（含 surface 能力分级与 Preview 连续试玩）。三条诚实边界：失败原因**不进 v1 快照帧**（线上读不到）；**开关门 / `InteractIntent` 未做**（需命令帧新增 intent id + 快照帧新增开合状态两处不兼容变更，宪法第十八条）；**Web 浏览器上未实测**（开发机 `export_templates` 为空）。`SimulationBundle` 新增可选 `conveyors` 袋，省略与空数组等价、旧编译体照常解码，因此不是 Schema 破坏性变更。该句被 `playability_batch_2 = conveyor_on_course_lift_launch`（2026-09-08）补充：第二批已交三件——示范课 `course_f_playable` revision 4 接上传送带侧廊；电梯（竖直 `mover` + `zone.tags` 的 `lift`，不另开袋）；弹射垫（可选 `launches` 袋，支撑上升沿弹一次）。不改 Component Schema v1、不改协议帧。该句中「开关门仍欠」被 `playability_batch_4 = pressure_plate_gates`（2026-09-09）覆盖：踩区开关门已交——固体 + `switch`/`gate` 标签 + 已有 `interactable.link_group`；可选 `switches`/`gates` 袋（省略 ≡ 空）；开合是当前占用的纯函数，不写 `state`、不进快照。`InteractIntent` 仍未接线。示范课 revision 5 把青绿开关放在出生点 +X 第一格（紫墙在其 +Z 一格，不挡冲线）。不改协议帧、不改 Component Schema v1。该句被 `playability_batch_5 = energy_wall_and_readable_gadgets`（2026-09-09）补充：第五批已交——能量墙（可破坏占用 + `zone.tags` 的 `energy_wall`，可选 `energy_walls` 袋，打碎走已有 UseItem）；`OccupancyGadget` 给传送带 / 电梯 / 弹射垫 / 开关 / 门 / 能量墙挂程序化占位；示范课 `course_f_playable` revision 6 在主路 x=5、+Z 一格能量墙。权威碰撞仍是一格盒。不改协议帧、不改 Component Schema v1、不改爆破半径。该句被 `playability_batch_6 = occupancy_portal_switch`（2026-09-09）补充：第六批已交——开关传送（传送占用 + `zone.tags` 的 `portal_switch` + 已有 `interactable.link_group`，可选 `portal_switches` 袋，关上不落地）；未开时旋翼停、变暗；示范课 `course_f_playable` revision 7 在主路 x=4、+Z 一格接出 `link_group=2` 开关传送。传送门自己的占用不开组。`InteractIntent` 仍未接线。不改协议帧、不改 Component Schema v1。该句被 `playability_batch_7 = spike_flame_crusher_occupancy`（2026-09-09）补充：第七批已交——地刺 / 喷火 / 压板（可选 `spikes` / `flames` / `crushers` 袋，省略 ≡ 空）；地刺被支撑才 hazard；喷火永远非固体、半周期开时重叠才烫；压板重叠且非乘客 crush。示范课 `course_f_playable` revision 8 在主路 +Z 一侧接出三机关。不改协议帧、不改 Component Schema v1。该句被 `playability_batch_8 = roller_rubble_obstacle_core`（2026-09-09）补充：第八批已交——滚柱（周期机关 + `roller`，半周期固体挡路）；碎石 / 障碍核心（可破坏占用 + 标签，打碎走已有 UseItem）；示范课 revision 9。不改协议帧。该句被 `playability_batch_9 = item_hud_bomb_dash`（2026-09-09）补充：第九批已交——Solo / Preview 道具栏 HUD 显示爆破球 / 冲刺持有数；线上快照无该字段。该句被 `playability_batch_10 = pendulum_ice_fail_hud`（2026-09-09）补充：第十至十二批已交——摆锤（水平 `mover` + crush）；冰面（走路步长滑）；失败次数 HUD；示范课 revision 10。不改协议帧、不改 Component Schema v1。该句被 `playability_batch_13 = shatter_floor_crush_warn`（2026-09-09）补充：第十三至十五批已交——可破坏碎裂反馈（纯表现碎片）；楼层着色（上层偏绿、下层偏品红）；压板 / 摆锤预警脉冲；Preview 开玩后打碎的箱子隐藏。不改协议帧。该句中示范课 revision 10「不挡 +X 冲线的侧廊展览」被 `course_f_playable = routed_run_revision_11`（2026-09-09）覆盖：示范课改成有路线选择的一局——危险捷径沿 +X（能量墙挡在 x=1，喷火 / 滚柱 / 摆锤 / 压板在主路上），安全路沿 z=+2（途中下到检查点 1，经开关门与冰面）。`--bot-run --course=course_f_playable` 重放危险捷径脚本。不计入 M5 官方课，匹配 HTTP 仍只 01–03。不接线 `InteractIntent`。口径见 [CD-61](../60-plan/61-milestones.md)、[CD-21](../20-gameplay/21-traprush.md)、[CD-32](../30-ugc/32-editor-and-preview.md)、[CD-42 §3.4](../40-technical/42-contracts-and-rulevm.md#34-实现落点)。该句被 `playability_batch_18 = demo_readable_official_side_gadgets`（2026-09-09）覆盖：示范课 revision 12 把喷火 / 滚柱 / 摆锤 / 压板移到 −Z 侧廊，捷径只挡能量墙（UseItem reach 只探 +Z，须站其 −Z 侧 Q），脚本不再 `wait`；官方 01–03 侧廊接上深化段机关且不挡既有捷径 / 安全路 / 破箱上楼脚本。官方 01/02 不加额外可破坏。匹配 HTTP 仍只三张。不接线 `InteractIntent`。该句中「人类宣布收口前不进 M4a」被 `playability_insert = closed_2026_09_09`（2026-09-09）覆盖：人类验证第十八批后宣布可玩性深化收口。第一至十八批保留为历史。下一动是 M4a Rule VM。不发明 M8。口径见 [CD-61](../60-plan/61-milestones.md)。该句中「下一动是 M4a Rule VM」被 `rule_vm_v1 = envelope_whitelist_gas`（2026-09-09）覆盖：首章已交，见上 D.8 同键。不发明 M8。该句中 M4a 收口后的「下一动是 M4b」被 `content_sign = sidecar_hmac_sha256`（2026-09-09）覆盖：M4b 第 1 章已交，见上 D.2 同键。不发明 M8。
- `web_light_edit = browser_closed_loop`（2026-09-09）覆盖 `playability_batch_1` 的「Web 浏览器上未实测」：`/play/?edit=1` 保留查询串；关窗 / 返回大厅拉回大厅；Web 草稿 `force_fs_sync`。规则模板仍属 M4a。口径见 [CD-32](../30-ugc/32-editor-and-preview.md) 与 [CD-61](../60-plan/61-milestones.md)。
- `web_tls_gate = public_ops_domain_not_c1`（2026-09-02，回写 D2/D11）：Web 仍是一期持续入口；域名 + 受信证书在公开运营前补齐，不在 C1。测试期明文见 D.8 `c1_test_remote`。口径见 [CD-11 §9](../10-product/11-scope-and-platforms.md#9-公开-web-与传输安全d2--d11) 与 [CD-51 §5](../50-engineering/51-dev-environment.md)。
- `cd62_c5_status_correction = pack_portal_review_netlock`（2026-09-02）：「网页/微信包体超限」改回未开始；「传送迷路或跳关」改为已缓解（镜头过渡零实现；该括注被 `playability_batch_1`（2026-09-08）覆盖：跳变镜头滑行 + 多层寻路读数 + 头顶方向箭头已交）；新增「单人审查带宽超载」与「在零延迟条件下锁定网络参数」。`_mcp_game_helper` 以第一次真导出为准。口径见 [CD-62](../60-plan/62-risk-register.md)。该句中「锁定仍待人类拍板」与 CD-53 文首「可玩性签署未发生」被 `c5_batch = complete_e9_lock_e11_facts`（2026-09-02，纠偏 C5 第 17 章收口）覆盖：C5 原产出与后续 E9 / E1 / 扫掠 / 静态盒阔相均已合入。生产路径 `game/src/**/*.gd` 与 `backend` 非测试 `*.ts` 由 `tools/dev-launcher/tests/e9_production_line_cap.test.ts` 锁 < 400 行。CD-62「零延迟锁定网络参数」按 E3 标已治理；CD-53 文首按 E6 标已签好玩。C5 批次结束。冻结令仍在，须人类解除。E10、字体入包、触控 UI、按 `asset_id` 解析、传送门模型、胶囊—胶囊仍线性不是 C5 退出条件。对照见 [c5-close audit](../../docs/audits/2026-09-02-c5-close.md)。该句中「冻结令仍在」被 `course_correction_freeze = lifted`（2026-09-03）覆盖：人类选 E10 选项 A（不设周 PR 上限；深审 = PR 失败模式 + 人类批准），并把字体入包推迟到一期收尾，然后解除 §1。触控 UI、按 `asset_id` 解析、传送门模型、胶囊对胶囊仍线性仍欠，不挡解冻。对照见 [freeze-lift audit](../../docs/audits/2026-09-03-freeze-lift.md)。
